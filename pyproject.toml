[project]
name = "voxys-tools"
version = "0.1.0"
description = "Python tooling for Voxys assets, screenshots, and Terrain Diffusion imports."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pillow>=11.0.0",
]

[tool.uv]
package = false
