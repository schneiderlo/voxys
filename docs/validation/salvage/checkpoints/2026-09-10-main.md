# Main checkpoint — 2026-09-10

The owner explicitly requested committing and pushing the current work to `main`. This is a work-in-progress checkpoint, not a passed game gate or approval of the current visuals. The full implementation goal through G14 remains active.

The fetched remote main was `7563f61fd536df7de5d209ff35d3cd2099ebcbb6`; the local branch already contains it and the prior G00 checkpoint `7f28fab`.

## Playable state

- The Cove retains stepped brick terrain and studs.
- Its workshop edits authored boat modules, with inventory costs, owned parts, design storage and launch/save/reload.
- The cutter can separate welded sections. Player support, independent roots, protected intact designs, rescue and starter rebuild follow the verified bounded paths described in [the cutter report](../MECH-05/live-cut-r01/README.md).
- This is still an unfinished prototype. The next milestone, LEGO-02 in the root implementation plan, is direct construction with individual 1×2, 2×2 and 2×4 bricks. That editor has not been delivered. Visual polish and the remaining game gates are not complete.

New offline brick models and their compiler checks are also included; see [brick assets](../LEGO-02/brick-assets-r01/README.md). They do not change the playable palette yet.

## Repository contents

The checkpoint contains current code, shaders, browser UI, build integration, reproducible authoring tools, every asset bundle referenced by the installed salvage registries, their source assets, validation Markdown, test XML and compact structured reports. Existing tracked history is preserved.

Large local captures, diagnostic pages, raw execution logs, oversized historical raw reports and duplicate frozen source snapshots remain in the local workspace. They are not deleted or claimed to be downloadable from this commit. Historical evidence may link to those local artifacts; use the report's recorded scope and source hashes when interpreting an older result.

## Verification

`bazel test //tests:voxy_tests //tools:terrain_diffusion_import_test --jobs=4 --test_output=errors` passes. The full native suite takes 1,080.8 seconds: 2,038 pass, three skip, four pre-existing application cases remain disabled. The skips are headless GLFW window creation and two opt-in benchmarks. The terrain-import target passes its Python suite with ten cases passing and one skipped. See [structured results](2026-09-10-results/summary.json) and the accompanying test XML.

The first run reached 1,585 of 2,041 enabled cases with no assertion failures before its 900-second timeout. The combined test target now has an explicit bounded one-hour timeout. No test was removed or filtered; the normal commit hook remains enabled.

Browser checks pass: twenty control/lifecycle cases, seventeen Cove save cases and four expedition storage cases. Both shader-source consistency checks pass. The new brick checker builds and passes through both CMake and Bazel. Its source/asset evidence is linked above.

This checkpoint does not claim the skipped checks passed, a new whole game gate passed, final visual acceptance, hardware performance or the playable brick editor. Successful creation of this commit also requires the unchanged pre-commit hook to run the required repository suite.
