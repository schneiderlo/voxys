# Moving machinery and recovery guidance

Status: verified combined D39–D44 checkpoint after published `411fa1cfc7e6a396b2d59b14b04f90eb4b83f6f8`. Native machinery controls and save/restart pass. The final native recovery/guidance/save/harbor-power journey also passes. Final browser controls also pass. Final native and browser older-save continuations also pass with the nine-presentation kit. The completed repository suite passes: 2,096 game cases, three skips and four disabled; terrain import ten passes and one skip. The containing commit records publication; this checkpoint does not complete a full game or visual gate.

## Player-facing change

The propeller rotates with accepted effective propulsion. Reverse thrust reverses it; neutral, disabled drive, pause and workshop stop it. Its guard remains fixed. The winch drum turns with the accepted cable-length change, including motor start/stop; the frame and fairlead stay fixed. Motion uses the existing physical body and existing lit material/depth/shadow rendering, with no additional physics parts or saved animation state.

A teal lane on the dock leads around the generator to an orange boarding pad. The markings sit on existing admitted dock faces, clear their panel edges and sockets, and follow the same camera-relative static root. They disappear in the workshop. There are no new collision objects, part identities or save entries.

The native presentation path also distinguishes a temporary surface-acquisition timeout from a real error. A skipped frame reserves no renderer/physics work. It emits one warning per consecutive timeout run and records reacquisition; genuine errors and validation scanners remain intact. Exact pinned native/WASM API constants, context move/reset behavior and an observed recovery are documented.

The native HUD guides the first recovery from accepting the job through building, boarding, hooking, lifting, delivery and harbor power. It shows actual unmet conditions and distinguishes a pending save from durable success. The browser’s Next objective card promotes the current existing action, keeps its exact permissions, and hides while building. The same action is checked again at click time.

A valid Launch now waits for bounded GPU upload capacity. Already accepted shape handles and unconsumed payloads are retained until the existing safe launch boundary can proceed. Terminal errors retain partial ownership for cancellation, leave the build unchanged and report a failure. The documented F9 frame-cap toggle also works in the Cove and workshop.

The generator and cargo cradle complete the visible kit’s molded cream/teal/orange palette, with service panels, accent pads and studs. Six new strictly cooked LODs preserve exact canonical metadata and working interfaces. Independent geometry review confirms every original vertex position is retained, with at most 9 mm/12 mm decorative expansion. Existing saves select the new visual packages without altering physical or economic identity. [Asset sources, geometry/cook evidence and old-save continuations](../LOOK-01/cargo-art-r01/README.md).

## Evidence

[Component source, art/cook provenance, retained failures, focused tests, exact package hashes and real-control journeys](../LOOK-01/driven-cove-r01/README.md). No screenshots or generated images were used.

The [final 88-file changed-input inventory](driven-cove-r01/final-code-inputs-r02.json) binds code, assets and validation drivers to predecessor `411fa1cf`; unchanged inputs are supplied by that commit. The [final 27-file application package manifest](driven-cove-r01/final-package-r02.json) identifies the nine-presentation native/browser packages. Earlier component manifests remain historical evidence. The [independent release audit](driven-cove-r01/release-review-r02.md) checks source/package integrity and evidence scope.

Thirteen focused mechanism/registry/owner cases pass. A separate numeric GPU case proves fixed frame regions, moving rotor/drum regions, exact live-body transforms at large sectors and rejection of stale body generations. Three dock CPU cases and the corrected single dock GPU case pass. Numeric inlay readback changes 78 opaque pixels, all with the expected nearer depth, and hiding it restores the prior color/depth exactly. Initial compile/test-driver errors and the actual reel-baseline defect remain recorded with their corrections; partial results do not substitute for a completed journey.

The seven-presentation mechanism/dock package requests 10,753,144 GPU bytes including 7,760 inlay bytes. The final nine-presentation generator/cradle package reduces that to 9,731,960 bytes; the actual GPU fixture without dock reports 9,724,200 bytes. Its supported 64-large-brick design plus palette uses 160 color draws. Inlay adds two material draws. Existing 16/48 MiB owner/resident limits, 256 instances, 512 expanded draws and 119,336 fixed requested bytes within the 128 KiB reservation are unchanged.

Native journey r06 passes 39 recorded stages and 11 phase comparisons in 68.056 seconds on frozen native package r04. It exercises real keyboard movement and both free settings refits, all propulsion directions/stop/disabled cases, actual hooking/reel/pay/release, workshop/pause freeze and durable save/process restart. Both process error arrays are empty. Exactly 11 parts, 1,035 kg, 48 material, zero paid part IDs and the disabled/reversed propeller setting survive restart; complete owned part/connection bytes are compared. An extra real reel pulse is used only when opposing accepted motions cancel exactly, so the restart begins from a demonstrably nonzero ephemeral drum phase.

Eight native HUD/guidance cases pass. The final native application then passes 42 recorded stages using actual controls, including a paid lifting rig, real delivery, durable process restart, refusal of a duplicate reward, return to the dock and actual K installation followed by durable harbor power. Both process error arrays are empty. The saved powered archive retains 11 parts, 1,035 kg and schema 4. [Native guidance, exact state/archive and validation](../UX-01/recovery-guidance-r01/README.md).

The browser controller passes 36 assertion groups, including ten new objective groups. The real browser objective check passes five stages at 1280×800 and 640×480, including Accept and workshop hide/restore. The initial two-layout attempt missed its Accept click. The next attempt records one trusted physical click followed by exactly one forwarded DOM click and passes in 3.033 seconds; its additional passive observations changed timing, so the first failure’s cause remains unresolved. No product fix is inferred from this pass. [Objective-card decisions, tests and runtime evidence](../UX-01/objective-card-r01/README.md).

Two scripted-resource launch cases pass for partial admission/retry and terminal ownership, alongside the existing actual GPU eight-operation full-ring/payload-preservation/drain/retry case. These remain distinct CPU and real GPU evidence. The final browser refit journey verifies the integration without added retry clicks.

Final browser mechanisms pass all 28 stages in 84.663 seconds after the five-stage objective check in the same browser process. Actual direction, neutral/disabled behavior, both free settings launches, hook/reel/pay/release, workshop/pause freeze and durable save/reload preserve the exact 11-part, 1,035 kg boat, 48 material and propeller settings. The outer report passes with empty browser errors, sample errors and error-console entries. Native and browser results attest their documented frozen packages; no unchanged native mechanism matrix was repeated.

The final nine-presentation art package passes two focused CPU/GPU fixture cases and both application builds. Short native and browser continuations each pass four stages from actual older saved boats, preserving exact owned part/connection/settings data, 11 parts, 1,035 kg and 48 material. Native takes 3.542 seconds; browser takes 2.371 seconds. Both prove later completed GPU/physics work on the same current owner. Normal restore can republish the archive generation; whole archive byte equality is not claimed. The first browser art attempt failed an invalid assumption that a transient shadow diagnostic must remain constant on every poll. The corrected driver matches each mode and flag, then proves a strictly later completed submission. That failure and its correction remain archived; no renderer change or precise false-sample boundary is inferred. [Final package, four-stage results and retained failure](../LOOK-01/cargo-art-r01/README.md).

The initial mandatory run was deliberately interrupted after 520.422 seconds when compatible generator/cradle candidates became ready for this same checkpoint. It returned exit 8 and `voxy_tests` had NO STATUS; this is not a passed full suite. The final completed suite covers all D39–D44 inputs after their focused/runtime checks: 2,096 game cases pass, three skip and four remain disabled; terrain import passes ten with one skip from its valid cache. Bazel elapsed time is 1,132.896 seconds; the test XML records 1,125.548 seconds for game cases. [Exact results, names, compressed XML/log and raw invocation](driven-cove-r01/checks/summary.json). All 88 frozen executable inputs still match after the run. The normal commit hook remains enabled and can reuse those valid results. Remote main was fetched before publication and matches predecessor `411fa1cf`; the containing commit and verified remote main identify publication. Browser timing observations came from the special headed X11 smoke setup with focus emulation; they do not establish normal user-browser frame rate. No scheduler change or performance acceptance is claimed. Parked second-job source, robot/art approval and all full implementation gates remain outside this checkpoint. The authorized session stops 2026-09-12 07:27:33 UTC.
