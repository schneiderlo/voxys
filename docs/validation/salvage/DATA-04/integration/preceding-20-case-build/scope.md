# Preceding integration checks

These are the initial 20-case native checks and app builds, before independent review corrected bootstrap participant references. They are retained as historical evidence, not final DATA-04 acceptance. The first CMake WASM build also lacked explicit catching support; later configurations supersede it.

Pre-fix module source identities are recorded in `../../wasm-exceptions/attempt-01/manifest.json`. Application bridge identities are in `../../app-integration-review.md`. Final acceptance must use the corrected 21-case suite and explicitly enabled browser exception handling.
