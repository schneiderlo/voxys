# Remaining browser Resume and design proof

Source-only review of the final remaining steps, after actual r06 Leave/history navigation and strict reload. No product change or repeated settings, Test/Return, map or picker sequence.

- `Application::updateCovePlayer`, application.cpp around 8939–8945: a bound Pause edge outside an existing menu opens Overview, including a world already paused. The earlier driver expectation that one O directly resumes was incorrect. The final driver first requires actual paused Overview.
- `#cove-menu-resume` uses `data-cove-forward="salvage-pause"`. `cove_menu.js` forwards only an enabled visible original button. `salvage_preview.js::onPause` rereads the real state and calls action91 when paused. Application action91 requires Paused and successful scheduling resume, clears pending player/input state and enters Running.
- `cove_menu.js::tick` closes Overview on the next paused-to-running DOM observation. The final driver waits for Running, gamepad.menuOwner=false, details closed, and the read-only controller Active getter=false before pressing View. It does not assume the native state update and 100 ms DOM poll are atomic.
- Shared preferences define Workshop pad8 (View). The driver already verifies exact retained canonical preferences against the prior accepted UI apply. Application handles Workshop only after menu input is released; action60 requires actual berth/workshop availability. The save is the actual dock state from r05, and the driver waits for ready/open/no-pending before export.
- Blueprint action1 is the existing read-only export, accepted only in the actual workshop. It is compared byte-for-byte with r05's original owned design. World, build ID, topology, ordered root keys, part IDs, material, mass and preferences are bound to the actual Saved. baseline. Fresh process/body handles may change and are then held stable for submission/completion proof.
- Retained Chrome shutdown observes its owned ChildProcess close event, requests Browser.close first and records bounded signal fallbacks. Graceful close does not alone assert storage persistence. r06 actually restored the expected preferences/world and closed normally with exit0; r07 must independently restore the same state again.

The new driver mode is `VOXY_SMOKE_COVE_UI_RESTORED_DESIGN=<actual r06 summary>` together with the actual r05 saved-baseline report and `VOXY_SMOKE_RESUME_WORLD`. The branch admits r06 only if its retained history URL and strict loaded state identify that same saved world. It performs two captures in a 120-second ceiling: strict saved start, then explicit Resume/View/exact original blueprint.
