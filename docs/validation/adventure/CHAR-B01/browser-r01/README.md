# Warm LEGO-style character: bounded browser integration

The explicit owner correction is **LEGO-style people** in a warm building adventure. The first actual base figure is authored, strictly admitted and integrated with the revised browser interface. [Actual Blender model proof](../../../../../data/adventure/human-r01/proof.png) and [asset source/checks](../../../../../data/adventure/human-r01/README.md).

The isolated browser package `adventure-1a49bcffb8919e9a` was exercised through ordinary pointer/keyboard controls at `http://127.0.0.1:42753`. Conversation, quest acceptance, illustrated part selection, focus return, a confirmed save and a real reload with the active quest all passed. One visual inspection showed the actual toy characters and actual-mesh building thumbnails together, with the duplicate native overlay absent. The previous published G-A package/origin and all player saves were preserved.

[Exact operations, build identity, character preload equality, source hashes and remaining checks](record.json). The focused asset loader test passes and rejects robot/adventure identity substitution. Twenty-three browser DOM behavior cases pass; native layout/authority/storage evidence is linked from the active plan. A screenshot or startup does not replace gameplay verification.

**Still open:** distinct villagers, an inhabited warm starting area, actual-mesh native thumbnails, complete new-character movement/build/quest/compass/restart journeys on both hosts, controller/large-text/narrow-screen checks, and real owner feedback. The opening still inherits racing loading copy. CHAR-B01, UI-B01, WARM-B01 and G-B are not complete; no gate commit is claimed here.
