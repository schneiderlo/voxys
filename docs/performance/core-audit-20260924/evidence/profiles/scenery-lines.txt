voxys Nix shell
  setup deps: ./scripts/fetch_deps.sh
  build:      bazel build -c opt //:voxy_native
  fullscreen: bazel run -c opt //:voxy_native -- --fullscreen --uncapped
  benchmark:  bazel run -c opt //:voxy_native -- --fullscreen --benchmark --no-validation
Total: 5.12s
ROUTINE ======================== voxy::game::adventure::CreativeScenery::admit in /nix/store/qxaq7jz61a6zkr2mq49i0zvqip2m2jj8-gcc-15.2.0/include/c++/15.2.0/bits/stl_algo.h
         0        3ms (flat, cum) 0.059% of Total
         .          .   1901:    __sort(_RandomAccessIterator __first, _RandomAccessIterator __last,
         .          .   1902:	   _Compare __comp)
         .          .   1903:    {
         .          .   1904:      if (__first != __last)
         .          .   1905:	{
         .        3ms   1906:	  std::__introsort_loop(__first, __last,
         .          .   1907:				std::__lg(__last - __first) * 2,
         .          .   1908:				__comp);
         .          .   1909:	  std::__final_insertion_sort(__first, __last, __comp);
         .          .   1910:	}
         .          .   1911:    }
ROUTINE ======================== voxy::game::adventure::CreativeScenery::admit in src/game/adventure/creative_scenery.cpp
     1.08s      2.82s (flat, cum) 55.16% of Total
         .          .    190:    CreativeScenery out;out.initialized_=true;
         .          .    191:    const glm::dvec2 focus(state.player.x,state.player.z);
         .          .    192:    if(!terrain.valid()||!std::isfinite(focus.x)||!std::isfinite(focus.y)||glm::any(glm::greaterThan(glm::abs(focus),glm::dvec2(10000))))return out;
         .          .    193:    out.cell_=glm::ivec2(std::floor(focus.x/32),std::floor(focus.y/32));
         .          .    194:    const glm::dvec2 center=glm::dvec2(out.cell_)*32.+16.;
         .      995ms    195:    out.village_=CreativeVillage::admit(state,terrain,owned,previous?&previous->village_:nullptr,clearance);
         .        1ms    196:    std::vector<Solid> reserved(owned.begin(),owned.end());
         .       11ms    197:    if(!out.village_.appendSolids(reserved))return out;
         .          .    198:    // Admission envelopes below protect visual bounds, but never enter the
         .          .    199:    // published collision set. In particular, every paving tile has an envelope.
         .          .    200:    const size_t reservedCollisionCount=reserved.size();
         .          .    201:    // Include full roofs/sails as tree admission envelopes as well as physical
         .          .    202:    // walls. Otherwise a decorative canopy could grow through an open room.
         .          .    203:    for(const auto& g:out.village_.groups())reserved.push_back({{},{},g.minimum,g.maximum});
         .          .    204:    const bool continuing=previous&&previous->initialized_;
         .          .    205:    if(continuing)out.suppressed_=previous->suppressed_;
         .          .    206:    const glm::dvec2 forward(-std::sin(creativeStartYaw),-std::cos(creativeStartYaw)),right(-forward.y,forward.x);
         .          .    207:    struct Candidate {uint32_t id;Kind kind;glm::dvec2 point;uint8_t yaw;bool authored;const CreativeProp* tree=nullptr;};
         .          .    208:    std::vector<Candidate> candidates;
         .          .    209:    for(size_t i=0;i<sites.size();++i) {
         .          .    210:        const auto& s=sites[i];const auto point=creativeStart+right*s.right+forward*s.forward;
         .          .    211:        if(glm::length(point-center)<320)candidates.push_back({uint32_t(i+1),s.kind,point,s.yaw,true});
         .          .    212:    }
         .          .    213:    // Orchard and woodland clusters tie the settlement edge into the meadow.
         .          .    214:    // These stable IDs remain outside the starting clearing's ID range.
         .          .    215:    for(size_t i=0;i<villageTrees.size();++i)if(glm::length(villageTrees[i]-center)<320)
         .          .    216:        candidates.push_back({uint32_t(100+i),i%4==0?Kind::Pine:Kind::Broadleaf,villageTrees[i],uint8_t(i&3),true});
         .          .    217:    out.forestCell_=glm::ivec2(std::floor(focus.x/128),std::floor(focus.y/128));
         .          .    218:    out.forestTerrain_=terrain;
         .          .    219:    const bool sameTerrain=continuing&&previous->forestTerrain_.samples.data()==terrain.samples.data()
         .          .    220:        &&previous->forestTerrain_.width==terrain.width&&previous->forestTerrain_.height==terrain.height
         .          .    221:        &&previous->forestTerrain_.heightScale==terrain.heightScale&&previous->forestTerrain_.cellScale==terrain.cellScale;
         .          .    222:    if(sameTerrain && previous->forestCell_==out.forestCell_) {
         .        7ms    223:        out.forestSource_=previous->forestSource_;out.forestSourceTiles_=previous->forestSourceTiles_;
         .          .    224:    }
       1ms        1ms    225:    if(!out.forestSource_) {
         .          .    226:        // Adjacent regions share immutable 128-unit tiles. Travel generates
         .          .    227:        // only the entering fringe, preserving the exact original recipe.
         .          .    228:        const glm::dvec2 forestCenter=glm::dvec2(out.forestCell_)*128.+64.;
         .          .    229:        constexpr double radius=2144;
         .          .    230:        std::vector<CreativeProp> trees;trees.reserve(60000);
         .          .    231:        for(int z=int(std::floor((forestCenter.y-radius)/128));z<=int(std::floor((forestCenter.y+radius)/128));++z)
         .          .    232:        for(int x=int(std::floor((forestCenter.x-radius)/128));x<=int(std::floor((forestCenter.x+radius)/128));++x) {
         .          .    233:            const glm::dvec2 minimum=glm::dvec2(x,z)*128.;
         .          .    234:            const auto delta=glm::clamp(forestCenter,minimum,minimum+128.)-forestCenter;
         .          .    235:            if(glm::dot(delta,delta)>radius*radius)continue;
         .          .    236:            const auto key=std::pair(x,z);
         .          .    237:            std::shared_ptr<const std::vector<CreativeProp>> tile;
         .          .    238:            if(sameTerrain) {
         .          .    239:                const auto found=previous->forestSourceTiles_.find(key);
         .          .    240:                if(found!=previous->forestSourceTiles_.end())tile=found->second;
         .          .    241:            }
         .        4ms    242:            if(!tile)tile=std::make_shared<const std::vector<CreativeProp>>(generateForestTile(terrain,{x,z}));
         .          .    243:            out.forestSourceTiles_.emplace(key,tile);
         .          .    244:            for(const auto& tree:*tile) {
         .          .    245:                const auto distance=glm::dvec2(tree.feet.x,tree.feet.z)-forestCenter;
         .        3ms    246:                if(glm::dot(distance,distance)<=radius*radius)trees.push_back(tree);
         .          .    247:            }
         .          .    248:        }
         .        2ms    249:        std::sort(trees.begin(),trees.end(),[](const auto& a,const auto& b){return a.id<b.id;});
         .          .    250:        out.forestSource_=std::make_shared<const std::vector<CreativeProp>>(std::move(trees));
         .          .    251:    }
         .       18ms    252:    const Reservations reservations(reserved);
         .          .    253:    std::unordered_set<uint64_t> identities;
         .        2ms    254:    for(const auto& s:state.structures){identities.insert(s.id);for(const auto& p:s.parts)identities.insert(p.id);}
         .          .    255:    for(const auto& c:state.components)identities.insert(c.id);
         .          .    256:    const bool namespaceBlocked=identities.contains(creativeSceneryStructureId);
         .          .    257:    std::unordered_map<uint32_t,const CreativeProp*> previousProps;
         .        1ms    258:    if(continuing)for(const auto& p:previous->props_)previousProps.emplace(p.id,&p);
         .          .    259:    out.distantTrees_.reserve(out.forestSource_->size());
         .          .    260:    for(const auto& p:*out.forestSource_) {
         .        4ms    261:        if(out.suppressed_.contains(p.id))continue;
       5ms       80ms    262:        if(namespaceBlocked||identities.contains(creativeSceneryPartBase-p.id)||reservations.blocks(p)) {
         .          .    263:            out.suppressed_.insert(p.id);continue;
         .          .    264:        }
         .        1ms    265:        const glm::dvec2 point(p.feet.x,p.feet.z);
      18ms       24ms    266:        if(glm::length(point-center)<=forestCollisionRadius)
         .          .    267:            candidates.push_back({p.id,p.kind,point,p.yawQuarterTurns,false,&p});
         .       53ms    268:        else out.distantTrees_.push_back(p);
         .          .    269:    }
         .          .    270:    // Ground details follow grove edges instead of competing with canopy trees
         .          .    271:    // for a single scatter slot. Their identity range is separate from trees.
         .          .    272:    for(int z=int(std::floor((center.y-180)/24));z<=int(std::floor((center.y+180)/24));++z)
       1ms        1ms    273:    for(int x=int(std::floor((center.x-180)/24));x<=int(std::floor((center.x+180)/24));++x) {
         .          .    274:        if(x< -256||x>=256||z< -256||z>=256)continue;
         .          .    275:        const uint32_t h=forestHash(x,z,0x73293u);
         .          .    276:        const glm::dvec2 point(x*24+2+int((h>>8)%9),z*24+2+int((h>>16)%9));
         .          .    277:        if(glm::length(point-center)>180||glm::length(point-creativeStart)<72)continue;
         .          .    278:        const int woodland=noise(int(point.x),int(point.y),384,0x92317u);
         .          .    279:        const bool flowers=woodland>16000&&woodland<36000;
         .          .    280:        if(h%100>uint32_t(flowers?32:10))continue;
         .          .    281:        candidates.push_back({1100000u+uint32_t(z+256)*512u+uint32_t(x+256),flowers?Kind::Flowers:Kind::Rocks,point,uint8_t(h&3),false});
         .          .    282:    }
         .          .    283:    // Preserve authored clusters; prioritize nearby world cells if at capacity.
         .        1ms    284:    std::sort(candidates.begin(),candidates.end(),[&](const auto& a,const auto& b) {
         .          .    285:        if(a.authored!=b.authored)return a.authored;
         .          .    286:        if(a.authored)return a.id<b.id;
         .          .    287:        const auto da=glm::length(a.point-center),db=glm::length(b.point-center);
         .          .    288:        return da==db?a.id<b.id:da<db;
         .          .    289:    });
         .          .    290:    size_t collisionCount=reservedCollisionCount;
         .          .    291:    for(const auto& c:candidates) {
         .          .    292:        // Capacity defers a prop; only actual construction/clearance conflicts
         .          .    293:        // suppress its identity. A later refresh may have room for it again.
         .          .    294:        if(out.props_.size()>=maximumProps)continue;
         .          .    295:        if(out.suppressed_.contains(c.id))continue;
         .          .    296:        CreativeProp p;bool found=false,existing=false;
         .        2ms    297:        const auto old=previousProps.find(c.id);
         .          .    298:        if(old!=previousProps.end()){p=*old->second;found=existing=true;}
         .          .    299:        else if(c.tree){p=*c.tree;found=true;}
         .          .    300:        for(int attempt=0;attempt<(c.authored?49:1)&&!found;++attempt) {
         .          .    301:            const int ring=attempt?1+(attempt-1)/8:0,dir=attempt?(attempt-1)%8:0;
         .          .    302:            const double angle=dir*3.141592653589793/4;
         .          .    303:            const auto raw=c.point+glm::dvec2(std::cos(angle),std::sin(angle))*double(ring)*.75;
         .          .    304:            const glm::dvec2 point(std::round(raw.x),std::round(raw.y));
         .          .    305:            glm::dvec2 extent=c.kind==Kind::Bench?glm::dvec2(2.25,.75):c.kind==Kind::Crate?glm::dvec2(1,1.07):c.kind==Kind::Rocks?glm::dvec2(1.5,.9):glm::dvec2(.95);
         .          .    306:            if(c.yaw&1)std::swap(extent.x,extent.y);
         .        1ms    307:            const auto [lo,hi]=ground(terrain,point,extent);
         .          .    308:            const double tolerance=c.kind==Kind::Bench||c.kind==Kind::Crate?.02:c.kind==Kind::Flowers?.33:.65;
         .          .    309:            if(!std::isfinite(hi)||hi-lo>tolerance||lo<double(installedWorld().waterHeight)+2)continue;
         .          .    310:            if(!c.authored&&hi>180&&c.kind!=Kind::Rocks)continue;
         .          .    311:            const auto kind=c.kind;
         .          .    312:            p={c.id,kind,{point.x,lo+.002,point.y},c.yaw};
         .          .    313:            const auto box=bounds(low[size_t(p.kind)],high[size_t(p.kind)],p);p.minimum=box.first;p.maximum=box.second;
         .          .    314:            const auto closest=glm::clamp(creativeStart,glm::dvec2(p.minimum.x,p.minimum.z),glm::dvec2(p.maximum.x,p.maximum.z));
         .          .    315:            if(c.kind!=Kind::Flowers&&glm::length(closest-creativeStart)<13)continue;
         .          .    316:            bool collision=false;
         .          .    317:            for(const auto& q:out.props_)if(overlap(p.minimum,p.maximum,{{},{},q.minimum,q.maximum},.15)){collision=true;break;}
         .          .    318:            if(!collision)found=true;
         .          .    319:        }
         .          .    320:        if(!found)continue;
         .          .    321:        bool blocked=false;
         .          .    322:        const auto aliases=[&](uint64_t id){return id==creativeSceneryStructureId||id==creativeSceneryPartBase-p.id;};
         .          .    323:        for(const auto& structure:state.structures) {
         .          .    324:            blocked|=aliases(structure.id);
       3ms        3ms    325:            for(const auto& part:structure.parts)blocked|=aliases(part.id);
         .          .    326:        }
         .          .    327:        for(const auto& component:state.components)blocked|=aliases(component.id);
     1.05s      1.60s    328:        for(const auto& b:reserved)if(overlap(p.minimum,p.maximum,b,.8)){blocked=true;break;}
         .          .    329:        if(!existing) {
         .          .    330:            const glm::dvec3 feet(state.player.x,state.player.y,state.player.z);
         .          .    331:            const double radius=AdventurePlayer::creativeRadius*AdventurePlayer::creativeScale;
         .          .    332:            const Solid actor{{},{},feet-glm::dvec3(radius,0,radius),feet+glm::dvec3(radius,5.7,radius)};
         .          .    333:            // A canopy above the player/bike is safe. Only physical trunks
         .          .    334:            // exclude a newly streamed tree around those moving reservations.
         .          .    335:            if(p.forestVariant<forestGeometry.size()) {
         .          .    336:                for(const auto& [lo,hi]:boxes(p)) {
         .          .    337:                    const auto b=bounds(lo,hi,p);
         .          .    338:                    blocked|=overlap(b.first,b.second,actor,.2);
         .          .    339:                    for(const auto& clearanceSolid:clearance)blocked|=overlap(b.first,b.second,clearanceSolid,.2);
         .          .    340:                }
         .          .    341:            } else {
         .          .    342:                if(overlap(p.minimum,p.maximum,actor,.2))blocked=true;
         .          .    343:                for(const auto& b:clearance)if(overlap(p.minimum,p.maximum,b,.2)){blocked=true;break;}
         .          .    344:            }
         .          .    345:        }
         .          .    346:        if(blocked){out.suppressed_.insert(c.id);continue;}
         .        2ms    347:        const size_t count=boxes(p).size();
         .          .    348:        if(collisionCount+count>AdventureSpatialQueries::maximumSolids)continue;
         .        2ms    349:        collisionCount+=count;out.props_.push_back(p);
         .          .    350:    }
         .          .    351:    return out;
         .        4ms    352:}
         .          .    353:bool CreativeScenery::appendSolids(construction::WorldNamespace world,std::vector<Solid>& output) const {
         .          .    354:    std::vector<Solid> added;
         .          .    355:    for(const auto& p:props_) {
         .          .    356:        const auto structure=creativeSceneryStructureId,part=creativeSceneryPartBase-p.id;
         .          .    357:        // No reserved ID may alias a restored/user solid, even in legacy saves.
