--- scripts/benchmark_browser.mjs
+++ /tmp/voxys-frame-gap/same-browser-headroom12.mjs
@@ -12 +12 @@
-const repositoryRoot = path.resolve(scriptDirectory, "..");
+const repositoryRoot = "/home/modkin/workspace/schneiderlo/voxys";
@@ -2048,0 +2049,4 @@
+                for (const variant of iteration % 2 === 0
+                    ? ["baseline", "headroom12"] : ["headroom12", "baseline"]) {
+                fs.unlinkSync(options.artifactDirectory);
+                fs.symlinkSync(`/tmp/voxys-frame-gap/${variant}`, options.artifactDirectory);
@@ -2051,0 +2056 @@
+                run.variant = variant;
@@ -2054 +2059 @@
-                    `${run.passed ? "PASS" : "FAIL"} `
+                    `${variant} ${run.passed ? "PASS" : "FAIL"} `
@@ -2058,0 +2064 @@
+                }
