# Voxys — build, explore, salvage: implementation TODO

Created: 2026-09-07. Source review baseline: `7563f61fd536df7de5d209ff35d3cd2099ebcbb6`.

**Purpose:** implement a complete, polished construction-and-salvage game using Voxys. This file contains the product requirements, architecture contracts, asset workflow, implementation order, and acceptance criteria. An implementing agent needs this file and the repository, not the originating conversation or a separate blueprint.

**Status:** implementation goal active from 2026-09-07. Checkboxes and the execution ledger record verified progress. Existing engine features are starting points, not completed expedition-game tasks.

**Owner clarification — 2026-09-09:** this remains a LEGO-style building game. The owner expects the playable world to retain the prototype's stepped brick terrain and studs, with player-built machines as the central activity. The smooth terrain in `salvage-cove` is a temporary integration regression, not an approved art direction. Restore the brick environment and compatible physical interaction before expanding salvage scenarios; then prioritize PLAY-02's minimum player builder. Preserve D20: no repeated screenshot/review loops. Historical checkpoints do not approve this regression.

**Owner clarification — 2026-09-10: actual brick construction comes next.** The owner asked when they will be able to construct LEGO and said the recent work did not show enough progress. The current Cove workshop assembles large authored boat modules; it is not yet a brick-by-brick builder. Do not present cutting/recovery or modular pontoon placement as completion of that central experience. Finish recording the verified cutter checkpoint, then prioritize the following playable builder before more recovery matrices, another job, or cosmetic diagnostics. Preserve all later scope and gate requirements.

- [ ] **LEGO-02 — Build a machine with individual bricks in the playable Cove (next milestone).** **Needs:** existing canonical construction/refit, workshop, part assets and verified Launch/save path. **Deliver:** a visible palette of recognizable studded 1×2, 2×2 and 2×4 bricks; pointer picking and a placement ghost on the actual build; click to snap/place; rotate; select/remove; undo; save and launch the resulting machine. Use the same owned build, catalogue costs, fine lattice, welded connections, collision/mass and physical save path as other parts. Preserve existing boat modules and terrain. This must be usable through the game UI, not a new disconnected diagnostics page or state injection.
  - [ ] **Admit actual brick content.** Reuse the existing procedural/headless Blender cooking pipeline for explicit studded meshes, sensible LODs, stable content/socket IDs, collision/occupancy, mass and costs. No Blender MCP or generated textures are prerequisites. Match one-metre stud pitch, .02 m lattice, .96 m engaged brick spacing and .18 m visible studs; sockets and underside clearance must permit actual stacking without an unexplained gap or invalid overlap. Do not substitute plain smooth boxes for brick visuals.
  - [ ] **Make the build budget useful.** Target at least 64 editable bricks in addition to installed scenery. Audit the Cove's current 32-placement arrays, mappings, masks, fixture admission, renderer instances, workshop history, payload limits, save/recovery design limits and shape budgets before changing limits. Keep physical root/water capacities explicit and refuse a whole over-budget operation without loss. Do not blindly replace every 32: scene placements, catalogue definitions and independent rigid roots are different budgets.
  - [ ] **Connect direct building controls.** Build-surface ray picking must agree with the workshop's displayed transforms. Show a clearly valid/invalid ghost, stable orientation and the real price. Route Keep/Launch through the existing session/refit path; preserve focus/pointer capture and C's outside-workshop cutter meaning. Keep controller support in PLAY-02's remaining scope unless implemented and verified here.
  - [ ] **Deliver and show a playable construction sequence.** On native and browser, place a multi-layer design with at least eight bricks of different sizes, rotate/place a brick on another brick, refuse overlap, remove/undo a placement, launch and board/sail the modified machine, then save/reload with exact parts/design/costs. Also prove the supported capacity and clean refusal beyond it. Use actual controls and read-only state; no screenshots. Open the newly verified build for the owner rather than leaving them on an older preview. This passes only the bounded keyboard/mouse brick-builder milestone; full PLAY-02, art/performance, controller and game gates retain their criteria.

- [x] **LEGO-01 — Restore brick terrain in the playable cove.** The original stepped brick surface and round studs are restored in `salvage-cove`. Player height queries and authored hull/cargo contacts share the plate/stud recipe; exterior contacts preserve pontoon gaps and exposed risers. The actual generator now starts above the studs instead of embedded in them. Eight native GPU contact cases, twenty-three cove/registry cases, two final real-hull grounding/player terrace-and-jump cases, both application builds and a thirteen-stage actual browser control journey pass. The cable stays intact through towing; Reset/resize/Leave work. [Implementation, exact results, source digests, reproduction and limits](docs/validation/salvage/LEGO-01/terrain-r01/README.md). No screenshots. The underlying heightfield remains; individual terrain-brick excavation is a separate feature. This scoped correction does not complete SIM-03, LOOK-01 or a full gate. Next prioritize PLAY-02's minimum workshop.

## How an agent must use this file

- Read this entire file, root `AGENTS.md`, and root `README.md` before implementation. Recheck source when it differs from the baseline below. Repository paths in this file are relative to the repository root.
- Communicate in short, clear paragraphs. The project owner is highly dyslexic. Keep game UI text short and readable too.
- Work on an unchecked task whose prerequisites are complete. Claim its ID and file ownership in the execution ledger at the end before editing. Coordinate changes to shared schemas, build files, `Application`, and shaders. Preparatory dependent work may continue when prerequisite implementations and their scoped checks are complete but an independent reviewer is unavailable. Record that status explicitly; this does not pass the prerequisite, waive review, authorize live activation, or allow any dependent task/gate to be checked before its prerequisites pass.
- Independent asset, gameplay, rendering, and tooling work may run in parallel once their shared contracts are fixed. Give workers one bounded deliverable each. Do not let multiple workers overwrite one `.blend`, schema, or generated file.
- Preserve unrelated user changes. Existing LEGO, smooth terrain, RIDGEBREAK, and WRECKWATER routes remain regression targets. Add the new game through `?experience=salvage` and `--config salvage.cfg`; those are **new interfaces to implement**.
- The working defaults below resolve unspecified design choices. They are planning assumptions, not claims of separately approved commercial commitments. Use them unless later project instructions change them. Record any change in the decision ledger and update affected tasks before implementing it.
- Do not silently shrink the launch scope to a prototype, a two-client demo, or a collection of engine tests. Temporary milestone restrictions are named explicitly.
- Every checkbox requires its deliverable **and** its acceptance evidence. Use `docs/validation/salvage/<TASK-ID>/` for reports/captures, or record an equivalent repository evidence location in the ledger. Report actual executed commands, source revision, settings, expected/actual results, skips, failures, and remaining limitations.
- Mark `[x]` only after the task's tests and review pass. Use the ledger to mark in-progress/blocked work; leave the checkbox unchecked. Missing GPU hardware, artists, human testers, deployment access, or funding is not a passing result. Continue independent work when a gate is blocked.
- **User-required checkpoint rule:** after each gate G00–G14 passes, update its checkbox and evidence, run required repository checks, and create a Git commit for that gate's completed work. Record the commit in the ledger. Do not count a failed commit/check as a completed checkpoint, include unrelated changes, or bypass repository hooks. Gate commits are authorized; publication and purchases still follow the permissions below.
- Distinguish implemented, integrated, measured, and human-approved results. Concept art is not a gameplay screenshot; a synthetic tester is not a human playtest; software-driver correctness is not hardware performance.
- Update both Bazel and CMake integration for new shared code. Keep authoritative content identity, native/WASM packaging, and shader-source synchronization correct. Do not disable assertions or reduce the scene secretly to pass a gate.
- Tests should protect meaningful behavior: transactions, physics invariants, import correctness, real control journeys, save recovery, and performance. Avoid tests that only repeat implementation details.
- This file defines work and completion criteria. Actual external publishing, spending, installation, and account operations follow the permissions of the executing task. Do not invent credentials or assume a service is configured.

## Product contract — the game to build

**Player promise:** build a machine you care about, take it somewhere difficult, and bring something useful home.

**Loop:** choose a recovery job → modify a machine → travel to a wreck → detach/lift/tow/secure cargo → manage the loaded return trip → bank the haul → restore the harbor/unlock capabilities → revise the machine → take another job.

The memorable outcome is: **“My design mattered. I understood the problem. My adjustment worked.”** A well-designed craft may succeed on the first attempt. Do not script a capsize to force the intended story.

**Visual progress requirement, clarified 2026-09-07:** the owner found the first
cove preview substantially worse than the original prototype. BOOT-05's plain
boxes are a temporary lifecycle fixture, not an accepted art direction or a
visual-quality milestone. Preserve the original routes' appearance. Establish a
convincing small in-engine scene through LOOK-01 during the asset phase, before
expanding the game around unfinished visuals. Do not postpone the first serious
visual review until Phase 8. Engineering work with completed prerequisites can
continue in parallel; final art, rendering, animation and human gates still apply.

### Working launch scope

- Premium game for **Windows and Linux native**, with keyboard/mouse and controller parity. Keep a **desktop WebGPU playable demo and parity target**. Full browser campaign support is a gated expansion, not assumed. Console, macOS, and mobile releases are outside this plan's mandatory launch scope.
- Demo profile: the final-quality cove, the generator and awkward-crate jobs, the robot/boarding/recovery systems, and the 14 definitions in the initial catalog below including cutting and crane modules. Native builds can launch this same isolated demo profile and host 1–4 players with native/browser guests. Browser hosting is not required. Compare the selected profile's authoritative content identity; unrelated installed campaign assets do not cause a mismatch. Browser guests cannot join full-campaign sessions. Demo saves/progression are separate from campaign saves; only design blueprints can cross through validation, with no physical-resource transfer.
- Complete solo campaign, separate creative mode, and **1–4 player host-authoritative co-op**. Two-player co-op is an intermediate gate. Local solo play works offline and requires no remote game server.
- One original expressive utility robot as the player avatar. Third-person exploration and driving; orbit/pan/zoom workshop camera. **Walking on moving boats, boarding, falling overboard, swimming to safety, and rescue are required for launch.** The first proof may use helm/dock transitions.
- Four authored coastal regions. Target **50–70 functional part definitions** and **12–18 hours for a representative first campaign playthrough**. Paint colors, cosmetic duplicates, and simple size variants do not inflate the functional-part count. Human playthroughs establish duration, not mission arithmetic.
- Boats first. Powered cranes, structural cutting/repair, and amphibious haulers are required later families. Aircraft, submarines, general electrical programming, automated factories, combat, PvP, survival hunger/thirst, and fully excavatable terrain are excluded unless the product contract is revised.
- Environmental challenge comes from access, current/waves, weight, stability, terrain, structural loads, and damage. No enemy AI is required by this plan.
- Original construction-toy identity. Existing `lego` names identify legacy code; final content uses original characters, labels, and branding. No licensed character or logo is assumed.
- Landscape remains a heightfield with its shared brick-like surface. Interactive wrecks are explicit editable assemblies. Player machines are parts grouped into rigid clusters and joints. Arbitrary caves/overhang excavation requires separate volumetric work outside launch scope.

### Economy, recovery, and ownership

- Start with two resource classes: general salvage material and special machinery that unlocks a capability. Useful sidegrades survive later progression; a broad barge trades access/speed for load stability.
- Major build edits happen at workshops/service docks with a stable craft. Field actions initially adjust controls, release/reposition cargo, cut, patch, or replace explicitly permitted modules. Free welding during uncontrolled motion is excluded.
- A saved blueprint is a design. Duplicating a blueprint is free; spawning a physical copy consumes inventory unless it is the designated free starter recovery or creative mode.
- Starter restoration and rescue prevent an unrecoverable grind loop. Protect blueprints. Important lost cargo remains recoverable or receives a documented mission reset. Bound abandoned objects by region and significance; never silently delete unique progression cargo.
- Give the host world one durable starter entitlement. Its replenishable loan parts retain provenance and yield no bankable material or sale value. Replacement atomically retires or recovers earlier loan instances; paid additions stay owned and are recovered/stored through the rescue policy. Repeated valid rescue/dismantle requests cannot manufacture inventory. Damage/cutting never removes loan provenance.
- Solo pause stops simulation time. In co-op, a menu does not pause the shared world. Major editing requires a docked craft under an explicit edit lease; other players can continue elsewhere.
- Co-op world/progression/inventory belongs to the host save. Guests use session-scoped participation; cross-world inventory transfer is excluded. Personal blueprint export is allowed as design data only. Guest rewards update the host world once.
- Host departure ends the session after the last valid durable checkpoint or an explicit final save. No seamless host migration is promised. Disconnected continuous controls decay to a safe neutral state; committed one-shot operations remain exactly once.
- One region is active per session. Crews may take separate jobs inside it; travel to another region is a whole-party harbor transition. Require all crew and transferred craft/cargo in a secured staging state, release edit leases, settle pending transactions, and checkpoint before travel. Pin each attached/towed dependency group; no rope crosses a region boundary. Commit the destination baseline and manifest atomically; failed loading leaves the prior durable region recoverable. Persist valuable objects left behind under their recovery rules, without running off-screen regional physics.

### First two-job proof

**Job 1: stranded generator.** Start with a half-built skiff. Fit pontoons, engine, and winch. Recover a generator from a nearby wreck and return it to power the harbor lift. A high/off-center tow may overwhelm a narrow craft; a broad hull, low central tow, counterweight, cradle, or safer approach offers alternatives.

At the wreck, change cable length, approach, and cargo position. Return quickly to the workshop for pontoon/winch relocation. Test at least two viable strategies, including a first-attempt success.

**Job 2: awkward crate.** Reuse the same cove and mechanics with a heavier or differently mounted crate. The player modifies the craft and earns general material. This must be playable before testing voluntary repeat interest.

First proof: fixed starter chassis with editable module sockets, placeholder art, local thrust, fixed winch mast, real cargo, persistence, banking, rescue, and the second job. Fixed sockets simplify topology; they **do not** remove correct mass, center of mass, inertia, buoyancy, and force placement.

Final-quality slice: one polished harbor/cove/wreck; a 15–20 minute first session; about 12 initial part definitions; two starter designs; general connected construction; one controlled breakage/cutting case; powered crane; character/cameras; saved builds/cargo; final materials, water composition, sound, UI, and accessibility.

Initial catalog: beam, plate, pontoon, engine, propeller, helm, winch, tow eye, cargo cradle, brace, ballast, repair module. Add cutting and crane modules as their mechanics arrive. Each definition has explicit costs, mass properties, sockets, collision, and function.

### Art direction, including when the concept image is unavailable

Tactile slightly scuffed plastic, clearly assembled parts, readable connection points, restrained detail, broad color masses, warm cream/ochre craft, coral safety details, teal translucent water, and cool slate/warm stone coastal terraces. Soft coherent environmental light; strong landmarks visible from water level. Water behaves and looks like water, not literal plastic blocks.

Use silhouette and icons alongside color to distinguish flotation, structure, power, tools, and cargo. Preserve socket readability. The robot has a distinctive mechanical body and expressive face, not a branded minifigure silhouette.

A reference storyboard exists at `docs/concepts/build-explore-salvage.png`: workshop assembly → a listing crane boat recovering a generator → successful dusk homecoming. It is optional reference art, not a dependency or a shipped asset. The text above defines the direction without it.

## Current code facts — do not mistake these for the finished game

| Area | Existing foundation | Boundary/gap |
|---|---|---|
| Application | C++20 shared native/WASM lifecycle, local GameSession and playable cove | `src/app/application.cpp` still coordinates prototypes; expedition ownership, connected paid construction/live launch and initial sailing/towing exist. Durable saves, full jobs/progression and production shell remain open. |
| Main browser route | `web/index.html` defaults to `lego-world` | README contains older default-route prose; inspect source |
| Terrain | 8192² R16 heightfield, conservative max-height mips, hierarchical traversal, analytic columns/studs | Solid heightfield, not individually removable terrain bricks |
| LEGO geometry/layout | Plate pitch .32; stud radius .30; stud height .18; 32² chunk grouping | Ownership changes seams/color, not solid geometry |
| Layout cache | Fixed 1024² R32Uint toroidal texture, 4 MiB; max four chunk uploads/frame | Material ownership streaming, not terrain/content streaming |
| Playground | 48 bricks, 8 balls, fixed pad, three shapes, target challenge, 10 Hz bounded snapshots | No clutch bonds, vehicle modules, inventory, saves, or construction networking |
| Current stacking | Body height .96 + stud .18 = 1.14 spacing | Production connections need socket insertion and .32 lattice, not inherited loose stacking |
| GPU physics | Broad/narrow phase, solver, islands/sleep, CCD, queries/events, resident render poses; owned authored compounds/full inertia and confirmed cove rebuilds | General joints, broad machine profiles, CPU-backend parity and production stress remain open; see SIM checkpoints. |
| Attachments | Powered tension-only distance rope with force/break limits | Does not supply hinge, suspension, piston, or general rigid latch |
| CPU backends | Box3D oracle and Jolt migration adapter | Not drop-in equivalents for LEGO terrain, authored compounds, events, transactions, lifecycle |
| Structural graph | Canonical connected builds, mass/shape/flotation/function compiler, bounded paid refits and compensating history | General articulation, structural-load/fracture mechanics and larger live-render budgets remain open. |
| Water | Two 256² FFT cascades plus long swells; GPU surface sampling | CPU approximation retains strongest 24 modes; not exact CPU/GPU parity |
| Composition | Terrain/water output precedes dynamic primitive/mesh drawing | Submerged dynamic objects lack correct refraction; water depth can reject them |
| Mesh content | GLB/glTF → VMESH, PBR texture slots, hierarchy, stored animation/skin data | Default mesh limits 256 instances/512 draws; shader does not yet execute a complete skeletal animation path |
| WRECKWATER | Native TCP graphical client, small authoritative world, replay/content contracts | Native bootstrap rejects WASM; authority scope is 3 main bodies + 8 fragments; not generic machine co-op |
| Determinism | Separate fixed-point fixtures | Production float GPU backend reports no cross-platform float determinism or lockstep |
| Performance | Stored visible hardware captures and software startup gates | Historical cached/offscreen FPS does not certify gameplay performance |

The existing WRECKWATER design is 12-player competitive salvage. This plan adds a separate expedition game; do not import its PvP/12-player requirements or erase its regression coverage.

## Architecture contracts — implement these consistently

### A. Units, identity, and time

- New assets use **metres, kilograms, seconds, radians**, right-handed canonical content coordinates with **+Y up, +X right, −Z authored forward**. Existing camera/projection code enables `GLM_FORCE_LEFT_HANDED`; keep canonical data independent and use explicit tested rendering/import bridges rather than changing legacy macros globally. Preserve existing terrain scale: one horizontal stud pitch = 1 engine unit = 1 metre for this game. Plate pitch .32 m; full brick body .96 m. These are stylized game dimensions, not real toy manufacturing dimensions.
- Author Blender meshes with applied unit scale, +Z up and −Y forward. Export/cook through one explicit basis conversion; test axes rather than applying a second guessed rotation. Quaternions in serialized game data are named `x,y,z,w`, normalized, with a canonical sign for hashing. Adapt GLM's constructor ordering explicitly.
- Canonical build placement uses an **isotropic .02 m integer lattice** on all three axes. One stud is 50 ticks, one plate 16, one brick body 48 and stud insertion 9. UI snapping exposes convenient stud/plate increments, while socket alignment may require finer offsets. The 24 proper axis-aligned rotations are exact signed-permutation matrices on this lattice. An anisotropic 1 m/.32 m translation grid is insufficient: a sideways plate moves its .32 m offset into X/Z. Keep occupancy sparse or analytic; do not allocate a dense .02 m voxel grid. The starter chassis can restrict permitted mounts. Engaged full-brick stacking advances .96 m; loose collision stacking is separate. Articulated root/joint motion remains continuous floating-point simulation, distinct from exact welded construction coordinates.
- Use durable asset/build/part/socket/connection/cargo/job IDs independent of GPU slots. A suitable initial runtime identity is a host-world namespace plus monotonically increasing 64-bit object counter; encode counters losslessly as strings/binary across JS. Reject exhaustion and duplicates.
- Keep `simulationTick`, `topologyRevision`, `authorityEpoch`, `requestSequence`, and generational `BodyHandle` separate. Commands name their expected revision and request identity. Serialization order must not depend on hash-map iteration or insertion order.
- Authoritative gameplay runs at fixed **60 Hz**. If physics substeps are used, their count/time are explicit. Render interpolation and browser RAF do not advance authoritative time independently. Every catch-up tick samples the correct water time.

### B. Canonical records and derived records

| Record | Required data |
|---|---|
| `PartDefinition` | Stable ID/version, permitted rotations/footprint, visual mesh/material/LODs, collision proxy, dry mass/local COM/inertia, buoyancy volumes, typed socket frames/occupancy, strength, module parameters, cost, salvage yield |
| `PartInstance` | Durable ID, definition ID/version, .02 m integer local placement/proper rotation, health, paint, configuration, owning build, paid/loan origin and starter-entitlement identity |
| `Connection` | Durable ID, endpoint part/socket IDs, weld/hinge/rope/etc. type, limits/strength/damage/enabled state |
| `BuildModel` | Durable build ID, revision, parts/connections/module settings, ownership/edit lease |
| `CargoRecord` | Durable ID, definition, mass/flotation, attachment/latch state, location/owner, mission role, recovery rule, value, banking receipt |
| `GameSession` | Accepted commands/receipts, fixed tick/epoch, inventory, jobs, discoveries, world changes, builds and cargo |
| `CompiledAssembly` | Derived root shapes, COM/inertia, joint anchors, buoyancy regions, part→root and contact→part mappings, content/topology hash; reconstructible, not canonical inventory |

Proposed new boundaries: `src/game/construction/{part_catalog,build_model,build_commands,assembly_compiler}.*`; `src/game/expedition/{game_session,expedition_state,salvage_inventory,save_store}.*`; `src/render/{assembly_path,scene_composition}.*`. These paths are proposals, not existing modules. Introduce them incrementally; keep GPU/render handles outside canonical game/save data.

### C. Commands and transactions

- UI previews query canonical occupancy/sockets immediately. Do not use 10 Hz debug readback as workshop authority. UI submits intent; only `GameSession` changes accepted topology, inventory, ownership, and missions.
- Receipts distinguish rejected, pending preparation, and committed. A repeated request within the retained receipt window returns the same outcome and cannot spend or bank twice. Persist contiguous processed-sequence high-water marks per admitted participant/session, retain pending requests, and reject expired older requests as stale/already processed after detailed receipts are compacted. Gaps enter a bounded reorder/resend policy; they cannot silently advance the mark. Retired session tokens never become valid again. Recheck expected revision at commit after asynchronous cooking.
- Persist banked-cargo/job-generation state or compact durable tombstones so evicting receipts does not make a reward executable again. Never reuse durable object IDs. Bound histories using checkpointed closed ranges/generations and explicitly retained active exceptions; do not accumulate one forever-growing vector of historical receipts or tombstones.
- Reserve the worst permitted resources before commit: parts, bodies, child shapes, joints, contacts/events as applicable, render mappings, journal/staging space. Keep the old revision valid while preparing; discard obsolete/canceled work safely.
- Commit graph, shapes, mass/inertia, body/joint endpoints, collision filters, render mapping, ownership, and journal under one revision. Never backdate a mutation into GPU ticks already submitted. Define a bounded completed-evidence frontier and pending queue.
- Old shape, body, draw and mapping resources remain valid until the last referencing GPU submission completes. Use generation/content-tagged resources and bounded fence-based retirement from the first live rebuild. A committed replacement cannot immediately recycle memory still read by rendering, queries, or simulation; later world streaming reuses this mechanism.
- Undo/redo are compensating transactions with inventory accounting. Co-op undo cannot erase another participant's later work.
- Initial cargo states: world-loose → towed (independent body + rope) → latched (rigidly incorporated into a compiled cluster) → released/world-loose or banked. Other valid implementations require an explicit contract revision. Never count cargo mass both in the hull and an independent body. Attach/release preserves pose, correct momentum, collision filtering, and ownership.
- Latching requires compatible cargo/cradle sockets and authoritative position/orientation/relative-motion checks. Start capture tuning at ≤.10 m socket separation, ≤5° angular misalignment, ≤.5 m/s relative socket speed and ≤30°/s relative angular speed; data may refine these after measured tests. Do not teleport distant cargo. Compute the merged body's `P=Σmᵢvᵢ`, `v=P/M`, and angular momentum about merged COM `L=Σ[Iᵢ_world ωᵢ+(cᵢ−C)×mᵢvᵢ]`, then `ω=I_merged_world⁻¹L`. This is controlled inelastic capture: dissipated kinetic energy is expected, spontaneous energy creation is not. Small alignment corrections must respect the capture envelope and documented momentum/energy policy. Release inherits the cluster velocity field at each resulting COM.
- Durable banking writes its accepted journal transaction before displaying a durable success receipt. A later checkpoint failure must not lose an acknowledged reward. Quota/full-disk failures produce a recoverable unsaved state and no false durable acknowledgment.

### D. Mechanics and conservation

- Welded connected parts form one rigid cluster. Create separate bodies for actual relative motion, cargo, loose objects, and significant disconnected fragments. Render parts from `rootPose × partLocalPose`.
- For mass `mᵢ`, part COM `cᵢ`, local inertia `Iᵢ`, and orientation `Rᵢ`: `M=Σmᵢ`, `C=Σ(mᵢcᵢ)/M`, `rᵢ=cᵢ−C`, `I=Σ[RᵢIᵢRᵢᵀ + mᵢ((rᵢ·rᵢ)Identity−rᵢrᵢᵀ)]`. Validate finite positive mass and physically valid inertia. Use full tensors, or a documented stable principal-axis decomposition with sign/order/degeneracy handling.
- Compile simplified exterior collision proxies and child acceleration. Do not use every decorative stud as a vehicle contact. Logical socket bonds supply connectivity; internal mating surfaces are removed from assembled collision.
- Apply buoyancy at submerged-volume centers: magnitude `ρgV`; torque `r×F`. Drag uses velocity relative to local water, including angular velocity at the sample. Thrust acts at the propeller point. Do not double-count overlapping volume.
- `WaterField` identifies tick, spectrum/seed, frame, height, normal, and velocity. Compute forces from the state for that tick without a CPU-readback dependency cycle. Define CPU approximation errors explicitly; render interpolation is presentation only.
- Welded internal bonds have no joint reaction impulse available. Structural stress uses a bounded documented load-distribution approximation; do not pretend to have full finite-element stress.
- Broken components inherit the parent's **post-solve** velocity field: `v_fragment=v_parent+ω_parent×r`, `ω_fragment=ω_parent`. Do not apply the breaking impact twice. Reserve significant-fragment capacity before breaking; preserve unique cargo/functional parts under pressure.
- One authoritative body has one solver owner. If a CPU machine path wins a later benchmark, adapt it explicitly and use compact pose uploads. Cosmetic GPU debris does not push authoritative CPU craft without a separately implemented coupling.

### E. Saves and co-op consistency

- Save schema/content versions, durable IDs, topology, root poses/velocities, joint and motor state, rope lengths, cargo/latch ownership, damage/flooding, module resources, inventory, jobs/discoveries, water/simulation time/seeds, and journal sequence.
- Capture one certified completed simulation tick. Do not combine current topology with old poses or incomplete events. Initial save guarantee is **semantic resume** with controlled settling, not bit-exact continuation. Exact continuation needs additional compatible solver/contact history and separate certification.
- Native: crash-safe checkpoint replacement plus bounded journal and backup. Browser: transactional IndexedDB plus export/import and quota handling. Never persist raw GPU buffers or transient handles as canonical saves.
- Float GPU replay needs authoritative checkpoints/outcomes, not input-only cross-device lockstep. Reuse fixed-point fixture algorithms selectively; they do not certify the production float world.
- Reliable ordered channels carry topology/economy/session events; bounded sequenced snapshots carry motion. Every snapshot names authority epoch, simulation tick, and topology revision. Unknown topology enters a bounded wait queue and requests a baseline. Show a revision only when compatible mappings and poses are ready.
- Replication interest retains all relevant rope endpoints, cargo, and connected machinery. Start testing with 20 Hz root snapshots over 60 Hz simulation; tune from measurements. Cross-channel ordering is not assumed.

### F. Rendering and resource ownership

Target order: fixed simulation/water → visibility and shadow inputs → terrain and dynamic opaque HDR/depth → water/refraction → ordered underwater/transparency/effects → temporal reconstruction → one exposure/tone/output transform → UI.

Keep sampled opaque HDR/depth immutable while water writes separate attachments; avoid sampled-attachment feedback. Keep static terrain visibility/material caches, but invalidate or composite moving shadows independently. Add dry-hull water exclusion and flooding-aware interior water.

Use root/local-part instance mappings, root-first culling, part-cluster LOD, mesh/material bins, indexed indirect draws, and topology/material delta uploads. The GPU path reads resident root poses; a deliberate CPU path has bounded root uploads.

Temporal work needs previous/current camera/root poses, motion vectors, depth/ID/topology rejection, water/foam reactive handling, and sector-rebase handling. Separate unjittered cache identity from jitter. Either implement valid cached visibility reconstruction or budget retracing; do not silently invalidate the terrain cache every frame.

Feature-gate subgroup optimizations. Do not assume a fixed SIMD width or mapping to local invocation indices. Profile bandwidth, registers/spills, dispatches, barriers, and full-frame time. High-degree constraints affect scheduling; long chains/loops and mass ratios stress convergence. Occupancy alone is not a performance goal.

## Capacity, performance, and human gates

These numbers are **initial design budgets requiring implementation and measurement**, not existing proven capacities. Unless noted, counts are total loaded-world counts, not per player.

| Resource | Initial budget |
|---|---:|
| Logical parts per player craft | 256 |
| Significant rigid roots, including links/cargo/fragments | 64 |
| Active mechanical joints, including ropes | 128 |
| Detailed visible part instances | 8,192 |
| Significant salvage items | 32 |
| Resident edited structures | 8 |
| Cosmetic debris | 512 pooled, lifetime/distance limited |

Define independent caps for candidate pairs, compound child pairs, contacts/manifolds, commands, events, pending mutations, topology history, snapshots, readbacks, uploads, and saves. Root count alone does not bound these. Use checked arithmetic for allocation sizes. On capacity pressure, reject/defer a request explicitly or use a predefined meaningful coarse representation; never silently lose contacts, cargo, or accepted edits.

Start with the known Radeon 890M as a reproducible low-end engineering reference. Before release, declare exact AMD/Intel/NVIDIA machines, CPU/RAM, drivers, power modes, display rates, resolution, and presets. Native Windows/Linux and the supported desktop-browser demo need real visible runs.

| Full GPU-frame planning allocation at 1080p | Ceiling |
|---|---:|
| Machine/contact simulation | 3.0 ms |
| Terrain/static work | 2.5 ms |
| Dynamic geometry/shadows | 2.5 ms |
| Ocean simulation/composition | 2.0 ms |
| Effects/temporal/output | 2.0 ms |
| Contingency | 1.5 ms |
| **Total** | **13.5 ms** |

Target CPU gameplay/encoding below 4 ms p95 on the named reference. CPU/GPU overlap must be measured, not assumed. Multiple fixed ticks required in one displayed frame must be included.

Product targets: fixed 60 Hz simulation; p95 presented cadence within 16.67 ms under a declared measurement tolerance; p99 below 25 ms; no topology/streaming hitch above 50 ms in the scripted slice route. These are proposed gates. Change thresholds only through a recorded contract revision with reasons, not after a failing run to hide it. Record frame misses, queue age, and input latency separately.

Stored 2026-09-07 evidence on Radeon 890M, Chrome 152, Linux/Wayland, 1920×1080: walking GPU p50/p95 6.69/7.54 ms and RAF cadence 17.70/19.30 ms; 48 bricks + 8 balls GPU 4.36/4.60 ms and cadence 18.70/23.30 ms. These are short earlier captures, not results of this implementation. RAF cadence is not direct photon latency. Historical ~945 FPS cached/offscreen renderer throughput is not the product gate.

Stored live WASM startup allocation is approximately 404.2 MiB of a fixed 512 MiB heap. GPU memory is separate. One 8192² R16 image is 128 MiB; its full same-format mip chain is approximately 170.7 MiB. The 4 MiB material atlas does not mean the world is memory-cheap. Measure transient peaks, staging, and resources pending GPU retirement.

Initial human gate: 8–12 actual participants, including construction-game newcomers. Targets: ≥80% launch within three minutes without spoken help; ≥70% finish a haul unaided; ≥60% deliberately improve a design and explain its effect; ≥50% voluntarily start the second job; zero critical save/progression failures. Record denominators, observations, blockers, and accessibility issues. This diagnostic sample does not establish commercial retention.

## Execution map

| Milestone | Required gates | Meaning |
|---|---|---|
| Baseline and contracts | G00, G01 | Reproducible starting point and compatible interfaces |
| Asset pipeline and early visual proof | G02, including LOOK-01 | Authored models and gameplay metadata work in engine; a small real scene demonstrates the intended look |
| First two-job playable proof | G03, G04, G05, G06 | Correct craft physics, durable state, usable water composition, actual repeated loop |
| Mechanical foundation | G07 | General assemblies, articulation, fracture, complete checkpoints |
| Final-quality slice | G08 | Finished visual/audio/input/character experience in one cove |
| Production tools and streaming | G09 | Designers can add content; memory and loading are bounded |
| Co-op proof then launch count | G10, G11 | Two real clients, then four real clients |
| Full game content | G12 | Four regions, functional part library, campaign and creative mode |
| Release candidate | G13 | Hardware, reliability, accessibility, and human gates |
| Release | G14 | Versioned packages, operational support, release evidence |

Each task below states **Needs**, **Deliver**, and **Pass**. Evidence destination is the task-ID directory defined above. Gates depend on all listed work; a gate is not satisfied by a screenshot alone.

## Phase 0 — baseline, scope, and collaboration

- [x] **BOOT-01 — Establish the actual checkout baseline.**  
  **Needs:** none. **Deliver:** source revision, dirty-file inventory, applicable instructions, existing routes, current build configuration, and source differences from this file's baseline. **Pass:** a second agent can reproduce the environment without overwriting existing work; stale documentation is identified.

- [x] **BOOT-02 — Make build and test commands reproducible.**  
  **Needs:** BOOT-01. **Deliver:** working Nix/toolchain setup, native/WASM builds, focused baseline tests, and exact commands/tool versions in the report. **Pass:** failures/skips are recorded honestly; both build systems include shared modules; existing scene assets match their sources. Do not claim a full suite from a selected test subset.

- [x] **BOOT-03 — Record visible rendering and memory baselines.**  
  **Needs:** BOOT-02. **Deliver:** walking, shore, full playground, cold startup, steady-state timing, and separate CPU/WASM/JS/GPU live/peak measurements. **Pass:** device/driver/display/power settings and screenshots accompany completed GPU timing; cached throughput is labeled separately.

- [x] **BOOT-04 — Publish the working product and release matrix.**  
  **Needs:** BOOT-01. **Deliver:** confirm this file's launch defaults, demo/campaign compatibility and whole-party regional travel in the decision ledger; assign feature/asset/QA owners and specify Windows/Linux/browser validation machines. **Pass:** every mandatory launch family, session profile and deferred platform has an explicit status. Routine implementation can proceed on the written defaults; missing funding/platform hardware is recorded, not guessed.

- [x] **BOOT-05 — Add a separate expedition launch path.**  
  **Needs:** BOOT-02, BOOT-04. **Deliver:** `salvage.cfg`, browser `experience=salvage`, a placeholder authored cove, clean game-state lifecycle, and packaging/build entries. **Pass:** all old routes still launch; the new mode initializes/resets/exits without leftover physics objects or UI state.

- [x] **G00 — Baseline ready.**  
  **Needs:** BOOT-01, BOOT-02, BOOT-03, BOOT-04, BOOT-05. **Pass:** an independent agent can launch old/new modes, find known failures, and begin a task without the conversation.

## Phase 1 — game data and command authority

- [x] **DATA-01 — Encode units, frames, IDs, and canonical serialization.**
  **Needs:** BOOT-04. **Deliver:** Contract A types/converters, isotropic integer placements/proper rotations, stable ID allocation, sorted canonical encoding, lossless JS exchange, and explicit schema version. **Pass:** all 24 rotation compositions preserve lattice/socket placement; sideways/upside-down connections, axis/quaternion fixtures, negative coordinates, 64-bit IDs, exhaustion rejection and insertion-order independence pass.

- [x] **DATA-02 — Implement the minimum immutable part catalog.**  
  **Needs:** DATA-01. **Deliver:** Contract B definitions for the starter catalog, module parameters, typed socket frames, authoring-validation errors, and content lookup. **Pass:** malformed mass, missing assets, incompatible sockets, invalid rotations, and duplicate definitions fail clearly; valid catalog loads without a renderer.

- [x] **DATA-03 — Implement canonical builds and occupancy.**
  **Needs:** DATA-02. **Deliver:** persistent parts/connections, fine-lattice placement with coarse UI increments, stable build revisions, sparse/analytic socket and solid occupancy, overlap rules, provenance and blueprint duplication. **Pass:** edit/encode/decode round trips preserve data; sideways/asymmetric connections fit exactly; full-brick engaged spacing is .96, not loose-stack 1.14; duplication creates no free physical inventory and dense tiny-cell allocation is avoided.

- [x] **DATA-04 — Introduce local GameSession with a fake physics adapter.**
  **Needs:** DATA-03, BOOT-05. **Deliver:** sole authority for commands, inventory, cargo, mission state and receipts, independent of rendering. **Pass:** duplicate/stale/invalid requests, canceled preparation, insufficient capacity, and failed edits leave canonical state unchanged; direct UI state mutation is removed from this mode.

- [ ] **DATA-05 — Implement transaction preparation, commit, and compensation.**  
  **Needs:** DATA-04. **Deliver:** pending/rejected/committed receipts, bounded receipt retention and durable processed-sequence markers, resource reservations, revision revalidation, cancel/discard, undo/redo and inventory accounting. **Pass:** delayed preparation cannot overwrite newer state; retries spanning receipt eviction remain stale/idempotent; gaps and retired tokens cannot bypass ordering; repeated/compensating operations create no value and the old revision remains usable until commit.

  - [x] **Bounded local compensation checkpoint:** CreateBuild/AddPart/MovePart/unconnected RemovePart support exact undo/redo with private retained identities, monotonic revisions, bounded history/escrow, staged eviction, reserved inverse debits and closure without another payout. All 54 focused cases pass in Bazel, CMake, strict undefined-behavior checks and actual JS-exception/Asyncify WASM, without skips. Nine publication boundaries make zero allocation attempts. [Implementation, evidence, preserved failures and handoff](docs/validation/salvage/DATA-05/stage-c1/README.md).
  - [x] **Coherent checkpoint capture and RAM transport:** the trusted storage interface captures accepted state, exact pending/canceled request intervals, receipts, ID horizons, history/escrow and journal coverage within a 2 MiB owned-record bound. Exact typed-prefix release frees only model transport capacity. All 62 shared cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM; seven actual allocation failures preserve authority in native and WASM. [Evidence, limits and loader/replay handoff](docs/validation/salvage/DATA-05/stage-d1/README.md). This capture component does not itself restore a live session.
  - [x] **Imported checkpoint validation and exact replay:** trusted expected world/content identity admits a private validated image; bounded contiguous journal deltas validate exact prior state, history, inventory, identities and coverage without resubmitting intents or activating a backend. All 80 shared cases pass in Bazel/CMake, strict undefined-behavior checks and actual JS-exception/Asyncify WASM. Actual allocation-failure injection preserves state through 96 admission and 172 replay failures in both native and WASM; both applications build. [Evidence, preserved failures, limitations and recovery-factory handoff](docs/validation/salvage/DATA-05/stage-e1/README.md). Shipping byte integrity remains separate from this logical validation/replay component.
  - [x] **Fresh local authority after recovery:** finalize the old pending interval in order, preserve completed decisions, retire old admission/history, skip the reserved-ID horizon, and construct a fresh token/epoch/admission/writer with an exact parent link. Old local edit leases are revoked and cannot be revived by import. All 89 shared cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM; 297 actual allocation failures preserve the recovery input in both native and WASM. Both applications build. [Evidence, storage/adapter boundaries and entitlement-retirement handoff](docs/validation/salvage/DATA-05/stage-f1/README.md). This is local GameSession recovery; shipping storage and scene physics restoration remain later SAVE/SIM/BOOT tasks.
  - [x] **Trusted starter-entitlement retirement:** reject active-loan/stale/full/exhausted preconditions without partial changes; selectively retire loan history/escrow, invalidate prepared candidates, and journal/replay exact retirement. Paid value and unrelated compensation survive. All 98 shared cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM; four retirement boundaries allocate nothing in each runtime, and 297 recovery-factory allocation failures preserve input at that checkpoint. Both applications build. [Evidence, replacement-fixture limits and review handoff](docs/validation/salvage/DATA-05/stage-g1/README.md). Shipping rescue/replenishment remains later SIM/PLAY work.
  - [ ] **Remaining transaction acceptance:** obtain [independent transaction review](docs/validation/salvage/DATA-05/REVIEW_HANDOFF.md) and resolve findings. Current receipts remain explicitly volatile. These checkpoints do not complete DATA-05/G01 or authorize a gate commit.

- [ ] **DATA-06 — Define bounded event and telemetry schemas.**  
  **Needs:** DATA-04. **Deliver:** stable typed events for commands, forces, attachment state, damage, cargo, rewards, UI/audio, and diagnostics; bounded queues and explicit overflow counters. **Pass:** consumers cannot mutate authority; slow consumers do not grow memory without bound or lose economic transactions.

  - [x] **Bounded observation transport and wire codec:** separate fixed-capacity domain/presentation/telemetry lanes, copy-only readers, explicit gaps, saturating counters, sequence-exhaustion refusal and token-free versioned payloads. All 15 focused cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM; four explicit byte fixtures and 22 lossless JavaScript counter reads pass. In each runtime, 2,000 synthetic publications, 1,002 reads and 1,000 codec round trips allocate nothing; hub creation failure is checked. [Evidence, exact memory sizes and integration handoff](docs/validation/salvage/DATA-06/stage-a1/README.md). Existing 98 authority/journal regressions and both application builds pass.
  - [x] **Live observation and coherent baselines:** GameSession emits actual ordered receipt/state/retirement/closure transitions with fresh public identities and private writer access. Bounded owner-boundary baselines recover after ring overrun and receipt eviction without spending again; in-place restart preserves reader lifetime and rejects old cursors. All 122 shared cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM. Each runtime passes nine transaction/four retirement allocation-free boundaries, 26 creation/seven baseline/294 recovery allocation-failure cuts. Both applications build; real browser walking/Reset/Leave/re-entry keeps inventory exact and proves Reset identity retention, fresh re-entry and one closure. [Evidence, hashes and handoff](docs/validation/salvage/DATA-06/stage-b1/README.md). The cove emits closure only; physical/economic/presentation producers retain their later gates.
  - [ ] **Remaining event acceptance:** obtain [independent observation review](docs/validation/salvage/DATA-06/REVIEW_HANDOFF.md), resolve findings and verify the final sources. No DATA-06/G01 acceptance or gate commit yet.

- [ ] **G01 — Canonical contracts ready.**  
  **Needs:** DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06. **Pass:** headless tests build/edit/undo/serialize a starter design without GPU or UI dependencies and preserve exact inventory semantics.

## Phase 2 — Blender and asset production

### Tool facts and rules for asset agents

Blender **5.2.1 LTS** was verified on the original host with a headless `bpy` session. It is installed through `/snap/bin/blender`; the Snap launcher needed normal host permissions outside the restricted workspace sandbox. That test establishes Blender/Python availability, not asset-export correctness. Recheck the executable/version on another host; do not bypass its security mechanisms.

The owner is interested in Blender MCP. Use a functioning Blender MCP when available and useful for scene inspection/interactive iteration. Evaluate its actual commands and document the result. Its absence must not block reproducible `bpy` generation. Preserve scripts, parameters, `.blend` sources, exports, and renders whichever control route is used.

Current agents can run Blender directly. Separate headless Codex workers are optional batch orchestration: `codex exec` is the non-interactive command; `-p` selects a profile. Each worker reads this file, receives an asset ID/specification and isolated output directory, and must render/validate its work. Do not launch unbounded workers or call file creation alone a finished asset.

Image generation may supply reference art, base-color artwork, decals, wear masks, and texture drafts. Use neutral illumination for base color; verify tiling, seams, UV placement, and the absence of painted lighting. Generate/bake coherent normal/roughness/metalness separately; plausible-looking image maps are not automatically physically correct.

The built-in image tool chooses its model internally; never falsely label its output “Image 2.” If **exact `gpt-image-2`** selection is required, use a configured supported Image API/official skill workflow and record the actual model; API access is separate from an installed Blender. Do not invent credentials or switch model silently. Store prompts, references, actual provider/model when exposed, and outputs.

Current export profile: triangulated glTF/GLB; UV0 only; base color, tangent-space normal, metallic/roughness, emissive; metallic in blue, roughness in green; color/emissive sRGB, data maps linear. VMESH embeds decoded RGBA8, not compressed KTX2. Separate AO has no current image slot; do not count on it being rendered. Bake Blender node graphs into supported maps/factors or explicitly implement equivalent runtime shading.

Current converter stores TRS animation with LINEAR/STEP and four joint influences; cubic splines, morph animation, and other features are not a ready runtime path. The mesh shader currently uses the instance transform, so stored skin/animation fields do not imply working animation. Some unsupported glTF features may be silently ignored: add validation before relying on them.

- [x] **ASSET-01 — Establish the authoring/control workflow.**  
  **Needs:** DATA-01, BOOT-02. **Deliver:** versioned Blender scripts/export preset, optional MCP assessment, per-asset output conventions, parameters/seeds, and smoke renders. **Pass:** a clean headless run regenerates one test asset; any MCP-assisted edits can be saved/reproduced; no existing scene is overwritten accidentally.

- [x] **ASSET-02 — Add gameplay metadata beside render assets.**
  **Needs:** DATA-02, ASSET-01. **Deliver:** versioned sidecar bound to the GLB content digest for stable sockets, collision proxies, buoyancy volumes, mass properties, LODs and tool anchors. **Pass:** metadata survives export/cook; object ordering/array indices are not durable IDs; stale sidecars are rejected.

- [x] **ASSET-03 — Enforce a supported glTF profile.**
  **Needs:** ASSET-02. **Deliver:** explicit validation for unsupported required extensions, sparse accessors, extra UVs/influences, morphs, vertex colors, texture transforms, primitive modes, nonfinite transforms and limits. Validate every vertex attribute's count against POSITION before conversion reads it. Correct the observed matrix rotation reversal in `decomposeMatrix` and test equivalent matrix/TRS nodes, including asymmetric rotations, before accepting matrix-authored assets. **Pass:** supported golden assets survive; malformed attribute counts and unsupported required appearance/function fail loudly instead of disappearing or causing out-of-bounds reads; matrix/TRS equivalence and canonical-frame fixtures pass. Only collision/flotation volumes that need closure must be watertight. The LOOK-01 preliminary investigation identifies these pre-existing importer gaps; they are not BOOT-05 regressions.

- [ ] **ASSET-04 — Validate one pontoon through the complete pipeline.**  
  **Needs:** ASSET-03. **Deliver:** parameterized `.blend`, GLB, sidecar, VMESH, LODs, material maps, thumbnail, turntable, socket/volume dimensions, and provenance. **Pass:** native and WASM show correct scale, axes, winding, material, socket fit and bounds in the same test scene; independent technical review validates the pipeline. Simple prototype visuals are sufficient here; final visual approval belongs to VIS-03/G08.

  - [x] **Inspection guides checkpoint:** native/browser clean view, exact metre/plate rulers, selected-LOD render bounds, canonical socket axes and clearance outlines. Shared GPU ownership, all-rotation/sector CPU geometry checks, real browser controls/Reset/drained Leave/re-entry/device loss and matched 1920×1080 captures pass. [Implementation, evidence and reproduction](docs/validation/salvage/ASSET-04/guides.md). This sub-checkpoint does not complete ASSET-04 or authorize a gate commit.
  - [x] **Crossbeam assembly checkpoint:** native/browser render the actual six-part/five-weld `BuildModel` fixture, with shared v1 prototype crossbeams and v2 pontoon stacking. Connected socket X-ray fixes guides previously hidden inside opaque parts. The 34 focused native cases pass in each build system, 66 shared CPU cases pass in WASM, and current browser controls/Reset/drained Leave/re-entry/device loss plus raw-route regression pass. Twelve matched clean/X-ray captures and exact source/package hashes are preserved in [implementation and evidence](docs/validation/salvage/ASSET-04/assembly.md). Prototype beam wells remain unmodeled; no independent review or gate pass is claimed.
  - [x] **Matched continuous-motion capture checkpoint:** native and current browser follow the same 30-second camera path at 1920×1080/FOV 60. Each pontoon follows 1→2→3→2→1 with six model draws and constant residency; original frames/traces and decoded videos are preserved. Native capture/refusal, ordinary PNG, current browser lifecycle/device loss and affected build-system tests pass. [Implementation, reproduction and scoped review](docs/validation/salvage/ASSET-04/native-motion.md). Independent moving-image/technical review and final visual approval are still open.
  - [x] **Authored hierarchy/winding checkpoint:** a real Blender stand retains two nontrivial parents and shared geometry, with an independent pre-export triangle/normal oracle. Three focused hierarchy cases pass in Bazel/CMake; the full shared CPU WASM suite now has 69 passes and zero skips. Ten paired native/browser views, actual mixed-asset LOD refusal, controls/resize/Reset/drained Leave/re-entry/device loss and original pontoon-route regression pass. [Implementation, reproduction, preserved failures and evidence](docs/validation/salvage/ASSET-04/hierarchy.md). Native Vulkan and windowed browser captures are accepted for this scoped check; the failed headless capture remains documented. No independent review or gate pass is claimed.
  - [x] **Rendered rotation checkpoint:** two installed galleries show all 24 proper orientations through actual two-pontoon welds. Eight admission cases pass in each native build system; 70 shared CPU WASM cases pass without skips. Thirty-four matched native/browser image pairs, all detail levels, guides, resize, Reset, drained Leave/re-entry and actual device loss pass. [Reproduction, review and evidence](docs/validation/salvage/ASSET-04/rotations.md). Actual camera-sector transitions remain separate; no independent review or gate pass is claimed.
  - [x] **Live camera-sector checkpoint:** inspection flight and browser camera placement now maintain canonical sectors. Actual native/browser 30-second paths cross X/Y/Z boundaries and return with the same assembly owner and completed GPU work; browser W/S crossing, socket views, Reset and cove regression pass. Fifty-four affected checks pass in each native build system. [Implementation, evidence and limits](docs/validation/salvage/ASSET-04/sectors.md). Distant coordinate captures do not approve art or replace independent moving-image review.
  - [x] **Visible hierarchy bounds checkpoint:** the installed hierarchy registry selects the authored stand for rulers/bounds. Every composed triangle corner is checked against its render bound; three hierarchy tests pass in each native build system and 70 shared CPU WASM cases pass. Four paired native/browser views and the real mixed-LOD/control/resize/Reset/drained Leave/re-entry journey pass. [Evidence and reproduction](docs/validation/salvage/ASSET-04/hierarchy-bounds.md). The renderer and models are unchanged; the previous registry is preserved.
  - [x] **Release candidate preparation:** `data/salvage/pontoon/release-candidates/v2-rc01/` contains editable source, maps, GLBs, sidecar, freshly cooked payloads, 512-pixel thumbnail, contact sheet, turntable and frozen regeneration recipes. Every included file is verified; runtime payloads match the inspected r04 bytes. [Preparation and remaining publication work](docs/validation/salvage/ASSET-04/release-candidate.md). This is staging, not publication or acceptance.
  - [x] **Exact release-candidate runtime admission:** all five installed registries select the staged v2-rc01 manifest. Twenty-five native cases and 70 CPU WASM cases pass without skips; seven matched native/browser views, actual browser lifecycle/device loss and all inspection routes pass. [Evidence, frozen inputs and limits](docs/validation/salvage/ASSET-04/release-runtime.md). This verifies candidate selection; independent review and publication remain open.
  - [ ] **Remaining complete-pipeline acceptance:** obtain independent moving-image/technical review using the [self-contained review handoff](docs/validation/salvage/ASSET-04/REVIEW_HANDOFF.md), then publish the final reproducible candidate. Preserve historical v1/candidate provenance and keep LOOK-01 open until the actual cove passes its own visual review.


- [ ] **ASSET-05 — Create the first functional asset kit.**  
  **Needs:** ASSET-04. **Deliver:** beam/plate, propulsion/tool module, helm, fixed winch, cradle, generator and follow-up crate using the same conventions. **Pass:** both starter designs assemble correctly in an asset/socket fixture; contact/attachment/buoyancy metadata validates. Runtime physics acceptance follows in G03; this task must not depend on later live compound support. Prototype art is allowed.

  - [x] **First authored kit and static assembly checkpoint:** nine original Blender parts with 27 LODs and strictly cooked metadata are staged at `data/salvage/functional-kit/r08/`. Narrow and broad starters each contain eleven authored placements and seventeen exact socket connections, reusing the unchanged pontoon candidate. Four kit cases pass in both native build systems; 74 shared CPU WASM cases pass without skips. Four matching native/browser view pairs at 1920×1080 pass root inspection; each skiff requests 12,977,224 GPU bytes within the existing ceiling. The corrected lower leg puts the propeller below the limited level-water estimate. [Implementation, reproduction, hashes and limits](docs/validation/salvage/ASSET-05/README.md). These are static inspection layouts; no live handling, cove approval, independent review or gate pass is claimed.
  - [x] **Runtime inspection checkpoint:** fixed a real Socket X-ray capacity failure by using the two existing bounded GPU buffers independently. Nineteen owner checks pass in both native build systems, six Bazel mesh checks pass, and nine invalid capture requests refuse safely. All three browser kit routes pass real detail/guide/resize/flight/Reset/drained Leave/re-entry controls; actual GPU-loss cleanup also passes. Six matched-light contact pairs and native/browser 30-second full-skiff captures are recorded; both pontoons traverse 1→2→3→2→1 with constant residency. Original assembly motion still passes in both runtimes. [Evidence, videos and precise review limits](docs/validation/salvage/ASSET-05/inspection.md). Root sampled the motion; independent continuous-video review remains open.
  - [x] **r09 geometry/proxy and forced-detail checkpoint:** corrected winch flange clearance and the helm's obstructed socket well; refined disjoint helm/winch/cradle proxies with recomputed COM/full inertia at unchanged dry mass. Actual exported helm checks reproduce 42 obstructed old samples and clear all 123 corrected samples. Five kit cases pass in both native builds and 75 CPU WASM cases pass without skips. Six paired native/browser runs retain the selected detail for every authored placement at all three LODs; root inspected their twelve first frames. All three browser control journeys, actual GPU loss, thirteen CLI refusals and original automatic-detail motion in both runtimes pass. [Correction, exact evidence, reproduction and limits](docs/validation/salvage/ASSET-05/r09-correction.md). Installed registries select r09; other seven model GLBs, all placements/connections and the pontoon remain unchanged. No parent or visual gate acceptance is claimed.
  - [x] **r09 close-contact checkpoint:** root inspected all 48 actual native/browser images: eight matched contact/underside cameras at each of three forced detail levels, including the helm seat and opposite winch face. Actual capture state verifies detail, placement counts and unchanged residency. All 60 config/CLI cases pass in both native builds; applications build, unsupported native hierarchy detail refuses without a PNG, original pontoon Auto capture passes, and current broad/hierarchy browser controls pass. [Evidence, reproduction and material findings](docs/validation/salvage/ASSET-05/r09-contacts/README.md). Socket-wall color changes, Far metal response and stretched grain remain ASSET-06 defects; no final-art acceptance is claimed.
  - [ ] **Remaining kit acceptance:** obtain independent technical review after ASSET-04 prerequisite acceptance and resolve findings; D18 permits preparation only. Keep the full ASSET-05 task open until prerequisite and review pass.

- [ ] **ASSET-06 — Establish material and texture standards.**  
  **Needs:** ASSET-04. **Deliver:** shared plastic/metal/rubber/glass response, limited palette, decal/trim usage, bake/image-generation recipe, texel-density and texture-memory limits. **Pass:** neutral/light/dark/wet previews remain coherent in engine; generated color textures have no unwanted fixed shadows; normals/roughness respond correctly to changing light.

  - [ ] **Material preparation / root under D18:** define the shared response and texture contract; correct the observed tessellation-dependent pontoon socket-wall colors, Far-LOD loss of metallic/roughness identity and stretched grain. Keep installed candidates frozen until replacement evidence exists. The strict salvage profile currently accepts opaque materials only: glass/transmission needs explicit admission/render support, and must not be silently claimed through the generic blend path. Existing MeshPath already generates full texture mip chains. Complete real changing-light and wet previews before checking this parent task; ASSET-04 independent review remains pending.
  - [x] **Opaque calibration and native checkpoint:** [material contract](tools/salvage_assets/MATERIALS.md), shared dry response and opt-in Blender profile are implemented. Two isolated candidates retain roughness/metallic maps at every LOD and correct the socket color domain. Actual exported GLBs reproduce nine old bad color samples and pass 45 corrected socket/three metal samples; triangle-position sets and physical metadata remain unchanged except the explicit pontoon version. Both strict cooks and six real native captures pass; root viewed all six PNGs. [Evidence and next work](docs/validation/salvage/ASSET-06/README.md). This is a clean calibration without grain/normal microdetail, not finished surface art. Browser packaging/parity, metric detail, rubber, wet/glass response, complete lighting checks and independent review remain; installed r09/v2-rc01 and the cove are unchanged.

  - [x] **Opaque calibration browser checkpoint:** the normal WASM package now includes `salvage-materials` and `salvage_material_fixture.cfg`, selecting the unchanged r01 calibration bundles. Six real browser views at 1920×1080/FOV 60 pass root inspection alongside the native views; all 14 control/resize/Reset/drained Leave/re-entry stages pass. Actual native Bazel and WASM CMake application builds pass. [Evidence, reproduction and frozen package hashes](docs/validation/salvage/ASSET-06/browser-r01/README.md). This closes r01 browser packaging only; metric detail, rubber, wet/glass response, changing-light/motion, independent review and parent/gate acceptance remain open.

  - [x] **Metric dry-material transport checkpoint:** both Blender recipes now support a bounded repeating detail profile at a fixed metre scale, plus a separate helm rubber material. Nine actual GLBs retain exact geometry and physical metadata, stay within 1.002 principal UV stretch and pass 4,277 height-derived normal samples plus palette/metal/socket checks. All three strict cooks and native Bazel/WASM CMake application builds pass. Root inspected all 18 native/browser opposing-light pairs at 1920×1080; the corrected 14-stage browser control journey passes. Actual fixture residency requests 5,564,672 bytes under the unchanged owner ceiling. [Implementation, retained failures, reproduction and evidence](docs/validation/salvage/ASSET-06/metric-r01/README.md). This establishes texture scale and dry material transport only; it is not finished tactile art or ASSET-06/LOOK-01 acceptance. Installed kit/cove remain unchanged.
  - [x] **Manufactured-normal checkpoint for winch/helm:** the opt-in `manufactured-v2` Blender recipe preserves flat panels/caps while smoothing rings, cylinder barrels and bevels. Six actual GLBs preserve exact geometry, winding, UVs, materials and physical metadata; measured panel/barrel/cap/grip normals stay within 0.013 degrees of the reference shapes. Both strict cooks and native Bazel/WASM CMake builds pass. Root inspected all 24 matched opposing-light/detail-level images; all 14 browser control/resize/Reset/drained Leave/re-entry stages pass. The existing surface-detail route selects these r04 candidates with the unchanged r02 pontoon and requests 5,485,472 GPU bytes. [Evidence, rejected first attempt, exact recipes and replay correction](docs/validation/salvage/ASSET-06/shading-r02/README.md). This checks these two parts only; it does not accept all surface art or pass a parent/gate.
  - [ ] **Environment-lighting preparation / root under D18:** the `detail-diagnosis-r01` native material ablation retains the strongest cream/steel bands with both normal and roughness maps disabled. Investigate the renderer before attributing those bands to texture charts. Own `environment_lighting.*`, its WGSL producer, focused GPU checks and shared build registration. Prepare bounded GGX specular convolution, directional diffuse irradiance and split-sum BRDF integration against analytic/independent numerical references. Native/browser execution and actual scene comparisons are required before claiming a visual fix. This is scoped material-lighting preparation; REND-01–04, prerequisite review and gate acceptance remain open.
  - [x] **Environment-convolution producer checkpoint:** implemented bounded RGBA16F specular/diffuse cubes and BRDF integration, independent of output exposure. Five shared checks pass in native Bazel/CMake and hardware browser WebGPU; all 14,274 sampled RGBA values match byte-for-byte. Analytic directional lighting and independent hemisphere quadrature expose/correct a retained first-attempt cube-face failure. Encoded resource release, abandoned encoding and re-bake checks pass. Both application build paths and the existing five-test/eighteen-readback browser mesh diagnostics pass. [Evidence, exact inputs, retained failures and integration handoff](docs/validation/salvage/ASSET-06/environment-r01/README.md). This producer is not yet bound by the application; actual game-view improvement, default bake cost, narrow-light convergence and full lighting/gate acceptance remain open.
  - [x] **Environment consumer and real-scene checkpoint / root under D18:** the surface-detail route now explicitly uses one filtered environment per admitted generation, shared across parts and charged to the unchanged owner ceiling. The bake is ordered before draws, acknowledged only on submission, retried after discard and retained through Reset/depth changes. All 89 focused native cases pass in both build systems; two actual consumer GPU cases produce five byte-identical native/browser readbacks. Root inspected 24 matched scene PNGs; fourteen control stages, drained Leave/re-entry, real device loss and the legacy mesh diagnostic pass. Actual reservation is 6,714,416 bytes including 1,228,944 lighting bytes. [Evidence and remaining work](docs/validation/salvage/ASSET-06/environment-consumer-r01/README.md). This is scoped integration, not parent, independent, owner or gate acceptance.
  - [ ] **Remaining surface refinement:** apply verified shading to the other parts and refine socket-cut surfaces; correct directional tile/chart transitions, inspect normal variance/mips and the 512 texels/m hand-contact target. Then complete matching bright/dark exposure cases, continuous motion, wet response and explicit glass support. Keep the owner-rejected cove visual gate open until water/opaque composition, contact shadows and actual authored-scene review pass. Do not treat the dry inspection checkpoint as permission to waive these requirements.

- [ ] **ASSET-07 — Add asset build, provenance, and review records.**  
  **Needs:** ASSET-03, ASSET-06. **Deliver:** deterministic asset manifest including source/cooker versions, content hashes, references/prompts, dependencies, ownership/license information, and review status. **Pass:** a clean build reproduces the declared inputs or records known nondeterministic steps; required placeholder/missing/unapproved assets are reported.

- [ ] **LOOK-01 — Prove the visual direction early in the real engine.**  
  **Needs:** BOOT-05, ASSET-05, ASSET-06. **Deliver:** one deliberately composed, walkable section of the cove with a recognizable modular skiff, dock and generator, using the authored asset kit. Establish tactile plastic/metal materials, readable bevels and connections, balanced lighting, useful foreground/mid-distance/horizon and a clear starting camera. A stationary craft is permitted at this milestone; label its simulation status. **Pass:** inspect actual native and browser screenshots plus a short moving-camera capture at declared matching resolution/settings. Compare with the original prototype and the textual art direction; reject large raw placeholder boxes as the main vessel, featureless flat surfaces, unreadable silhouettes, obvious intersections and a degraded terrain/water backdrop. Independent visual review must identify what improved and any remaining composition/rendering defects. Retain the owner's feedback and do not equate an agent's review with owner approval. An offline Blender render or generated concept cannot pass this task. Visible water/opaque ordering defects must be fixed or keep this task pending; REND-01–04 remain responsible for the complete dynamic composition contract. This is the next visual milestone, before broad content production; it does not replace final VIS-03/VIS-05/G08 approval.

  - [ ] **Authored cove composition preparation / root under D18 and D19:** replace the raw-box focal vessel in a separate, explicitly stationary cove candidate with the existing authored skiff, dock modules and generator. Own the cove registry/configuration, scoped scene placement/control integration and required packaging. Use already validated dry material/lighting components; incomplete wet/glass and full ASSET-06 acceptance remain separate prerequisites for final LOOK-01 acceptance. Inspect one representative actual scene after composition materially changes, then use identified defects to drive the next implementation. Walkability, water ordering and shadows must be implemented before LOOK-01 can pass.
    - [x] **Static assembled-scene checkpoint:** complete skiff, dock modules and generator now render in the separate `salvage-cove` candidate. Water-relative anchoring and actual shoreline samples replace elevated inspection placement. Native/browser startup and focused admission/configuration/capacity checks pass; root viewed one successful arrival image per runtime. [Evidence and specific defects](docs/validation/salvage/LOOK-01/cove-r01/README.md). This scoped checkpoint does not accept the parent task, final materials, visual quality or live physics.
    - [x] **Walkable dock and moored-boat interaction checkpoint / root under D20:** `salvage-cove` starts on authored collision with WASD walking, Space jumping, proximity-checked E boarding, helm use/exit, return to dock, surface swimming and R recovery. Schema 3 navigation identifies safe positions and boat collision membership; landing works even without the board button. Seven focused native player checks, nine existing registry cases, eight UI lifecycle cases, native/WASM builds and the actual browser nine-stage keyboard/button journey pass. [Implementation, exact inputs, retained failures and limits](docs/validation/salvage/LOOK-01/dock-play-r01/README.md). No new screenshots. Stationary first-person scope only; no sailing, robot, moving-deck/controller-parity/ACT/PLAY or parent/gate acceptance.

- [ ] **G02 — The asset pipeline and early visual direction are proven.**  
  **Needs:** ASSET-01, ASSET-02, ASSET-03, ASSET-04, ASSET-05, ASSET-06, ASSET-07, LOOK-01. **Pass:** a designer changes a pontoon parameter, rebuilds, and sees the correct native/browser result without editing C++ or WGSL; the real small cove scene passes LOOK-01 visual review.

## Phase 3 — correct starter-machine simulation

- [ ] **SIM-01 — Build an analytical assembly compiler.**  
  **Needs:** DATA-03, ASSET-02. **Deliver:** welded-component detection, exterior collision proxies, mass/COM/inertia, buoyancy regions, module/joint frames, stable contact→part and part→root mappings, and cache identity. **Pass:** independent asymmetric fixtures validate the parallel-axis formula; ballast relocation changes COM; equivalent insertion orders produce equivalent outputs; invalid/over-limit input is rejected.

  - [x] **Welded roots and dry-mass foundation:** owned intermediate `AssemblyMassPlan` validates current catalog/build data, groups enabled welds and preserves exact part/root frames while calculating dry mass, COM and full inertia. All 11 compiler + 23 build cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM. An independent rational fixture, all 24 rotations, moved ballast, large coordinate offsets and capacity boundaries pass; 21 actual allocation-failure cuts preserve input/existing output in each runtime. Both core libraries build. [Evidence and remaining compiler work](docs/validation/salvage/SIM-01/stage-a1/README.md). This is not yet a complete physical assembly or live machine.
  - [x] **Bounded exterior collision preparation:** an owned `AssemblyCollisionPlan` compiles exact disjoint box volumes, clips full/partial internal mating faces, retains exterior contact→part/proxy identity and builds a queryable flat child BVH. All 12 collision/union + 11 mass + 23 build cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM; 62 actual allocation-failure cuts preserve input/existing output and 2,000 queries allocate nothing in each runtime. Independent integer-cell/face oracles, cavities, all rotations, input-order equivalence and a 256-part welded stack pass. Both core libraries build. [Evidence, capacities and backend contract](docs/validation/salvage/SIM-01/stage-b1/README.md). SIM-02 must honor the exterior patches; raw cells must not be activated as unmasked box contacts.
  - [x] **Overlap-aware buoyancy preparation:** owned `AssemblyBuoyancyPlan` partitions displacement into disjoint cells retaining every solid/sealed contributor, exact region/root frames and durable source identity. All 13 coverage/buoyancy + 12 collision + 11 mass + 23 build cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM; 70 actual allocation-failure cuts preserve input/existing output and 8,000 reference reads allocate nothing in each runtime. Independent unit-cell/active-subset oracles, nested regions, cavities, all rotations and a 256-part build pass; both core libraries build. [Evidence, capacities and remaining mechanics](docs/validation/salvage/SIM-01/stage-c1/README.md). This prepares geometry; flooding, submersion and force producers retain their later gates.
  - [x] **Module and connection frames:** owned `AssemblyFunctionPlan` retains all typed module limits/settings, health/strength, exact root-local functional/socket frames and canonical link bindings. All 10 new + 59 prior shared cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM; 74 actual allocation-failure cuts preserve input/existing output, and 7,000 frame/9,000 socket reads allocate nothing per runtime. Dense matrix rotation/large-offset oracles, disabled welds, rope/latch provenance, 768 frames and 8,192 sockets/1,024 connections pass; excessive canonical mating work remains an explicit rejection. Both core libraries build. [Evidence, retained failure and future mechanics](docs/validation/salvage/SIM-01/stage-d1/README.md). This prepares frames; it does not activate joints or certify cargo capture.
  - [x] **Complete owned assembly and physical cache identity:** `CompiledAssembly` owns the matching mass/collision/buoyancy/functions and hashes actual used physical metadata, canonical topology/state and all twenty profile fields through an explicit versioned grammar. All 9 new + 69 prior shared cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM; the independent 670-byte golden digest matches both platforms. Each runtime preserves input/existing output/identity through 74 allocation failures and performs 7,000 frame/9,000 socket/1,000 identity reads without allocation. Both core libraries build. [Evidence, grammar and retained test-build correction](docs/validation/salvage/SIM-01/stage-e1/README.md). This is complete CPU preparation; backend activation and live mechanics remain later gates.
  - [ ] **Final compiler acceptance:** independent review, resolution of findings and affected validation before checking SIM-01. The [self-contained review handoff](docs/validation/salvage/SIM-01/REVIEW_HANDOFF.md) is a request, not a performed review. No gate pass or live solver activation is claimed.

- [ ] **SIM-02 — Extend physics shape and mass contracts.**  
  **Needs:** SIM-01. **Deliver:** authored compound/shape identity independent of material flags, local COM/full inertia support, and a documented body/build frame transform. Start with the bounded starter hull profile. **Pass:** pose integration and force-at-point torque agree with analytical tests; legacy primitive/studded-brick behavior remains valid; optional CPU capabilities fail explicitly if unsupported.

  - [x] **Full-tensor mass/frame preparation:** fixed-size `RigidMassFrame` produces proper principal axes centered at COM while retaining root/body pose, point, velocity and full angular-response mappings. All 8 numerical + 78 prior shared cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM. Independent rational impulse values, all rotations, 2,000 dense physical fixtures, degenerate/slender moments, invalid input and actual compiled offset ballast pass. Each runtime performs 5,000 preparations, 1,000 rejections, 10,000 transform round trips and 5,000 impulse checks with zero allocations; both core libraries build. [Evidence and retained first-build corrections](docs/validation/salvage/SIM-02/stage-a1/README.md). This is CPU preparation; it does not advertise or activate a backend capability.
  - [x] **Owned shape format and CPU lifetime ledger:** `AuthoredShape` packs validated exact exterior geometry with checked f32 COM, principal inertia and quaternion; `ShapeHandle` carries pool/index/generation separately from materials. `AuthoredShapePool` bounds resident plus retiring storage and waits for registered completion and retained readers before reuse. All 130 shape/mass/assembly/build/catalog/grid cases pass in Bazel/CMake, strict undefined-behavior checks and configured WASM. Each runtime preserves three preparation/one creation allocation failures and performs 1,000 lifetime cycles, 1,000 capacity refusals and 14,000 provenance reads with zero allocations. Both applications build; checked authority headers match across build systems. [Format, budgets, evidence and retained corrections](docs/validation/salvage/SIM-02/stage-b1/README.md). Completion serials at this checkpoint are a tested CPU model; actual GPU work is recorded in the following checkpoint and live capability remains open. Current 12-part catalog passes all 288 allowed rotations; later cutter/crane work still supplies the 14-part launch scope.
  - [x] **Actual GPU shape resource and mass-helper checkpoint:** a bounded single-buffer atlas uploads generation-checked geometry/mass, tracks real queue submissions plus error scopes, preserves encoded uses and retained readers through retirement, and invalidates descriptors before reuse. All eight shared GPU cases pass in native Bazel/CMake, strict undefined-behavior/float-cast checks and actual hardware Chrome WebGPU; seven paired readbacks match exactly on the reference GPU. All 78 existing GPU-physics/LEGO/cove cases pass; both full applications build. [Format, ownership, evidence, reproduction and retained failures](docs/validation/salvage/SIM-02/stage-c1/README.md). This is a standalone resource component and tested WGSL math, not live compound-body activation or visual acceptance.
  - [x] **Persistent body layout and prepared mass consumption:** physics, direct rendering/culling and all nine shape-consuming shaders share the 64-byte body record, with independent shape-reference fields. Actual force/integration/static-contact paths consume stored inertia; primitive spawn/destroy clear the reference and material edits preserve it. All 12 shared GPU cases pass in Bazel/CMake, strict undefined-behavior/float-cast checks and hardware Chrome; 12 paired readbacks match exactly. All 123 targeted legacy native cases pass; both applications build and native authority headers match. The complete browser cove lifecycle passes through explicit hardware X11 mode; the reproduced Chrome/Wayland mouse-lock crash on both current/prior packages remains open under QA-06. [Evidence, preserved failures, source hashes and remaining integration](docs/validation/salvage/SIM-02/stage-d1/README.md). Kernel fixtures do not activate authored bodies; this is not visual/performance or parent/gate acceptance.
  - [x] **Backend-owned resource interface:** `PhysicsWorld` exposes opt-in shape resources with fresh pool identities, stable borrows across world moves, zero-delta completion polling and explicit uninitialized/CPU refusal. Actual backend physics and an authored-shape read share a checked queue submission; restart rejects the old handle. All 14 shared GPU cases pass in Bazel/CMake, strict undefined-behavior/float-cast checks and hardware Chrome, with fourteen byte-identical paired readbacks. The native selection passes 131 cases; both applications build, actual server dependency checks pass and native authority headers match. [Evidence, cache/run distinctions, failures and ownership handoff](docs/validation/salvage/SIM-02/stage-e1/README.md). This is resource ownership, not authored-body activation, visual approval or parent/gate acceptance.
  - [x] **Checked root/body motion and point conversion:** an owned packed-mass frame separates authored-origin and COM motion, preserves sector-local precision, checks f32/sector overflow without clamping, and maps force/attachment points into principal axes once. Ten CPU and sixteen shared GPU cases pass in Bazel/CMake, strict undefined-behavior/float-cast checks and actual hardware Chrome; sixteen paired GPU readbacks match exactly. Real force and integration kernels confirm a rotated asymmetric tensor and sector-crossing COM/root reconstruction; both applications build. [Evidence, precise fixture scope and remaining consumers](docs/validation/salvage/SIM-02/stage-f1/README.md). This provides conversion/preparation code, not live body admission, visual acceptance or a parent/gate pass.
  - [ ] **Remaining shape/mass integration:** connect typed body admission and automatic live shape-use declarations to the actual application submission boundary, use the checked frame conversion at pose/force-point/COM consumers, retain assembly/revision/provenance bindings, handle device loss/recovery, explicitly reject unsupported authored-body capabilities, validate live GPU/legacy behavior and obtain review. SIM-01 review remains pending; SIM-02 acceptance waits for its prerequisite.

- [ ] **SIM-03 — Carry compounds through every physics consumer.**  
  **Needs:** SIM-02. **Deliver:** correct bounds, narrow phase, terrain contacts, CCD, ray/sweep queries, events, mass use, render/debug mappings, and generation-safe lifetime. **Pass:** the same asymmetric compound settles, collides, sweeps, and renders correctly; sockets/contacts map to the correct part after handle reuse.

  - [x] **Exterior query and bounded contact checkpoint / root under D20:** shipping GPU rays/overlaps/sphere/capsule sweeps use the compiled exterior and BVH; shallow sphere/capsule/box/compound contacts produce parent COM anchors and source-face identity. An actual contact-solver impact moves and turns the asymmetric body while preserving linear momentum and avoiding energy creation. Twelve new cases pass on native and hardware browser WebGPU; eight legacy query cases, four legacy narrow-phase cases, native/WASM application builds and shared-shader checks pass. The compute runner now runs headless and never captures screenshots. [Evidence, reproduction and explicit limits](docs/validation/salvage/SIM-03/contacts-r01/README.md). Typed live body admission remains disabled. Deep containment, opposing/capacity-limited patches, terrain contacts, CCD, render/root mapping and automatic submission/revision ownership remain required before sailing or parent/gate acceptance.

  - [x] **Hull–seabed and actual cove boat preparation / root under D20:** exposed hull faces now contact the actual heightfield triangles through the production static-contact solver, including interior seabed peaks, pontoon gaps and rotated COM/sector frames. Four new native/browser GPU cases and four backend regression cases pass; invalid/unsupported/over-budget evaluations report failure without partial geometry. Restore the cove's missing seventeen welds in schema 4. `src/game/expedition/cove_boat.*` compiles its eleven selected parts into one owned assembly with mass, exterior hull, non-overlapping flotation, functional frames and placement/source mappings. Actual preparation is 1,035 kg and 9.884032 m³ maximum displacement; dock/scenery are excluded. Eighteen native cove/registry cases, both application builds and one ordinary headless browser startup pass. [Evidence, exact scope, reproduction and next integration](docs/validation/salvage/SIM-03/terrain-r01/README.md). No screenshots. Runtime explicitly reports `boat.prepared=true`, `boat.active=false`: no live boat admission, buoyancy, sailing, visual/performance/review or gate pass is claimed. Next connect that assembly to typed body admission and queue ownership, required CCD/containment, root rendering, water/propulsion forces and the moving helm/player.

  - [x] **First physical sailing checkpoint / root under D20:** the actual eleven-part cove skiff now enters the GPU world through typed body admission and owned queue submission, retains its shape until completed use, and renders exact root-to-part transforms from the live GPU pose. Compiled disjoint displacement cells provide buoyancy; the authored propeller/helm frames provide thrust and steering torque. Spawn uses calculated dry draft; the cove uses triangle terrain and a locally deepened starter berth. The player boards, uses W/S throttle and A/D steering, follows the moving deck, leaves the helm and recovers with R; browser Leave drains the owned scene. Fifteen focused native physics/cove checks pass, including actual-hull calm-water stability. A real browser keyboard/button journey sails about 19 metres and turns, with zero screenshots. Native and browser application builds pass. [Implementation, reproducible checks and remaining work](docs/validation/salvage/SIM-03/sailing-r01/README.md). This is one intact craft with still-water drag and a render-produced wave texture; it does not complete water epochs, power networks, flooding, authored CCD, deep/multiple-patch contacts, boat–dock collision, cargo or campaign identity. SIM-02/03/04/05/07, ACT-03, prerequisites/review and gates remain open. Next add physical dock obstacles, then towing/cargo through the existing dependency and ownership contracts; do not resume screenshot loops.

  - [x] **Fixed cove scenery collision / root under D20:** the dock and generator now have a fixed authored exterior built from their thirteen actual non-boat collision proxies. Static admission zeros inverse mass/inertia, rejects movement/water drivers and clears fixed state on handle reuse. Real GPU impact stops/rebounds the skiff against the dock while an identical craft in clear water advances; the dock remains fixed and capacity checks pass. All sixteen focused native tests and the actual browser boarding/sailing/reset/resize/drained-Leave journey pass. Scene ownership retires both hull and scenery only after their dead-body observations. The first browser run exposed a snapshot backlog; one pending observation now prevents readback-ring exhaustion. Both application builds pass. [Evidence and continuation](docs/validation/salvage/SIM-03/dock-collision-r01/README.md). No screenshots, parent acceptance or gate commit. Next towing/salvage through the existing scheduling, water and campaign-identity contracts; authored CCD, full containment/patch coverage and review remain open.

  - [x] **Browser LEGO terrain support correction / root under D32:** the generator fell through the studded seabed in the current and byte-identical previous browser package. Removing the extra 17-point local array from the contact reservoir fixes the full shipping solver while preserving duplicate merging, deepest contact retention and spread selection. A new hardware-browser regression fails on the frozen old shader and passes six corrected cases, including 129 bodies, smooth terrain, penetration correction and reservoir coverage/deepest/duplicate invariants. All 88 native authored-GPU/cove tests pass. [Reproduction, retained failures and current integration status](docs/validation/salvage/MECH-05/cove-roots-r01/README.md). The actual browser generator now remains on the LEGO seabed; complete delivery/recovery and performance acceptance remain separate. No screenshots or gate pass.

- [ ] **SIM-04 — Establish fixed-tick scheduling and the GPU frontier.**  
  **Needs:** DATA-04, SIM-03. **Deliver:** explicit scheduled/submitted/completed ticks, bounded ticks in flight, complete pose/event evidence, timeout/error behavior, pause/catch-up policy, and future mutation scheduling. **Pass:** rendering at 30/60/144 Hz does not change accepted tick order; no mutation is applied to an already submitted tick; device stalls do not produce false completed state.

  - [x] **Owned GPU execution confirmation / root under D20:** `PhysicsWorld::tickFrontier()` separates scheduled, encoded, actually submitted and confirmed ticks for a fresh owned incarnation. Confirmation joins a bounded GPU counter/status readback with the existing ordered queue/error-scope completion; discarded encoding never becomes confirmed. Queued ticks and proof batches are bounded before encoding. Fix the 60 Hz accumulator's 144 FPS boundary error. Eighteen focused native cases pass, including exact 120-tick GPU runs at 30/60/144 Hz, abandoned-work/restart checks and the prior physical hull/dock controls. The real browser sailing/reset/Leave journey verifies ordered confirmed ticks within the configured two-tick bound; both application builds pass. [Contract, evidence and remaining owner integration](docs/validation/salvage/SIM-04/tick-proof-r01/README.md). No screenshots, parent or gate pass. Full pose/event evidence joining, event-capacity preflight, explicit stall/device-loss tests, authoritative pause, future GameSession publication and WaterField epochs remain required before cargo rewards. Never use the CPU test tick driver to activate topology after its GPU tick has already completed.

  - [x] **Confirmed cargo pose/event consumption and capacity preflight / root under D20:** pose/event copies carry their actual owned submission serial and publish only after queue validation and confirmed execution; public records include the incarnation. Preflight all pending per-tick event copies and requested pose slots before encoding. The cove drains contiguous event batches, reports real rope breaks and holds Leave until events retire. The real one-slot GPU winch-break test proves backpressure before encoding, confirmed handles/force and safe resumption. [Shared towing evidence](docs/validation/salvage/SIM-08/towing-r01/README.md). Forty-one focused native and fourteen actual browser gameplay stages pass. Per-operation GameSession joins, deliberate stall/device-loss/overflow fault cases, authoritative pause and shared water epochs remain open; parent/gates are not accepted.

- [ ] **SIM-05 — Implement the authoritative WaterField.**  
  **Needs:** SIM-04. **Deliver:** tick/spectrum/seed/frame contract, GPU height/normal/velocity sampling, named CPU approximation with error tolerance, and presentation interpolation. **Pass:** multiple catch-up ticks use their own sea state; forces do not require reading back tick N to populate tick N; solo pause freezes gameplay waves consistently.

  - [x] **Cove shared wave clock / root under D33:** bind the FFT and analytical swells to the exact submitted physics incarnation/tick/phase. Both cove runtimes admit one tick per submission; stalled/discarded frames spend no wave time, and neutral pause/restore/service ticks preserve it. Relative integer ticks avoid large saved-epoch drift. Stale/foreign/unsupported water frames refuse before physics encoding. Six clock cases, a real GPU phase/large-epoch case, 69 affected Bazel cases, 193 JS-EH/Asyncify WASM cases, and both native/WASM builds pass. Native haul/restart passes 25 stages before the final arithmetic refinement; the final frozen browser package passes 29 stages through paid construction, towing, harbor hoist, durable payment, tick 656→657 reload with unchanged water phase, duplicate-payment refusal and sailing. [Exact sources, scope, retained failed input strategy and corrected journey](docs/validation/salvage/SIM-05/cove-clock-r01/README.md). Full per-tick catch-up fields, GPU water velocity/relative drag, spectrum/seed/frame contract, CPU error bounds, player-clock joining, interpolation and performance remain open. No SIM-05/G03 acceptance or gate commit.

- [ ] **SIM-06 — Bind prepared build transactions to physics.**  
  **Needs:** DATA-05, SIM-03, SIM-04. **Deliver:** atomic prepare/commit/discard across shapes, bodies, filters, mappings, ownership and revisions, plus generation-safe fence retirement for old resources. **Pass:** concurrent edit/cancel/rebuild with delayed GPU completion leaves referenced revisions/resources valid; stale preparations, early reuse and capacity failures cannot partially spend inventory, alter collision or expose retired mappings.

- [ ] **SIM-07 — Make module placement affect the boat.**  
  **Needs:** SIM-05, SIM-06. **Deliver:** bounded submersion-based buoyancy, water-relative drag, engine/propeller forces, steering/thrust controls and configured module limits. **Pass:** offset loads change roll/trim; a wider flotation arrangement resists the same load better; propeller relocation changes torque; overlapping volumes do not multiply flotation.

- [ ] **SIM-08 — Complete winch and cargo-restraint behavior.**  
  **Needs:** SIM-07, DATA-06. **Deliver:** selecting valid tow eyes, attach/reel/pay-out/release, tension/overload events, capture-envelope validation, cradle latch via the prepared compound transaction, and combined-momentum merge/unlatch/filter restoration. **Pass:** analytical asymmetric two-body merge/release fixtures conserve mass and momentum without creating kinetic energy; distant/fast/misaligned cargo is rejected; slack-to-taut and high-load tests remain stable and cargo has one mass representation and durable identity through repeated cycles.

  - [x] **First playable towing / root under D20:** the cove now contains an independent 420 kg salvage generator on the seabed. F hooks/releases its authored tow eye; Q/Z reel/pay out through the existing GPU distance solver. The winch uses actual COM/principal anchors, force/length/speed limits and confirmed overload events. A depth-tested cable connects the observed endpoints. Boat and cargo receive independent water forces, and R/Leave recover/retire both plus fixed scenery. Forty-one focused native cases, nine UI checks, both application builds and a real fourteen-stage browser input journey pass. Reeling closes the gap from 6.47 to 4.68 m and sailing physically moves the load; release/reset/Leave work. [Implementation, exact inputs, evidence and next work](docs/validation/salvage/SIM-08/towing-r01/README.md). No screenshots, parent acceptance or gate commit. Private scene identity only; generalized selection, latching, campaign preparation/receipts, jobs/rewards, fixed-tick water, broader stability and independent review remain open. Next make the haul count toward an actual objective through the future-tick ownership path; do not return to screenshot loops.

- [ ] **SIM-09 — Bound mixed-size collision and awake work.**  
  **Needs:** SIM-03, SIM-08. **Deliver:** measured large-object path, compound-child acceleration, contact budgets, active/sleeping compaction and wake propagation. The existing oversized-body all-resident scan is the baseline to compare. **Pass:** dense craft/cargo/debris tests report bounded candidates and zero silent contact loss; sleeping systems wake under engine, rope, contact, or edits.

- [ ] **G03 — Starter mechanics are physically meaningful.**  
  **Needs:** SIM-01, SIM-02, SIM-03, SIM-04, SIM-05, SIM-06, SIM-07, SIM-08, SIM-09. **Pass:** two differently configured hulls show understandable empty/loaded handling; rope/latch operations preserve identity and mass; physics evidence covers native and WASM GPU paths.

## Phase 4 — minimal durable progress, before the first playtest

- [ ] **SAVE-01 — Specify versioned save and journal formats.**  
  **Needs:** DATA-05. **Deliver:** canonical blueprint/inventory/job/cargo schema, checksums, processed-sequence markers, bounded reward tombstones/generations, compatibility policy, migration entry points and journal compaction. **Pass:** canonical encode/decode, unknown-version rejection, duplicate-ID detection and corrupt/truncated records are tested without the renderer; replaying an old request after compaction and reload cannot spend or reward again.
  - [x] **Canonical blueprint bytes checkpoint / root under D21/D24:** Implemented SVBP v1 with exact little-endian ordinal/content/placement/paint/settings/connection records, bounded size/counts, SHA-256, canonical order and output-preserving rejection. No physical IDs, condition, loan entitlements, inventory or authority are representable. Native round-trip/corruption/version/ordinal/settings/content tests and the actual WASM round-trip/corrupt-import journey pass. [Exact format and compatibility policy](docs/salvage-blueprint-format.md), [192 native checks, shipping builds and actual exported file](docs/validation/salvage/PLAY-02/designs-r01/README.md). This completes the blueprint component only; full inventory/job/cargo/checkpoint/journal byte formats were completed by the subsequent component below; durable migration/compaction and anti-replay after actual storage reload remain SAVE-01. Parent/gate stay unchecked.

  - [x] **Portable expedition checkpoint/journal bytes / root under D21/D24:** Added SVSC/SVJB v1 for the full validated logical recovery model: owned parts/welds/provenance, inventory/cargo/jobs, processed requests, allocator reservations, history, loan retirement and parent/child lineage. Fixed little-endian fields, bounded counts/retained memory, SHA-256, closed tags, canonical re-encoding and independently expected identity checks reject malformed data before publication. Existing capture/replay fixtures now cross the codecs. Both application builds, 199 native regressions, 140 strict JS-EH/Asyncify WASM checks and 140 UBSan checks pass; three frozen complete-envelope hashes agree across runtimes. Encoded replay at every tested banking-journal split and a RAM-model compaction/reload preserve ownership and prevent a second reward. [Format, migration policy and storage ordering](docs/salvage-session-save-format.md); [results, preserved failures, source hashes and self-contained continuation](docs/validation/salvage/SAVE-01/session-r01/README.md). No screenshots. This is a data component: real native/browser world storage, durable compaction/acknowledgment, version migration fixtures and certified physical recovery remain open. Parent/gate stay unchecked.

- [ ] **SAVE-02 — Implement native durability and backup recovery.**  
  **Needs:** SAVE-01, BOOT-02. **Deliver:** platform save directories, staged writes, correct flush/replace sequence, journal replay, last-good backup, and errors for full disk/permissions. **Pass:** interruption before/after each edit/bank/undo boundary cannot duplicate inventory or lose an acknowledged durable banking receipt.


  - [x] **Linux native generation storage component / root under D21/D24:** Added the shared SVSG v1 bounded integrity envelope and `NativeSaveStore`: absolute private directories, process-exclusive writer lock, revision-checked staging, file flush/close, atomic replacement, directory flush and two independent copies before acknowledgment. Loading reports differing copies, preserves the latest intact generation and refuses conflicting/unsupported/both-damaged data. Uncertain publication poisons the writer until close/reopen/reconciliation; full-disk/quota/access failures are explicit. Nine native cases include 16 actual SIGKILL cuts and 16 injected I/O failures per suite, plus real SVSC session close/reopen preserving a purchased part and rejecting its old request. Final ext4 runs pass 211 native regressions and 12 independent CMake cases; 143 strict WASM shared checks and both application builds pass. Fixed a missing CMake headless-authority source already required by its manifest and built that target successfully. [Protocol and API](docs/salvage-native-save-store.md); [results, preserved failures, filesystem/source identity and exact continuation](docs/validation/salvage/SAVE-02/native-r01/README.md). No screenshots. This is the Linux file component only: Windows, browser world generations, coherent session/physical archive, actual game save worker/UI, durable journal acknowledgment/compaction and physical recovery remain open. SIGKILL is not power-loss certification; storage faults were injected. Parent/gate stay unchecked.

- [ ] **SAVE-03 — Implement browser persistence and export/import.**  
  **Needs:** SAVE-01, BOOT-05. **Deliver:** IndexedDB transactions, bounded write queues, quota/error reporting, save backup and user export/import. **Pass:** reload/interruption/quota/corruption cases preserve the last good state; exported IDs/counters are lossless; imports validate before replacing any current save.
  - [x] **Browser blueprint persistence checkpoint / root under D21/D24:** A bounded 32-design IndexedDB library now supports name/save/update/load/copy/rename, one backup per design, export/import and explicit removal. Strict durability is requested; success follows transaction completion. Current/backup writes share a transaction, stale revisions reject, and imports validate before storage. Eight isolated real IndexedDB checks pass, including an injected quota failure after backup write, rollback of both rows, two-tab conflict, capacity and payload-corruption backup recovery. Nine actual gameplay stages prove reload, backup, real file export/import, corrupt-file refusal, then a correctly priced physical launch and sailing. [Evidence, exact package, limits and next steps](docs/validation/salvage/PLAY-02/designs-r01/README.md). No screenshots. This stores design intent only, within the same browser profile/origin; native storage, full world saves, actual disk/quota/power-loss certification and durable banking remain open.


  - [x] **Browser world-generation storage component / root under D21/D24:** Added `web/expedition_store.js` with the native-compatible SVSG v1 envelope, independently expected world identity, mandatory host archive validator, exclusive Web Locks, one pending operation, eight-world capacity and strict IndexedDB transactions covering current/mirror copies. It rechecks exact records and key presence after async preparation, acknowledges only transaction completion, preserves quota errors through rollback, handles copy corruption, rejects conflicting/unsupported/malformed data, and fences closing/revoked/upgraded owners. Present undefined values cannot become new worlds. Nineteen real-browser cases and five actual tab/reload/process-kill stages pass on an ext4 profile; both kills occur before caller acknowledgment and recover the correct complete old/new generation. Three native format cases, 143 strict WASM cases, four JS codec cases and fourteen existing UI cases pass; all runtimes verify the same maximum-u64 fixture. [API and continuation contract](docs/salvage-browser-save-store.md); [source hashes, retained failures, exact crash locations and full evidence](docs/validation/salvage/SAVE-03/browser-r01/README.md). No screenshots or game mutation. This backend is loaded but not connected to the game save UI; coherent session/physical archive, world selection, durable journal acknowledgment, physical restoration, world export/import/migration and native Windows work remain open. The test validator is synthetic and must not ship. Parent/gate stay unchecked.

- [ ] **SAVE-04 — Capture certified starter-expedition state.**  
  **Needs:** SAVE-02, SAVE-03, SIM-08, SIM-04. **Deliver:** checkpoint one completed tick including build/cargo motion, latch/rope targets, module state, inventory/jobs, water time and journal sequence. **Pass:** save/load during towing, after latching, after release and after banking restores the same meaningful state without double mass or rewards; semantic-resume settling is controlled.
  - [x] **Coherent body/rope packet and player-resume preparation / root under D21:** Added bounded combined GPU readback for exact solver rope length, motor, anchors/endpoints, limits and break state alongside body motion, retaining owned tick/submission proof. The cove checks rope identities and control completion and shows actual cable length. Added validated player state restoration with boat-local feet, helm/walking/jump/swim modes and vertical velocity; success clears pending controls, refusal preserves the current player. Both applications build, 51 native GPU/cove cases and 14 UI cases pass. One real browser keyboard/button journey passes 13 recorded stages plus drained Leave: cable length 6.34→4.34→5.38 m through reel/payout, intact towing and LEGO terrain preserved. [Exact APIs, bounds, source/package hashes, results and self-contained continuation](docs/validation/salvage/SAVE-04/capture-r01/README.md). No screenshots. This does not join full session/player/water state at one certified tick or implement archive bytes, pause/drain, save/load controls, storage acknowledgment or physical recovery. Parent/gate stay unchecked; no gate commit.

  - [x] **Playable Pause/Resume and completed capture boundary / root under D21:** Added owned scheduler pause with bounded optional final ticks, retained queued work and cleared clock debt. The cove blocks new input/transactions, drains prior work, executes one neutral control tick, then waits for matching boat/cargo/rope, ordered events and GameSession tick before reporting Paused. P/browser controls resume safely; Leave re-enables retirement scheduling. Player controls and the cove water clock freeze; paused rendering shows the final pose. Both builds, 52 native GPU/cove checks and 16 UI cases pass. One real browser journey passes 17 records plus Leave directly from pause: all required observations agree at tick 472, attempted movement/reset/build/bank input changes nothing for 800 ms, Resume retains neutral winch control, and the final dock pause closes at tick 899. [Contract, source/package identity, retained logs and archive continuation](docs/validation/salvage/SAVE-04/pause-r01/README.md). No screenshots. This completes the local pause/join component only; full physical archive, storage acknowledgment, save/load UI and restoration, fixed-tick WaterField, explicit fault coverage, latching and remaining SAVE acceptance remain open. Parent/gate unchecked; no gate commit.

  - [x] **Portable cove archive, actual capture and nonactivating load preparation / root under D21:** Added bounded SVCE v1 joining the existing SVSC checkpoint and optional finalized parent to authored-root boat/cargo motion, player state, water clock/settings and rope state. It checks independently selected content/world roles, exact accepted tick, cargo/job/ownership consistency, canonical bytes and lineage. The live cove captures only at its joined paused boundary; its reader rebuilds the saved owned boat from installed content and validates a temporary restored player without replacing the live world. Both builds, 263 affected native cases and all 150 strict WASM cases pass. A real towing archive captures tick 467 and its 4.1724725 m cable; a second real purchase/Launch journey captures two paid pontoons (13 parts / 1,275 kg, IDs 35/36, 0 material). That actual payload passes mirrored browser publication and database-owner reopen with the real application validator: generation 1, identical bytes, no repair. [Format and API](docs/salvage-cove-save-format.md); [exact evidence, retained failures and complete next-work contract](docs/validation/salvage/SAVE-04/archive-r01/README.md). No screenshots. This is capture/preparation plus isolated storage integration, not live restoration or page reload. **Next implement world-slot selection, an owning load candidate, fresh GPU initial tick, physical activation, paired lineage publication and actual Save/Load controls.** Fixed-tick WaterField, latching, durable banking/receipts, Windows and remaining fault/human acceptance stay open. Parent tasks/gates remain unchecked; no gate commit.

  - [x] **Playable browser expedition Save/reload component / root under D21:** Added an owning restore candidate, fresh GPU initial logical tick, pre-main selected-world startup, actual saved boat/cargo/player/water/rope activation, and paired recovered-session/retired-parent publication before releasing input. Pause → Save expedition commits a manual browser checkpoint and updates its return URL; reloading starts paused after exactly one neutral settling tick. Commands carry the current recovered epoch. Both applications build, 311 affected native checks, 8 save coordinator/UI cases and 16 preview UI cases pass. One real hardware Chrome journey passes 17 records, three actual reloads and drained Leave: a bought pontoon survives reload; another purchase afterward yields 13 parts / 1,275 kg, paid IDs 35/100 and 0 material; an accepted job and 5.26532 m attached cable survive the third reload, then actual reeling, release and sailing work. World/revision/inventory/water and helm-local player are preserved, with fresh epochs 2/3/4. [Exact evidence, source/package identity, retained failures and next work](docs/validation/salvage/SAVE-04/resume-r01/README.md); [host lifecycle and APIs](docs/salvage-cove-save-format.md). No screenshots. Manual browser saves only; native host worker/controls, world listing/export/import, durable banking, live fault/restart cases, latching, fixed-tick WaterField and Windows remain open. Parent tasks and G04 remain unchecked; no gate commit.

  - [x] **Linux native expedition Save/restart component / root under D21:** Added a bounded disk worker and the native cove host: P/Pause then F10 captures an immutable certified archive; the title acknowledges only completed mirrored publication. Selected-world startup holds the exclusive store, publishes the recovered owner/retired-parent pair before physical activation, and starts paused after one neutral tick. Explicit world/root options, missing-save/competing-owner refusal, retryable versus uncertain failures and drained worker shutdown are implemented. Fixed stalled native GPU callback processing and guarded native-only save titles in the shared WASM application. Both native build systems and the WASM application build; 335 affected native tests, 16 ext4 storage cases and 16 CMake storage cases pass. A final real native journey passes 14 records and two process restarts: paid ID 35 survives the first load; another purchase yields 13 parts / 1,275 kg, IDs 35/100 and 0 material, preserved on the second load. Exact pause ticks 33→34 and 69→70, fresh epochs 2/3, LEGO terrain and water state are retained. [Implementation, actual logs/archives, source hashes, retained failures and self-contained continuation](docs/validation/salvage/SAVE-04/native-host-r01/README.md). No screenshots. Native towing/banking restart acceptance, durable delivery receipts, Windows, world selection/export/import, autosave and remaining fault/review cases stay open. This scoped component does not pass SAVE-02/04 or G04; no gate commit.

- [ ] **G04 — The first proof cannot lose progress silently.**  
  **Needs:** SAVE-01, SAVE-02, SAVE-03, SAVE-04. **Pass:** native/browser recovery fixtures pass; banking acknowledgment and checkpoint semantics are documented in the report and reflected in UI state.

## Phase 5 — coherent scene composition

- [ ] **REND-01 — Define frame resources and pass contracts.**  
  **Needs:** BOOT-05, ASSET-05. **Deliver:** explicit HDR color/depth formats, linear/world depth conversion, exposure ownership, static cache keys, render-target lifetime and synchronization. **Pass:** a test boat/cargo scene has a documented resource graph; no sampled-attachment feedback or duplicated output conversion.

  - [ ] **Cove opaque/water integration preparation / root under D18/D19:** own `opaque_scene.*`, BlitPath, MeshPath HDR/depth output, fixture/frame plumbing, cove camera and focused renderer checks. Seed separate per-frame RGBA16F opaque color and R32F radial depth from the immutable terrain cache; draw the complete cove before water; defer output conversion to final composition. Reuse hardware object depth, keep sampled inputs distinct from attachments, and handle resize/discard/Leave. Record the 12-bytes-per-pixel targets and actual scene evidence. This static cove integration does not accept later dynamic physics, shadows, dry hulls, transparency or REND-02/G05.
    - [x] **Stationary opaque/water checkpoint:** separate HDR/depth targets, pre-water cove draws and discard/resize/Leave handling pass focused native and browser GPU/state checks. Requested extra target storage is 12 bytes/pixel. [Retained results, failures and limitations](docs/validation/salvage/LOOK-01/opaque-r01/README.md). Dynamic assemblies/cargo, shadows, dry interiors and full REND/G05 acceptance remain open.

- [ ] **REND-02 — Draw dynamic opaque objects before water.**  
  **Needs:** REND-01, SIM-03. **Deliver:** terrain plus dynamic machines/cargo/character into common opaque HDR/depth; immutable sampled inputs and separate water output attachments. **Pass:** submerged cargo is visible through refraction; foreground occlusion and above/below-water crossings are correct; WebGPU validation is clean.

- [ ] **REND-03 — Unify lighting, shadows, and hull interiors.**  
  **Needs:** REND-02. **Deliver:** shared environment/direct lighting, moving-object shadows, static-cache separation, dry-hull water exclusion and flooding-ready interior-water interface. **Pass:** a stationary camera sees moving shadows update; dry open boats contain no ocean plane; object/terrain/water exposure agrees.

- [ ] **REND-04 — Specify transparency and medium composition.**  
  **Needs:** REND-03. **Deliver:** ordered ropes/glass/spray/particles/underwater medium, depth sampling rules and bounded reflection fallback. **Pass:** cable, window, splash and submerged-part test cases remain readable; off-screen reflection limitations are documented instead of hidden.

- [ ] **G05 — Water and machinery form one visible world.**  
  **Needs:** REND-01, REND-02, REND-03, REND-04. **Pass:** native/browser test captures show correct underwater salvage, shadows, hull interior and output color through the actual application.

## Phase 6 — first two-job game, using real controls

Current checkpoint: **PLAY-05 protected custom recovery designs verified**.
Starter rebuild/H restores the original loaned boat and stores fitted paid parts
with exact identity/condition/settings. It now also protects up to four distinct
accepted custom designs. Original starter and repeated recovered layouts do not
fill extra slots; a full list refuses a new design before replacement. K loads a
backup into the editor, J cycles it, and F explicitly removes it. Launch reuses
owned parts and charges normally for missing parts. Removal cannot close an
unlaunched design. Rebuild/stock withdrawal/removal save before Resume unlocks.

Native r01 passes 12 actual-control stages, including unwritable-directory
failure, frozen controls, F10 retry, exact paid IDs 35/100 and process restarts.
Legacy browser r01 passes 10 stages; fresh browser r02 passes 28 construction,
haul, delivery and recovery stages, each plus drained Leave. Exact normalized
design digests, owned parts, materials, unique cargo and LEGO terrain survive.
Both native builds/WASM pass, with 13 archive, 37 cove and 128 authority cases,
three CMake CPU cases and 17 save/21 preview UI cases.
[Complete implementation, evidence and limits](docs/validation/salvage/PLAY-05/recovery-design-r01/README.md).

Cut preparation is now verified under D32: canonical weld cuts preserve owned
parts and prepare complete fragment shapes and inherited motion.
[Preparation evidence and live integration checklist](docs/validation/salvage/MECH-05/cut-preparation-r01/README.md).
Next implement authorized cut transactions and multi-root physics/save/recovery
before enabling the player cutter.

**Current priority: LEGO-02's actual brick builder above, following the owner's 2026-09-10 clarification. After that playable milestone, return to the remaining PLAY-05 loss/exploit matrix, general fragment work and PLAY-06's second job.** Keep all required cutting, repair, abandoned-object
bounds and banking/reload checks. The new backups protect the last accepted
custom boat; unkept or unlaunched edits are still ordinary editor work. The bounded cutter/recovery prerequisites now pass in MECH-05/live-cut-r01; wider fault/loss coverage remains required;
do not treat the later mechanics gate as already completed. Root owns the work
under D20/D21/D29/D30/D31. Full parent tasks/gates remain open; no gate commit.

Local recovery-design build: `http://127.0.0.1:38206/?experience=salvage-cove&telemetry=0`,
served from `build-recovery-design-snhtkn8t/web-r01`, the exact final browser
package. `docs/validation/salvage/PLAY-05/recovery-design-r01/manifest.json` records
source/runtime/evidence hashes. Earlier preview origins retain their saves.
The previous starter-rebuild component remains at port 38205 and
`docs/validation/salvage/PLAY-05/starter-recovery-r01/README.md`; Rescue/R evidence
remains at `docs/validation/salvage/PLAY-05/rescue-r01/README.md`.

Verified local harbor build: `http://127.0.0.1:38203/?experience=salvage-cove&telemetry=0`,
served from `build-harbor-live-13t9ivnw/web-r08`. Its exact package/evidence hashes
are in `docs/validation/salvage/PLAY-04/harbor-live-r01/manifest.json`. Browser
saves belong to that origin; earlier previews retain their own saves.

Verified local delivery build: `http://127.0.0.1:38202/?experience=salvage-cove&telemetry=0`,
served from `/tmp/voxys-durable-haul-nnz3sh1t/web-r02`. Its evidence is
`docs/validation/salvage/PLAY-04/delivery-r01/`. Browser saves belong to this
origin/profile. The prior manual-save preview at port 38201 is preserved.

- [ ] **PLAY-01 — Author the cove and job state machine.**  
  **Needs:** BOOT-05, DATA-04, ASSET-05. **Deliver:** workshop, launch/recovery spots, route landmarks, wreck, generator, crate, harbor extraction, and data-driven states: available/accepted/launched/recovery/return/banked plus abort/rescue/retry. **Pass:** predicates use authoritative state; no mission script teleports cargo or injects success to replace the mechanic.

- [ ] **PLAY-02 — Add input actions and a minimum builder.**  
  **Needs:** DATA-05, BOOT-05. **Deliver:** keyboard/mouse/controller action map, socket/ghost preview, valid/invalid feedback, select/add/remove/move/rotate/configure, undo, save/name/duplicate, camera focus/orbit/pan/zoom. **Pass:** both input schemes modify a starter hull through commands; UI focus/pointer capture cannot accidentally trigger construction or propulsion.
  - [x] **Starter workshop design checkpoint / root under D21:** the actual cove now exposes B/Workshop at its starting dock. Select, move by stud/plate, rotate, snap to an actual free socket, remove, keep and undo edit a separate bounded design using the real assembly compiler. Green/red part feedback, camera orbit/zoom and immediate keyboard focus handoff are integrated. Three native design cases plus a fixture regression, both application builds, eleven UI lifecycle cases and an eleven-stage actual browser journey with drained Leave pass. [Sources, behavior, resolved failures, reproduction and required launch integration](docs/validation/salvage/PLAY-02/workshop-r01/README.md). No screenshots. This is explicitly design-only, retained in RAM while the scene stays open; it does not mutate the live craft, inventory or GameSession. Full PLAY-02 remains unchecked while DATA-05 review, authoritative connected edits/live launch, added parts/settings, durable named designs, picking/controller/full camera scope remain. Next connect kept designs to real launch through the owned GPU/session transaction boundary, then sail a changed craft.

  - [x] **Session-owned starter checkpoint / root under D21:** the actual cooked eleven-part/17-weld skiff now has a GameSession build, exact canonical part-to-render mappings and starter-loan provenance. A checked bootstrap ID handoff avoids aliases with participant/job/cargo identities; initial physics uses the same canonical assembly. Twenty native cove checks and both application builds pass. A thirteen-stage real browser journey verifies the owned build while boarding, sailing, towing, resetting and leaving; LEGO terrain remains enabled. [Implementation, evidence, remaining transaction work and handoff](docs/validation/salvage/PLAY-02/ownership-r01/README.md). No screenshots. The workshop still edits a separate design; whole connected edits and confirmed live launch remain unfinished.


  - [x] **Connected edit/session checkpoint / root under D21:** one immutable bounded request now moves/configures/removes/adds a connected welded design through GameSession, with exact retained/fresh IDs, net catalog costs, zero loan refunds, reversible part/weld history, owned byte budgets and validated recovery. The actual workshop's moved winch and removed cradle compile through this canonical path with matching mass/ownership. All 154 native session/journal/event/cove checks, 133 strict configured WASM shared checks and both application builds pass. Native undefined-behavior and WASM publication probes each make zero allocation attempts across refit/undo/redo/cancellation/closure/prefix release. [Implementation, exact limits, results, source hashes and self-contained live-launch handoff](docs/validation/salvage/PLAY-02/refit-r01/README.md). No screenshots. The actual scene adapter still rejects changed builds; this is not live GPU launch, added-part UI or durable saving. Next wire Launch, prepare and confirm replacement of the real boat, then board/sail/reset the edited craft. Full PLAY-02 and parent gates remain unchecked.

  - [x] **Live starter launch checkpoint / root under D21:** kept designs now replace the actual GPU boat through GameSession preparation, a future physics tick and confirmed body/event evidence. Stable owned part slots drive rendering and player collision; removed parts disappear, and authoritative Undo/Redo restores their original loan identities. The real browser journey removes the cradle (1,035 → 945 kg), undoes/redoes that change, moves the winch onto the freed socket, hooks/reels cargo from the changed anchor, sails/turns the boat, observes Reset at the berth and drains Leave. Blocked helm standing space and missing propulsion are safely refused. Both application builds, 154 native checks, 12 UI lifecycle cases and the fourteen-stage real-control browser journey pass. [Implementation, exact evidence, commands, source/package hashes and remaining work](docs/validation/salvage/PLAY-02/launch-r01/README.md). No screenshots. This supersedes the older checkpoints' design-only/live-launch-next limitations. Preview: `http://127.0.0.1:38195/?experience=salvage-cove`, package `/tmp/voxys-launch-web-r03`. Controls: B opens the dock workshop, Keep accepts design edits, Enter launches, I/O undo/redo a launch, U undoes a design edit; keyboard requires canvas focus. Wave-driven movement does not disqualify an upright, unoccupied boat at its berth. State remains RAM-only and Leave loses it. **Next:** finish owned add-part/configuration UI, picking/controller/full camera and durable named designs through SAVE; cover live Leave/failure/capacity timing edges without repeating passed screenshots or journeys. Full PLAY-02 and all parent gates remain unchecked; no new full-gate commit is claimed.

  - [x] **Paid parts drawer checkpoint / root under D21/D22:** the player can choose eight admitted part types, add/edit/cancel a ghost, Keep and Launch a purchased expansion. Prices and available stock appear before purchase; UI uses lossless integer strings and GameSession independently reserves catalog costs. New paid IDs and restored originals map into reusable dynamic render/collision slots; a new-part mount preference keeps the cove's authored standing anchors clear. Both application builds, 156 native checks, thirteen UI cases and a fifteen-stage real browser journey pass. The journey purchases two 24-material/120-kg pontoons (11 → 13 parts; 1,035 → 1,275 kg), proves undo/redo identity and exact costs, refuses an unfunded launch, sails/turns, observes Reset without refilling stock, dismantles one paid pontoon for 16, restores it through Undo, and drains Leave. [Source/package hashes, exact results, commands, failure correction and next-agent handoff](docs/validation/salvage/PLAY-02/parts-r01/README.md). No screenshots. Current preview `http://127.0.0.1:38196/?experience=salvage-cove`; package `/tmp/voxys-parts-web-r02`. B opens Workshop, C selects a type, V adds, Keep accepts design edits and Enter launches; keyboard requires canvas focus. **D22** grants 48 general material and zero machinery only to a fresh host-world bootstrap, never Reset/Launch or a future restored save. Original loan refunds stay zero; paid dismantling uses catalog salvage yield. The present 32-slot scene permits seven active additions alongside its 25 installed placements; inactive paid slots can be reused after publication. **Next:** implement typed module configuration through the design/canonical/physical consumers, picking/controller/full camera and durable named/duplicated designs through SAVE; retain live failure/cancellation/capacity edge coverage. Full PLAY-02 and parent gates stay unchecked.
  - [x] **Working module settings checkpoint / root under D21/D23:** Propeller on/off, 0–100% thrust limit and reversal; helm on/off and steering limit; winch on/off now affect the actual sailing/towing consumers. X toggles, L cycles limits in 25% steps and N reverses drive; Keep then Launch applies the setting for no material cost. Runtime scene overlays, canonical compilation/refit, bounded local history and accepted scene mapping preserve settings through subsequent edits, authority undo/redo and physical Reset. Unsupported engine/channel/rope controls are not exposed. Both application builds, 166 native tests, fourteen UI cases and one nine-stage real browser journey pass. Four confirmed replacements prove zero propulsion under held throttle, disabled Hook, 75% reversed thrust driving backward, 50% steering ceiling, reopened settings, observed Reset and exact undo/redo; stock stays 48 and mass stays 1,035 kg. [Exact implementation, commands, source/package hashes, evidence and continuation](docs/validation/salvage/PLAY-02/settings-r01/README.md). No screenshots. Current verified preview `http://127.0.0.1:38197/?experience=salvage-cove`; package `/tmp/voxys-settings-web-r01`. **Next:** durable named/duplicated designs through SAVE, picking/controller/full camera, remaining typed consumers, and targeted live cancellation/failure/capacity coverage. Full PLAY-02 and parent gates stay unchecked.
  - [x] **Persistent browser named-design checkpoint / root under D21/D24:** The Workshop now offers Saved designs with name/save/load/update/duplicate/rename/backup/export/import. Loading creates one undoable kept draft, reuses matching owned parts and prices extras through GameSession at Launch; importing an old loan design cannot recreate its entitlement. The actual browser journey saves a twelve-part boat with 75% thrust/disabled winch, copies/renames/updates/restores it, reloads, loads the saved layout, exports/imports a real file, rejects a corrupted import, pays 24 material for the extra pontoon, and boards/sails the resulting 1,155-kg craft. Both builds, 192 native tests, fourteen UI cases and eight isolated IndexedDB cases pass. [Full evidence, source/package hashes, format and self-contained continuation](docs/validation/salvage/PLAY-02/designs-r01/README.md). No screenshots. Current preview `http://127.0.0.1:38198/?experience=salvage-cove`; package `/tmp/voxys-designs-web-r03`. Library entries persist only within the same browser profile/origin; export/import transfers them. **Next:** native/browser world persistence using the verified SVSC/SVJB checkpoint/journal codecs below, certified physical restore, native named-design UI, picking/controller/camera and other remaining PLAY-02 scope. Full PLAY-02 and SAVE gates stay unchecked.

- [ ] **PLAY-03 — Implement launch, helm, towing, and latching UI.**  
  **Needs:** PLAY-02, SIM-08, PLAY-01. **Deliver:** dock/helm transition, chase view, target selection, reel/pay-out, tension feedback, release and confirmed cradle latch. **Pass:** a player performs each action without debug controls; controller-only use is possible; the loaded return route is visibly and physically different.

- [ ] **PLAY-04 — Implement banking and visible harbor reward.**  
  **Needs:** PLAY-03, SAVE-04. **Deliver:** generator banking transaction, lift-power harbor state, capability unlock, general material rewards and persistent job receipts. **Pass:** repeated extraction triggers/reloads/requests cannot pay twice; the world visibly changes only after a committed reward.

  Earlier completed records below retain their scope at that time. The final live harbor component supersedes their open-harbor notes.

  - [x] **Browser durable generator-delivery component / root under D20/D21/D25:** Real delivery now secures the observed cargo, commits the canonical reward, joins a paused physical/logical archive and publishes it before exposing success or allowing Resume. Both hosts register trusted storage; exact whole-archive acknowledgment gates durable success. Failed writes remain pending for explicit retry, closure/rejected acknowledgment revoke ownership. Both native builds and WASM build pass; 264 affected native regressions, twelve save-coordinator cases and sixteen preview-UI cases pass. A 22-stage actual browser journey removes the loan cradle, buys a 12-material beam, moves the winch, launches the 1,035-kg lifting rig, hooks/lifts the 420-kg generator, steers the loaded return and delivers. Actual reload preserves paid ID 35, secured static cargo and 96 material at tick 1250 → 1251; repeated H pays nothing and the boat sails away at 2.14 m/s. [Exact sources/runtime hashes, controls, retained failures, limits and native/harbor continuation](docs/validation/salvage/PLAY-04/delivery-r01/README.md). No screenshots. Native automatic delivery is integrated/built but its real H/save/restart journey is still open. Full PLAY-03/04/SAVE-04/G04/G06, final harbor lift/unlock, live fault recovery, latching and second-job progression remain unfinished; no full-gate commit.

  - [x] **Linux durable delivery, real storage failure and process restart / root under D20/D21/D25:** Both native builds pass. Actual keyboard journeys build the paid 1,035-kg lifting rig, hook the 420-kg generator, tow it home partly submerged and hoist inside the unchanged harbor gate. The ordinary automatic-save path passes 34 stages; a separate 40-stage path makes the isolated ext4 save root unwritable, verifies the real permission error and frozen pending delivery, restores permission and retries with F10. Both terminate/reopen the actual game, preserve paid ID 35 and 96 material, refuse another reward and sail away while the banked generator stays fixed. Three observer CLI refusals pass. [Exact commands, source/binary hashes, actual saves, retained failures and next work](docs/validation/salvage/PLAY-04/native-delivery-r01/README.md). Read-only bounded observation enables the native check without screenshots or game-state setters. This verifies ordinary delivery and one prepublication fault; it does not certify mid-write/power-loss faults, Windows, general craft stability, fixed-tick water or a full gate. **Next:** the durable generator-powered harbor lift and useful unlock, then rescue/second job. Parent tasks/gates remain unchecked; no gate commit.

  - [x] **Harbor lift physics and archive preparation / root under D20/D21/D26:** Added a ten-beam fixed hoist recipe and four force-limited lines derived from the actual two-pontoon hull and body frames. The actual 1,035 kg skiff rises 3.337 m, holds upright with the entire hull 2.700 m above water, lowers at 0.25 m/s and releases afloat; mass, buoyancy, gravity and force/break limits remain real. Preserved faster-lowering/collision-clearance failures. SVCE v2 explicitly installs the new service and retains four lengths/break bits; profile 0 preserves exact v1 bytes and no added collision. Current restore deliberately refuses profile 1 until all physical consumers exist. Both native builds and WASM build pass; historical full affected 32 cove + 9 save tests, final 12 focused native checks, 4 CMake checks including the actual hoist and 152 shared WASM tests pass. [Exact scope, source/binary hashes, retained failures and required live integration](docs/validation/salvage/PLAY-04/harbor-lift-r01/README.md). No screenshots. This is preparation, not a playable harbor reward, browser GPU proof, arbitrary-craft/6-ton service rating, installed upgrade or physical save/restart. **Next:** explicit installation/migration, visible machinery, dock controls, all-rope lifetime and actual native/browser resume; parent PLAY-04/SAVE-04 and all gates remain open. No gate commit.

  - [x] **Live powered harbor component / root under D20/D21/D26/D27/D28:** Explicit installation parks the same banked generator on actual pier support, retires its old static body, admits its replacement and ten-beam frame through joined neutral ticks, and publishes the whole v2 archive before unlocking. Four real slings support safe queued attachment, Raise/Lower/Stop/Release, player collision, Pause, save/restore and owned shutdown. The shared solver uses eight accumulated-impulse sweeps; optional compliant slings absorb wave snatch loads with the original 30 kN force cap and 45 kN break threshold. Native r12 passes 12 actual keyboard stages, rising 3.563 m, saving at tick 3029 → 3030 restart and sailing at 2.151 m/s. Browser r13 passes 30 fresh-game stages plus Leave: paid construction, haul/bank/reload/no duplicate payout, install/Return/attach, 3.802 m rise, suspended tick 1980 → 1981 reload (<1 mm root change), intact lowering/release and 2.348 m/s sailing. Paid ID 35, 96 material and generator pose [7,1.925,-47] survive. Both native builds and final WASM build pass; 63 affected native cases, 20 render-owner cases, 13 save and 19 preview UI cases pass. The meaningful impact/energy comparison and exact formerly failing browser save also pass. [Final sources/runtime hashes, actual saves, retained failures and self-contained continuation](docs/validation/salvage/PLAY-04/harbor-live-r01/README.md). Full PLAY-03/04/SAVE-04/G04/G06, controller/latching, full rescue/second job, wider faults/craft coverage and final art remain open. No gate commit.

- [ ] **PLAY-05 — Implement rescue and protected recovery.**  
  **Needs:** PLAY-04. **Deliver:** abort/rescue path, safe spawn, durable starter entitlement and loan provenance, paid-addition recovery, retained blueprints, unique-cargo recovery/reset rules and bounded abandoned objects. **Pass:** loss at every first-job stage is recoverable; repeated rescue→dismantle→bank→restore cycles create no free value, including after cutting or reload; rescue does not duplicate cargo or erase the only progression item; repair/return options are clear.

  - [x] **Durable return of the accepted boat / root under D20/D21/D29:** Rescue/R joins pause, retires the real tow/harbor constraints, returns the existing accepted boat/player and only unbanked cargo, observes a final neutral tick, and publishes the exact archive before unlocking Resume. No part, cargo, entitlement or reward identity is allocated. Native r05 preserves two paid parts and zero material through available-job rescue, actual accepted/hooked/winch rescue, two process restarts and sailing; native r04 preserves the suspended paid boat/installed generator through real save failure, frozen controls, explicit retry, two rescues/restarts and sailing. Browser focused r01 and fresh r02 pass installed/uninstalled harbor cases; r02 includes actual construction/haul/delivery plus two rescues/reloads in 25 stages and drained Leave. Both native builds/WASM, 42 movement/archive, 113 ownership/recovery, 16 save and 20 preview UI cases pass. [Actual archives, precise source/runtime hashes, failures and reproduction](docs/validation/salvage/PLAY-05/rescue-r01/README.md). Removed-loan replenishment, replacement/paid storage, cut-fragment recovery, full exploit/fault matrix and parent gates remain open. No gate commit.

  - [x] **Starter rebuild and exact paid storage / root under D30:** Trusted installed kit registration and RebuildStarter restore removed loans with fresh IDs under the original entitlement, deposit exact paid parts without refunds, and consume stored paid source IDs without another purchase. Selective history invalidation prevents revival/duplication. SVSC/SVJB v2, exact forward/reverse validation, installed-scene grant binding, workshop H/Add UI, physical launch and durable native/browser pause barriers are integrated. Final native r03 passes 9 stages including actual save failure/retry and three restarts; final fresh browser r03 passes 28 stages plus Leave through construction/haul/delivery/rebuild/reuse/reload. 128 authority, 36 mapping/movement, 9 archive and 17 save-coordinator cases pass; both native builds/WASM and preview UI pass. Existing saved design-library entries are preserved. Automatic backup was not included in this checkpoint; the next component below now implements it. [Evidence, exact source/runtime hashes and remaining requirements](docs/validation/salvage/PLAY-05/starter-recovery-r01/README.md). Cut-fragment recovery, full loss/exploit/blueprint matrix and parent gates remain open. No gate commit.
  - [x] **Protected custom recovery designs / root under D31:** Before starter rebuild, stage the accepted custom boat as one of four bounded normalized design-only backups; skip the installed starter, deduplicate repeats and refuse full capacity before replacing the boat. SVCE v3 retains backups with exact legacy v1/v2 fallback. Workshop K/load, J/cycle and F/remove use existing editor/ownership costs and durable save barriers; removal cannot discard an unlaunched edit. Native r01 passes 12 stages including actual permission failure/retry, exact paid ID reuse and process restarts. Legacy browser r01 passes 10 stages; fresh r02 passes 28 construction/haul/delivery/recovery stages, each plus drained Leave. 13 archive, 37 cove and 128 authority checks pass, plus three CMake CPU cases and 17 save/21 preview UI cases; both native builds/WASM pass. [Implementation, exact evidence and limits](docs/validation/salvage/PLAY-05/recovery-design-r01/README.md). Full loss/cut/fragment/exploit matrix and parent gates remain open; no gate commit.

- [ ] **PLAY-06 — Make the second job and sidegrades useful.**  
  **Needs:** PLAY-05. **Deliver:** heavier/awkward crate job, altered tow/latch challenge, two valid build/route strategies and an immediately useful upgrade. **Pass:** both jobs can be completed without one mandated blueprint; a stable first design is not forced to fail; reward→revision→second-haul works.

- [ ] **PLAY-07 — Automate the complete visible journey.**  
  **Needs:** PLAY-06, G04, G05, G02. **Deliver:** native/browser journey through shipped actions, including save/reload, rescue and both jobs, with screenshots/counters/timing. **Pass:** actual controls produce success; no substituted physics, direct success injection, hidden reduced workload, or debug-only dependency.

- [ ] **PLAY-08 — Run and respond to the human fun test.**  
  **Needs:** PLAY-07. **Deliver:** actual 8–12-person observations against the inline human targets, reasons for confusion/failure, and implemented follow-up fixes. **Pass:** targets pass or the product is explicitly revised and retested. Agents cannot synthesize participant reports; continue independent engineering if people are unavailable.

- [ ] **G06 — First two-job proof is worth expanding.**  
  **Needs:** G02, G03, G04, G05, PLAY-01, PLAY-02, PLAY-03, PLAY-04, PLAY-05, PLAY-06, PLAY-07, PLAY-08. **Pass:** real players build, recover, return, improve and voluntarily play again; durable state and visible performance evidence accompany the result.

## Phase 7 — general assemblies, crane, and structural damage

- [ ] **MECH-01 — Generalize construction beyond the starter sockets.**  
  **Needs:** G06, SIM-01, DATA-05. **Deliver:** full permitted lattice/orientations, arbitrary valid weld connections, disconnected-component handling, multi-root compile and over-limit rejection within the stated budgets. **Pass:** diverse 256-part builds compile consistently; socket fit, collision and visible geometry agree; invalid disconnected launch states have clear feedback.

- [ ] **MECH-02 — Implement the powered-crane decision spike.**  
  **Needs:** MECH-01, SIM-09. **Deliver:** hinge limits/motors/force caps and a loaded crane test: long arm, high mass ratio, grounded base, loop/limit cases, slack-to-taut line and two interacting machines. **Pass:** record stability, error, energy, frame/tick cost, input latency and implementation effort against declared tolerances; no sparse-body benchmark substitutes.

- [ ] **MECH-03 — Select and finish the machine solver route.**  
  **Needs:** MECH-02. **Deliver:** recorded GPU extension versus explicit CPU-adapter decision. Default stays WebGpuSoft when it meets stability/development goals. If CPU wins, integrate shapes, LEGO terrain, handles, events, transactions, joints, water and bounded render uploads. **Pass:** one solver owns each authoritative body; all earlier game/save tests pass under the selected route.

- [ ] **MECH-04 — Estimate structural loads and give warnings.**  
  **Needs:** MECH-03, DATA-06. **Deliver:** bounded contact/winch/module load distribution onto bonds, material-strength parameters, accumulated damage, creak/strain events and tool-driven cuts. **Pass:** controlled lever/brace fixtures fail in understandable locations; warnings correspond to actual risk; no claim of unavailable welded-joint impulses or full FEM.

- [ ] **MECH-05 — Commit fracture and meaningful fragments atomically.**  
  **Needs:** MECH-04, SIM-06. **Deliver:** graph split, reserved new bodies/shapes, mass/inertia, joint retargeting, collision-cache invalidation, render mapping and ownership under one revision. **Pass:** post-solve velocity inheritance preserves mass/momentum; impacts are not doubled; repeated split/reset/reuse leaves no stale parts/endpoints; full pools never erase important cargo.

  - [x] **Cut preparation and fragment motion / root under D32:** `AssemblyFracturePlan` disables exact enabled weld IDs, preserves every paid/loan part and bond record, increments topology once and compiles all resulting mass/collision/buoyancy/function roots. Immutable parent/fragment mappings and checked post-solve origin-velocity inheritance preserve momentum and energy without accepting another impact impulse. Eight shared cases cover rotations/asymmetric mass, alternate weld paths, independent roots, a 256-part/64-root split, capacity/stale/malformed refusal and canonical round-trip. The actual purchased cove pontoon separates into two valid prepared shapes with all 12 parts and 1155 kg retained. 126 Bazel cases, nine CMake cases, 88 JS-EH/Asyncify WASM cases and native/WASM application builds pass. [Exact evidence and required integration](docs/validation/salvage/MECH-05/cut-preparation-r01/README.md). This preparation does not activate live cuts: authorized journal/replay, multi-root GPU publication, saves, rope/player/render remapping and rescue remain mandatory. MECH-05/PLAY-05 and gates stay open.

  - [x] **Cut authority and logical recovery / root under D32:** shared `BuildModel::cutWelds`, exact `CutWeld` command and immutable before-bond transition preserve all parts, stock, starter bindings, resources and allocator IDs. Cuts retire only their build's launch history, reserve the complete journal payload before preparation, recheck future-tick leases and publish after confirmed execution. Pending crash recovery cancels without changing ownership; committed cuts replay exactly and survive fresh-writer recovery. Logical SVSC/SVJB v3 adds only the required retained cut fields and preserves old bytes. Eight new authority cases include thirteen forged-payload/ID cases, eleven refusal paths, every journal boundary, exact memory limits, unrelated-build Undo and repeated rebuild after three-part separation without paid duplication. 250 Bazel, 156 CMake, 179 authority/save WASM and 88 compiler/fracture WASM cases pass; both native application builds and configured WASM build pass. [Exact evidence, format and next integration](docs/validation/salvage/MECH-05/cut-authority-r01/README.md). The live cove adapter still refuses cuts until every physical root, motion archive, rope/player/render mapping and rescue path is supported. Full MECH-05/PLAY-05 and gates stay open.

  - [x] **Per-root cove physical preparation / root under D32:** `CoveBoatAssembly::compileFragments` prepares all root shapes, disjoint water cells and per-root module settings, selected by an explicit stable helm part. Existing single-root admission/Launch uses the shared preparation. Three new cases cover a detached lowest-ID pontoon, every part separated, and actual native GPU replacement of the moving 1035 kg skiff with two floating bodies. All 88 native authored-GPU/cove cases pass; both applications build; the corrected native delivery/save/restart journey passes 34 stages. Six browser collision cases pass; full browser recovery failed during the loaded lift; water time advanced six times faster than physical time, as documented in [the evidence](docs/validation/salvage/MECH-05/cove-roots-r01/README.md). This completes prepared content and the controlled GPU split fixture only. Live cutting remains disabled; no MECH-05/PLAY-05 or gate acceptance.

  - [x] **Native atomic parent-retirement primitive / root under D32:** `PhysicsMutationBatch::bodyDestroys` validates all already encoded parents before changing the live queue. Invalid/stale/duplicate handles, unexecuted spawns, conflicting commands/attachment creates and full capacity reject the entire set. Preallocated linear-time membership and retirement storage make commit allocation-free; discard preserves handles/generations. Eight actual native transaction cases pass, including three new rejection/transfer/reuse/capacity cases; the authored moving-skiff-to-two-floating-roots fixture passes using the new batch. Fixed missing cove runfiles in the combined test target; the failed fixture setup and subsequent pass are retained. Both native build paths and the WASM application compile. [Exact scope and evidence](docs/validation/salvage/MECH-05/parent-retirement-r01/README.md). This is native execution only; existing fixed-tick preparation restrictions remain. Add checked joined-pause admission for the accumulator-driven cove, every child reservation, live root ownership and browser execution before claiming full cuts. No parent task or gate pass.

  - [x] **Joined cove section ownership and ordinary Launch / root under D32:** `CoveRigidRoots` replaces the scalar runtime boat state with bounded keyed records. Application maps parts to their root, observes every record, requires a common completed tick for save/replacement, owns pending children and borrowed parents, cancels unexecuted children on refusal and drains retired shapes. Checked joined-boundary admission supports the actual accumulator workshop. Native construction/haul/save/restart passes 37 stages; browser construction/delivery/reload passes 29 with paid part 35, 96 material and preserved wave phase. The later all-or-nothing inherited-motion helper passes native two-parent/four-child refusal/position/velocity checks and the actual moving-skiff-to-two-floating-bodies test, with the helm on root 1. The final combined selection passes 11 cases; all 11 mass-frame cases and final native/CMake/WASM builds pass. [Exact artifacts, retained failures and remaining integration](docs/validation/salvage/MECH-05/live-roots-r01/README.md). Gameplay journeys attest the frozen ownership/Launch build before the helper; browser fragment execution, all-root startup, full capacity rollback, atomic render publication, v4 live saves, rider/rope remapping and remote recovery remain open. Cutting stays disabled; no full MECH-05/PLAY-05 or gate pass.

  - [ ] **Own every live cove root and publish cuts atomically:** replace `Application::SalvageLocalSessionState::AssetPreview`'s single boat shape/body/observation with a bounded owning root collection. Key records by `AssemblyMassRoot::key` (least member part ID) plus accepted build revision; never persist GPU handles or assume root zero owns the helm. Reserve all shapes, bodies, pose/event slots and supported water drivers before retiring any parent. Zero-buoyancy roots get no water driver. Stage `AssemblyFracturePlan`'s certified post-solve motion; await every child and old-parent retirement before no-fail logical/physical publication. Exercise allocation/driver/queue rejection at each stage, full pools and canceled unexecuted children without losing owned parts.

  - [x] **Physical fragment archive codec / root under D32:** SVCE v4 stores every compiled root's stable key and authored-origin motion, with explicit controlling helm and rider-root IDs. Exact membership/order comes from the accepted weld graph; the primary mirror, rider/helm and detached multi-root harbor rules are checked. Missing/duplicate/foreign/stale roots and re-signed v1/v2/v3 downgrade attempts refuse; valid older single-root bytes and the total size ceiling remain unchanged. Eight new cases cover all 32 roots plus four protected designs, wrong bindings, corruption/capacity, a detached winch and fresh-writer/retired-parent recovery. 224 Bazel cases (73 freshly executed, 151 valid cached), 21 CMake archive cases and 187 JS-EH/Asyncify WASM cases pass; both native build paths and the WASM application build pass. Native/WASM assert identical 1878-byte and 8862-byte v4 fixtures. [Wire format, exact evidence and required live integration](docs/validation/salvage/MECH-05/root-archives-r01/README.md). This is the portable codec only: startup still admits v1-v3 and multi-root physical capture/restore/admission remains required below. Full archive integration, MECH-05/PLAY-05 and gates stay open.

  - [x] **Live v4 saves and section-aware restore preparation / root under D32:** the host captures every owned root at the joined pause tick and accepts v1–v4 startup. `CoveRestoreCandidate` reconstructs accepted disabled-weld topology, all saved motions and rider/winch section bindings without reviving absent starter collision. Application admits the root collection and observes each section; player collision and tow controls use their owning section. 39 CPU cases and 3 CMake restore cases pass, including a three-section boat with helm on root 1, detached rider and detached winch. Native v4 purchases/two restarts pass 14 stages; an actual historical v1 load/F10 upgrade/v4 restart passes 3 with paid part 35, 96 material and onboard root 7 intact. Browser r01 completes two paid v4 reloads; after diagnosed driver fixes, the nine-stage r05 continuation boards, hooks/reels, saves at tick 134, reloads at 135 with unchanged water phase/cable, reels/releases and sails at 2.07 m/s. Native/Bazel, native/CMake and WASM builds pass. [Exact scope, retained failures, hashes and reproduction](docs/validation/salvage/MECH-05/root-resume-r01/README.md). Multi-root CPU preparation is not an actual runtime fragment journey. Full capacity rollback, atomic cut publication, all-root Rescue and cut/reload acceptance remain open below. Cutting stays disabled; no full MECH-05/PLAY-05 or gate pass.

  - [x] **Atomic section recovery preparation and host integration / root under D32:** Rescue prepares the whole accepted layout, validates every section's joined motion, reserves all teleports/velocity resets plus unbanked cargo in one physics mutation, and saves only after all actual observations join. Parts, disabled welds, inventory and banked cargo remain unchanged. 41 CPU cove cases plus one paid-layout/dock-clearance case pass. The actual two-section GPU case proves distant return and all-or-nothing queue/stale-body/discard refusal; its combined invocation's unrelated CPU assumption was corrected and separately rerun. Both native application builds and WASM pass. Native delivered-boat Rescue/two restarts/sailing passes 7 stages; browser attached-generator Rescue/two saves/reloads plus Leave passes 6 checkpoint stages with exact paid IDs and cargo. [Evidence, failures, reproduction and next work](docs/validation/salvage/MECH-05/root-recovery-r01/README.md). This checks the native GPU primitive and ordinary application recovery, not actual multi-root application cut/recovery. The wider-boat boarding regression is resolved by the following item; full multi-root application recovery and parent tasks/gates remain open.

  - [x] **Fix the reproduced paid-boat boarding failure after Rescue / root under D32:** actual GPU sampling identified a 75 cm placement error at the failed berth/phase. The old CPU estimate sampled the wrong spatial origin and omitted spectral modes. `WaterSimulation::samplePlacementHeight` now reconstructs the complete spectrum at the two cascades' eight required texels, applies half-float rounding/bilinear filtering and shared swells; normal per-frame sampling keeps its 24-mode budget with corrected coordinates. Spawn, Launch and Rescue use the checked placement query. All five water tests pass, including 40 GPU comparisons across two spectra, repeat boundaries and saved phases, within 5 mm. The unchanged previously failing native driver now passes all eight Rescue/boarding/hook/reel/restart/sailing stages for the original 13-part source; paid IDs 35/100, zero material and the unique generator remain exact. First post-Rescue speed is 0.04532 m/s rather than 1.2483 m/s. Browser r02 passes two Rescues/reloads, boarding/Helm/sailing at 1.8039 m/s and drained Leave; browser errors are empty. R01 completed gameplay but failed its old-owner poll after normal Leave navigation; the driver now waits for the return page. Both native builds and WASM pass. [Cause, retained failures, exact scope and reproduction](docs/validation/salvage/MECH-05/boarding-r01/README.md). No reach, collision, parts, wave strength or GPU-force rule was weakened. This is a cove placement correction, not full SIM-05/WaterField or performance acceptance. Full cuts, multi-root application recovery and gates remain open.

  - [x] **Archive the exact physical fragment set:** physical SVCE v4 validates and captures every accepted root's least-member durable key, authored-origin motion, stable helm and ridden section at one joined tick; logical SVSC/SVJB v3 stays independent. Missing/duplicate/foreign/stale roots and inconsistent bindings refuse; v1-v3 bytes/loads remain compatible and fragmented saves cannot downgrade. Actual native and browser journeys cut the moving 13-part boat into roots 7/15, reload both, Rescue/reload both, rebuild and reload paid storage. Final 21 archive cases and the earlier corruption/capacity/legacy/portable golden fixtures pass. [Full integration, artifacts and reproduction](docs/validation/salvage/MECH-05/live-cut-r01/README.md). This completes the bounded cove root archive item, not general world persistence or a gate.

  - [x] **Retarget all live cove consumers by part/root:** rendering, collision/rider support, helm and winch/tow bindings use the accepted part's owning body; no root-zero assumption. At a cut, preserve build-local feet on the supporting part and leave Helm for Walking if that deck separates from the helm. Freeze input while parents retire; never send commands to their retained cleanup handles. Active cables, airborne/swimming cuts and unsupported multi-root harbor operations refuse before mutation. Native r03 verifies a real attached cargo cable prevents a cut, then releases it and completes cut/reload/Rescue/rebuild; browser r01 completes the same section lifecycle. Prior detached-winch/rider fixtures and endpoint/retirement tests remain in the regression suite. [Exact verified limits](docs/validation/salvage/MECH-05/live-cut-r01/README.md). Arbitrary joint retargeting and moving-platform jumping remain later mechanics/actor work.

  - [x] **Recover fragment ownership before exposing cutting:** **C / Cut weld** selects the named nearest enabled weld within 2 m of the player's world-space hand and rechecks that exact weld at joined execution. Stage the last intact custom design in the four-slot recovery library before cutting; built-in starter protection needs no slot. For already cut builds, an all-weld temporary copy only identifies an existing protected design; never publish it or invent missing protection. Full/missing protection refuses; no backup deletion while cut bonds remain. Admit all shapes/bodies/water drivers, cancel all unexecuted children on refusal, then retire all parents and publish at joined execution. Cut saves automatically and Resume waits for acknowledgment. Rescue preserves disabled welds and returns every section; the broken workshop supports H/Rebuild and K/Load protected design. Rebuild requires every section within 4 m horizontally of its authored home anchor and stores each paid part exactly once. Native r03 passes 15 stages, browser r01 passes 14 plus drained Leave; paid IDs 35/100, zero material, the accepted unique generator and the intact design survive cuts, reloads, Rescue and repeated rebuilding. Both native builds/WASM pass; final 54 cove and 21 save Bazel cases, 46 CMake CPU and eight CMake GPU cases pass, including partial body/water-pool refusal and reuse. [Retained failure/fix, frozen builds, exact results and next work](docs/validation/salvage/MECH-05/live-cut-r01/README.md). No screenshots. This completes the bounded D32 cutter/recovery prerequisite; broader allocation/fault coverage above, full loss/exploit cases, MECH-05/PLAY-05 and gates remain open.

- [ ] **MECH-06 — Add repair, cutting, and assembly recovery.**  
  **Needs:** MECH-05, PLAY-05. **Deliver:** repair/cut tools, restricted field replacement, workshop reattachment with inventory and momentum policies, damaged module behavior and rescue integration. **Pass:** damage changes function/readability; repair costs/refunds are exact; no free-motion welding exploit or unreachable essential component.

- [ ] **MECH-07 — Add compartments, breaches, and flooding.**  
  **Needs:** MECH-06, SIM-05, REND-03. **Deliver:** authored sealed/open volume contract, breach/flood state, pumps/patching, flotation/mass response and matching interior water/exclusion. **Pass:** controlled breach changes motion and visual water correctly; displaced water and interior mass are not double-counted; draining/reload/repair is consistent.

- [ ] **MECH-08 — Complete dynamic checkpoints and bounded replay.**  
  **Needs:** MECH-07, SAVE-04. **Deliver:** all Contract E states, certified tick snapshots, bounded authoritative replay checkpoints/events and semantic resume for articulated/damaged/flooded craft. **Pass:** interrupted saves with detached cargo, partial cuts, latched loads and motor targets recover; replay shows meaningful events without claiming cross-GPU bit-exact input replay.

- [ ] **G07 — General machine foundation complete.**  
  **Needs:** MECH-01, MECH-02, MECH-03, MECH-04, MECH-05, MECH-06, MECH-07, MECH-08. **Pass:** a constructed crane craft loads, breaks, repairs, saves/reloads and finishes both jobs with consistent simulation/render/ownership revisions.

## Phase 8 — final-quality slice: presentation, player, and tools

- [ ] **VIS-01 — Implement the assembly renderer.**  
  **Needs:** SIM-06, REND-04, ASSET-03. **Deliver:** current/previous root poses, local-part tables, root-first culling, part-cluster LOD, mesh/material bins, indexed indirect draws and bounded outputs. **Pass:** exercise 256-part craft and 8,192 detailed visible-part targets; moving GPU roots do not require per-part CPU pose uploads; resets/splits leave no stale instances.

- [ ] **VIS-02 — Add motion history and temporal reconstruction.**  
  **Needs:** VIS-01. **Deliver:** camera/object motion, jitter policy, unjittered cache identity, depth/material/ID/topology rejection, reactive water/foam, rebase/teleport handling and sharp reference mode. **Pass:** moving views reduce shimmer without objectionable blur/trails; splits/edits have correct history; cache refresh cost matches the declared design.

- [ ] **VIS-03 — Finish materials, LODs, and visual damage.**  
  **Needs:** VIS-02, ASSET-06, MECH-05. **Deliver:** final production meshes/materials for the slice kit including the pontoon, consistent plastic/metal/wetness response, pixel-footprint stud/bevel/seam LOD, broken interiors and damage states. **Pass:** independent visual review approves the actual playable kit; important silhouettes/socket cues survive LOD; directional lighting exposes no baked-light artifacts; color/exposure matches terrain/water and remains readable in motion.

- [ ] **VIS-04 — Add local water and impact effects.**  
  **Needs:** VIS-03, SIM-05, DATA-06. **Deliver:** pooled wakes, propeller foam, splashes, drained water, dust/impact effects and readable rope rendering. **Pass:** effects follow actual events/forces, respect depth/medium ordering, fade within bounds and do not alter authoritative cargo or exceed their allocation budget.

- [ ] **VIS-05 — Produce the final cove and harbor art.**  
  **Needs:** ASSET-07, VIS-04, PLAY-06. **Deliver:** coherent foreground/mid-distance/horizon; workshop, wreck, crane, navigation landmarks, shore treatment and a final weather state. **Pass:** independent review of playable captures approves silhouette, lighting, material consistency, water integration and navigation; no required placeholder is hidden by camera framing.

- [ ] **UX-01 — Finish workshop and blueprint management.**  
  **Needs:** PLAY-02, MECH-01, G04. **Deliver:** symmetry, multi-select, move/replace, paint, named build library, duplicate/import/export, optional COM/flotation/stress views and clear invalid-placement reasons. **Pass:** controller and keyboard/mouse can construct, name, test, undo, save, reload and launch without inventory drift; model diagnosis uses understandable labels.

- [ ] **UX-02 — Complete application screens and input lifecycle.**  
  **Needs:** PLAY-03, UX-01. **Deliver:** title/new/load, job board, map, inventory, workshop, on-foot/driving, pause, rescue, results and settings; text entry, focus/modal/back rules, rebinding, sensitivity/inversion/deadzones and hold/toggle alternatives. **Pass:** hot input switching, controller reconnect, focus loss and resize cannot trap input or trigger unintended actions.

- [ ] **UX-03 — Define safe test-launch and pause behavior.**  
  **Needs:** UX-02, SAVE-04. **Deliver:** workshop test mode uses a temporary copy and clearly discards test damage/rewards on exit; expedition launch uses authoritative resources/state. Solo pause freezes simulation; co-op UI later honors the shared-world rule. **Pass:** no test-mode reward/inventory leakage; restart/quit behavior is clear and recoverable.

- [ ] **UX-04 — Add accessible onboarding and localization structure.**  
  **Needs:** UX-02, PLAY-06. **Deliver:** action-based tutorials, short repeatable help, scalable text/UI, icon+color cues, captions, reduced motion, remappable controls, localization keys and expansion space. **Pass:** full controller-only journey and dyslexia/accessibility sessions reveal no mandatory unreadable or unreachable step; instructions never require long paragraphs to proceed.

- [ ] **ACT-01 — Build the robot asset and actual animation runtime.**  
  **Needs:** ASSET-03, ASSET-06, PLAY-03. **Deliver:** rig or rigid-node animation, sampling/blending/hierarchy evaluation, locomotion and tool/helm interaction states. Implement skinning explicitly if used. **Pass:** exported animations run in native/WASM, with correct normals/bounds/anchors; declared but unused VMESH skin fields are not treated as implementation.

- [ ] **ACT-02 — Implement dock locomotion and camera collision.**  
  **Needs:** ACT-01, SIM-03, UX-02. **Deliver:** grounded movement, jumps/steps, robust spawn, third-person camera collision, orbit/chase transitions, load visibility and reduced-motion settings. **Pass:** cliffs, studs, slopes and nearby machinery do not cause camera clipping or character penetration; transitioning controls remains predictable.

- [ ] **ACT-03 — Support moving decks, boarding, and water recovery.**  
  **Needs:** ACT-02, MECH-03, SIM-05. **Deliver:** moving-platform contact/frame handling, inherited velocity, boarding/helm occupancy, exit/fall states, simple swimming and rescue. **Pass:** walk/jump/fall on rolling/turning boats and moving links without artificial drag or velocity loss; a player cannot be trapped by a sunken craft. Multiplayer reconciliation follows in NET tasks.

- [ ] **SND-01 — Implement event-driven audio and optional haptics.**  
  **Needs:** DATA-06, UX-02. **Deliver:** native/browser audio backend, bounded voices, priorities, spatialization, buses, persistent volume settings, browser unlock and optional intensity-controlled haptics. **Pass:** output-device changes and focus/suspend recover; overload is bounded; mute/captions/reduced-motion preserve critical information.

- [ ] **SND-02 — Author the slice soundscape.**  
  **Needs:** SND-01, MECH-06, VIS-04. **Deliver:** engine-load/propeller, winch strain, plastic stress, material impacts, splashes, latches, cutting/repair, UI/rewards, ambient coast and music states. **Pass:** recorded actual playback matches events and remains intelligible under load; graph execution alone does not prove sound quality or output.

- [ ] **G08 — One final-quality expedition is complete.**  
  **Needs:** G07, VIS-01, VIS-02, VIS-03, VIS-04, VIS-05, UX-01, UX-02, UX-03, UX-04, ACT-01, ACT-02, ACT-03, SND-01, SND-02. **Pass:** independent visual/audio review and renewed human sessions approve both jobs through shipped controls; moving-deck behavior, saves, performance, accessibility and recovery work together.

## Phase 9 — scalable authoring and bounded world loading

- [ ] **WORLD-01 — Add part and mission authoring tools.**  
  **Needs:** G01, ASSET-07, PLAY-06. **Deliver:** part test scene and site/mission authoring for sockets, modules, cargo IDs, objective predicates, extraction, prerequisites, rewards, spawns and recovery. **Pass:** a designer adds one new valid pontoon and one complete recovery job without editing C++ or WGSL; errors identify the offending asset/property.

- [ ] **WORLD-02 — Integrate content identity and cooking.**  
  **Needs:** WORLD-01. **Deliver:** stable content-ID registry, dependency graph, deprecation/migration rules and runtime bundles. Include authoritative schemas, shapes, relevant shaders/code and physics parameters in compatible identity for the selected demo/campaign profile. **Pass:** mismatched authoritative content is rejected; native/browser demo identities agree while full-campaign admission rejects a demo-only client; unrelated installed bundles do not cause false incompatibility; moved/renamed assets preserve saves through explicit migration; both build systems package the same manifests.

- [ ] **WORLD-03 — Split experience packages and load asynchronously.**  
  **Needs:** WORLD-02, SAVE-04. **Deliver:** load only selected-mode essentials, background bundle loading/decoding, bounded staging/upload queues, deduplicated resources and clear readiness states. **Pass:** unrelated prototypes are not preloaded by salvage; interactive areas require ready collision/gameplay data; missing/corrupt bundles cannot corrupt the canonical save.

- [ ] **WORLD-04 — Implement region and assembly residency.**  
  **Needs:** WORLD-03, MECH-08. **Deliver:** sector-local state, camera-relative rendering, checkpoint/unload rules, dependency-group pinning and the whole-party region-transition state machine. **Pass:** repeated travel/unload/reload retains changes and has bounded memory; occupied craft/crew/cargo transfer together after leases and pending edits settle; interruption recovers the prior or fully committed destination region; no rope spans regions and valuable abandoned cargo follows its recovery rule.

- [ ] **WORLD-05 — Add safe cancellation and GPU resource retirement.**  
  **Needs:** WORLD-04, SIM-04. **Deliver:** cancellation of obsolete loads/cooks, revision/content checks, completed-fence retirement, device-loss reload and bounded pending resources. **Pass:** teleport/reset/cancel/device recovery under ongoing uploads leaves no leaks, use-after-free, stale assets or false-completed work.

- [ ] **WORLD-06 — Add compressed-texture support and quality presets.**  
  **Needs:** WORLD-03, VIS-03. **Deliver:** versioned cooker/runtime support for KTX2/Basis or an equivalently documented supported format, target-format selection, shared-material deduplication and explicit presets. **Pass:** decoded appearance matches reference and memory/loading improve; unsupported formats fall back deliberately. Existing VMESH RGBA8 data is not mislabeled as compression.

- [ ] **WORLD-07 — Resolve terrain residency from actual region sizes.**  
  **Needs:** WORLD-04, WORLD-06, VIS-05. **Deliver:** measured per-region terrain/collision/shadow memory; retain resident compact regions if budgets pass, otherwise implement real chunk/mip streaming with conservative traversal and collision readiness. **Pass:** selected design supports all four planned region sizes, teleports and boundaries without silent geometry changes; material-atlas streaming is not counted as terrain streaming.

- [ ] **WORLD-08 — Measure content-production throughput.**  
  **Needs:** G08, WORLD-01, WORLD-02. **Deliver:** timed source→export→cook→in-game→review cycles for a final part and site; a staffing/time forecast with real measured rates and remaining tooling blockers. **Pass:** another content creator reproduces the process; the campaign estimate includes integration/review and rework, not only mesh generation.

- [ ] **G09 — Production content can be made and loaded safely.**  
  **Needs:** WORLD-01, WORLD-02, WORLD-03, WORLD-04, WORLD-05, WORLD-06, WORLD-07, WORLD-08. **Pass:** second-region test content loads, plays, unloads and restores within documented memory/time budgets, and a designer can deliver new content without source edits.

## Phase 10 — real co-op, first two players and then four

- [ ] **NET-01 — Generalize the host authority and protocol.**  
  **Needs:** MECH-08, DATA-05, WORLD-02. **Deliver:** host-owned expedition state, participant IDs/leases, canonical command ordering, idempotent receipts, compatible content checks and bounded protocol schema. **Pass:** two in-process participants cannot duplicate edits/rewards; every canonical field has one authority; legacy WRECKWATER's 11-body scope is not inherited as claimed capacity.

- [ ] **NET-02 — Add real secure native and browser transports.**  
  **Needs:** NET-01, BOOT-04. **Deliver:** encrypted transport, admission/authentication, host/discovery/join/leave flow and explicit deployment adapter configuration. Native hosts support matching native/browser demo participants; full-campaign sessions require native campaign clients. Reuse transport-independent session code. **Pass:** actual processes join over the selected transport; browser code is wired to the demo expedition and profile compatibility errors are readable. Browser hosting is not required. Credentials and access are provisioned explicitly, never fabricated.

- [ ] **NET-03 — Replicate roots, topology, and dependency groups.**  
  **Needs:** NET-02, VIS-01, WORLD-04. **Deliver:** bounded root/joint snapshots, reliable topology/economic events, epoch/revision handling, unknown-revision queue/baseline requests and dependency-aware interest. **Pass:** out-of-order split/motion delivery never produces invalid root mappings; rope/cargo endpoints cannot disappear independently; bytes and queue peaks are measured.

- [ ] **NET-04 — Add prediction, correction, and crew interactions.**  
  **Needs:** NET-03, ACT-03. **Deliver:** responsive input, occupied/towed-machine-aware prediction policy, interpolation/correction, moving-platform reconciliation, helm/repair/winch interaction ownership and edit leases. **Pass:** two players walk/operate one moving craft without fighting authority; corrections are bounded and visible tests show acceptable control feel.

- [ ] **NET-05 — Implement reconnect and host save ownership.**  
  **Needs:** NET-04, SAVE-02, MECH-08. **Deliver:** disconnect-neutral controls, rejoin baseline, host-owned progression, guest session state, canceled leases, durable receipts/processed markers and explicit host-departure flow. **Pass:** disconnect during latch/bank/edit and retries after receipt eviction/checkpoint/rejoin cannot duplicate or lose cargo/inventory; retired tokens stay invalid; guests cannot transfer resources to another world; host departure follows documented recovery behavior.

- [ ] **NET-06 — Validate two real rendered clients under adversity.**  
  **Needs:** NET-05, G08. **Deliver:** two-client complete haul, boarding/cutting/latching/return/save/reconnect, and transport adversity tests at ≥100 ms RTT, jitter, 2% loss, duplication and reordering. **Pass:** final canonical ownership agrees; actual packet/transport behavior and deterministic frame-adversity tests are distinguished; local latency/correction quality is reported.

- [ ] **G10 — Two-player co-op is proven.**  
  **Needs:** NET-01, NET-02, NET-03, NET-04, NET-05, NET-06. **Pass:** two real clients complete and resume an expedition without duplicates, broken topology, or unusable moving-deck control.

- [ ] **NET-07 — Expand to four-player launch workload.**  
  **Needs:** G10, G09. **Deliver:** four-player roster/UI, multiple occupied craft, simultaneous tools/edits, whole-party travel, full launch-capacity budget and interest/host-load tuning. **Pass:** four actual clients complete shared/separate jobs inside one active region and travel together; mixed native/browser demo clients obey the same profile limits; combined roots/joints/cargo stay within world limits; one client cannot starve session work.

- [ ] **NET-08 — Finish social coordination and hosted operation.**  
  **Needs:** NET-07. **Deliver:** room/invite or connection workflow, contextual pings, player/build names, host permissions, session errors and operating instructions; sufficient service monitoring for the chosen hosting route. Voice/chat services are outside mandatory scope unless added explicitly. **Pass:** unfamiliar players join, coordinate, reconnect and leave without developer help or private debug arguments.

- [ ] **G11 — Launch multiplayer count is proven.**  
  **Needs:** NET-07, NET-08. **Pass:** four real rendered clients pass the complete expedition and adversity/reconnect suite at declared client/host budgets. A two-client gate cannot substitute.

## Phase 11 — produce the complete game

These are working region names, not finished branding. Each region needs a distinct recovery problem, landmarks, workshop access, a safe recovery route, optional discoveries, and readable progression. Region transitions may use authored passages/loading; a seamless planet is not required.

| Region | Required gameplay identity | Main progression role |
|---|---|---|
| Harbor Reach | Sheltered water, nearby wrecks, introductory towing and loaded returns | Starter craft, generator/lift restoration, first sidegrades |
| Cliffworks | Elevated platforms, heavy suspended loads, constrained access | Powered cranes, cutting, bracing, load-path diagnosis |
| Marsh Delta | Shallows, channels and land approaches that make pure boats inconvenient | Amphibious mobility, traction, route and weight tradeoffs |
| Stormbreak | Exposed water, flooding risk and multi-stage recovery | Combined mastery and final harbor restoration |

Campaign progression must unlock useful capabilities and new recovery opportunities. Difficulty comes from interacting systems with multiple valid solutions. A job may recommend a capacity/tool; it must not require one exact hidden build. Stormbreak needs forecast/readability and safe exits, not randomly unavoidable losses.

- [ ] **CONTENT-01 — Establish the staffed production schedule.**  
  **Needs:** G08, WORLD-08, BOOT-04. **Deliver:** remaining work breakdown, named ownership/capacity for engine/physics, gameplay/network, technical art/tools, environment/character art, design/UI, sound and QA; critical path, integration cadence, operating costs and risk reserve. **Pass:** dates use measured content throughput and actual staffing. As an initial hypothesis only, baseline/contracts may take 4–8 weeks, the playable proof 8–16, the final-quality slice 12–24, and production tooling/co-op 8–16, with overlap only where dependencies permit. Re-estimate full production after the slice; do not promise a AAA campaign on those milestone estimates alone. Budget using staff-months × actual fully loaded rates + contractors/services/hardware + a stated contingency (initial planning range 25–35%). AI generation time is not a substitute for integration, animation, design review, or QA.

- [ ] **CONTENT-02 — Author the campaign and economy graph.**  
  **Needs:** G09, PLAY-08, CONTENT-01. **Deliver:** all required/optional jobs, prerequisites, material sources/sinks, capability unlocks, harbor restoration stages, finale, recoverability rules and a route through all four regions. **Pass:** automated graph checks and designer walkthroughs find no unreachable mandatory objective, consumed unique-key dead end, repeat-reward exploit or forced grind after rescue; first-time players understand why they want the next machine.

- [ ] **CONTENT-03 — Implement amphibious running gear.**  
  **Needs:** G07, MECH-03, WORLD-01, G11. **Deliver:** powered wheels, steering, suspension/contact model, brakes, drive/traction tuning, relevant editor diagnostics, and water/shore transition handling. **Pass:** an actual player-built hauler drives on slopes and soft-looking authored terrain, enters/exits water, transports cargo and saves/reloads. Wheel/suspension forces derive from contact/compression and buoyancy from submerged volume; their combined reaction approaches weight at equilibrium, rather than assigning full weight to each system. Test continuous wheel unloading with immersion, loss of ground contact, low/high speed, uneven terrain, varied COM, articulation and four-player replication. Traction parameters, slope limits and supported surfaces are data, not hidden per-mission hacks.

- [ ] **CONTENT-04 — Complete the functional part library.**  
  **Needs:** CONTENT-02, CONTENT-03, G02, MECH-06, WORLD-02. **Deliver:** 50–70 distinct functional definitions across structure/flotation, power/control, towing/latching, crane/tool, repair and amphibious families; production meshes/materials/LODs, sockets, colliders, mass/inertia/buoyancy, costs, damage variants, names/icons and localization keys. **Pass:** each part runs through authoring/import validation and a functional demonstration; representative combinations, awkward socket rotations and maximum craft sizes are tested. The catalog supports meaningfully different craft; cosmetic colors and trivial size variants do not satisfy the count.

- [ ] **CONTENT-05 — Finish Harbor Reach and Cliffworks.**  
  **Needs:** CONTENT-02, CONTENT-04, G09, G11. **Deliver:** final landmarks/sites/wreck assemblies, jobs, cargo, environmental lighting/sound and workshop access for the first two regions. **Pass:** solo and co-op crews complete each required branch using at least two viable engineering approaches where appropriate; travel, editing, streaming, cutting, cargo recovery and harbor progress survive save/rejoin.

- [ ] **CONTENT-06 — Finish Marsh Delta and Stormbreak.**  
  **Needs:** CONTENT-05, CONTENT-03. **Deliver:** final amphibious sites, exposed-water recovery chains, clear wave/weather cues, safe harbors, late capability challenges and finale. **Pass:** cargo remains recoverable, all required tools have been introduced, topology/memory budgets hold, and the final mission combines learned systems without an unexplained new control or exact-build requirement.

- [ ] **CONTENT-07 — Ship separate creative mode and blueprint exchange.**  
  **Needs:** UX-01, MECH-08, CONTENT-04, NET-05. **Deliver:** separate creative-world saves, unlocked parts, free spawning, reset/undo, blueprint library, preview and versioned local file import/export. **Pass:** campaign ownership/resources cannot be imported from creative; blueprints reconstruct through current validation/migration and consume resources when physically spawned in campaign. Local file sharing is mandatory. Public galleries, account inventory and moderation services are deferred unless the product contract is extended.

- [ ] **CONTENT-08 — Complete production art, narrative, and sound.**  
  **Needs:** CONTENT-06, CONTENT-07, ACT-03, SND-02, ASSET-07. **Deliver:** coherent final assets for all four regions, character animations, brief job text, world signage, completion feedback, audio mix/music, VFX and credits/provenance. **Pass:** the shipped campaign contains no unintentionally visible debug art, reference-image labels, missing clips, silent required cues or inconsistent material scale; visual/audio review checks complete actual playthroughs, not only promotional renders.

- [ ] **CONTENT-09 — Tune the whole campaign with human playthroughs.**  
  **Needs:** CONTENT-08, G11. **Deliver:** solo and co-op first-time playthrough observations, build diversity, completion time, job abandonment, recovery pain, economy and optional/post-finale play. **Pass:** representative first-time playthroughs support the 12–18 hour target and show understandable, useful design iteration; fix padding, dead time, dominant builds, unrecoverable cargo and confusing progression. Repeat the affected playthroughs after material changes; do not infer duration by multiplying mission estimates.

- [ ] **G12 — Full game content complete.**  
  **Needs:** G09, G11, CONTENT-01, CONTENT-02, CONTENT-03, CONTENT-04, CONTENT-05, CONTENT-06, CONTENT-07, CONTENT-08, CONTENT-09. **Pass:** all four regions, final campaign, creative mode, required machine families and functional catalog work as one integrated solo/four-player game. The polished cove alone cannot satisfy this gate.

## Phase 12 — release-candidate reliability and quality

- [ ] **QA-01 — Create a release test matrix and reproducible workloads.**  
  **Needs:** G00, G08, G12. **Deliver:** reproducible installable Windows/Linux and browser-demo candidate packages with immutable code/content/artifact manifests; named hardware/OS/browser/preset matrix, maximum supported scenes, fresh/aged saves, varied mass ratios, moving crews, dense contacts, ropes/cranes, fracture bursts, full cargo and region boundaries. **Pass:** QA installs the actual candidate without development tools; tests identify exact package hashes, capacity settings and expected bounded workloads; 30/60/144 Hz presentation does not change authoritative 60 Hz behavior. Packaging changes invalidate affected candidate evidence.

- [ ] **QA-02 — Meet measured performance and memory budgets.**  
  **Needs:** QA-01. **Deliver:** visible native Windows/Linux and browser-demo CPU/GPU/presentation/latency captures, steady state and cold/warm loading, worst supported topology changes, four-client host load, and memory by category including peak staging and pending GPU retirement. **Pass:** the declared targets in this file and BOOT-04 matrix pass. Report p50/p95/p99, hitches, frame misses, queue age and measurement limitations; maintain at least 20% measured WASM heap headroom at peak on the supported demo route, or revise the heap/package contract before acceptance. A software renderer, average FPS or cached static camera alone is insufficient.

- [ ] **QA-03 — Exercise persistence faults and shipped migrations.**  
  **Needs:** QA-01, MECH-08, NET-05, CONTENT-07. **Deliver:** kill/crash at journal/checkpoint boundaries, truncated/corrupt saves, quota/full disk, unavailable storage, stale/repeated receipts, older supported schemas, changed content IDs, missing assets and interrupted region transitions. **Pass:** last durable ownership and rewards recover exactly once; corrupt new data cannot destroy the last valid save; the user can identify/recover an unsaved state. Browser demo storage failures are covered even though the full campaign is native at launch.

- [ ] **QA-04 — Harden untrusted content and network boundaries.**  
  **Needs:** QA-01, NET-08, CONTENT-07. **Deliver:** bounded parser/command/property-based and fuzz tests for blueprint files, saves, protocol messages and cooked asset headers; sizes/counts, NaN/infinity, bad IDs/revisions, decompression limits and path traversal rejection. **Pass:** malformed data cannot allocate unbounded work, reference arbitrary files, bypass resource/host authority, or crash a session; accepted content is validated before simulation and GPU upload. This does not require a public mod marketplace.

- [ ] **QA-05 — Run long-session physics and co-op reliability tests.**  
  **Needs:** QA-01, QA-03, QA-04. **Deliver:** at least a four-hour four-client representative soak plus targeted overnight automated topology/save/load stress; disconnect/jitter/loss/reordering, repeated boarding, fracture, banking, repair and cross-region travel. **Pass:** no unbounded growth, invalid body handles, runaway energy, deadlocks, duplicated cargo or permanent desynchronization; document failure seeds and reduced reproductions. Automated soak does not replace human control/comfort review.

- [ ] **QA-06 — Validate lifecycle and existing-mode regressions.**  
  **Needs:** QA-01, WORLD-05, UX-02. **Deliver:** install/first launch, shader compilation, reload/reset, resize/DPI/fullscreen, controller hotplug, focus/pointer-lock loss, suspend/resume, browser tab throttling, GPU/device loss and unavailable audio tests. Preserve LEGO, smooth terrain, RIDGEBREAK and WRECKWATER smoke coverage. **Pass:** state remains coherent, queues stay bounded, returning from suspension does not simulate an unbounded catch-up burst, and errors offer a usable retry/exit/save recovery path. Record platform cases that need real hardware.

  - [ ] **Recorded Chrome/Wayland mouse-lock failure (2026-09-08):** Chrome 152 closes when the cove journey clicks into mouse-look, after successful startup/walking/Reset. The current body-layout package and the previously passing DATA-06 package fail at the same step, without game-side GPU errors. [Current/baseline reports and reproduction](docs/validation/salvage/SIM-02/stage-d1/README.md#full-application-runtime-investigation). Resolve or explicitly disposition the supported platform behavior and rerun the real journey; an X11 diagnostic pass does not close this case or satisfy performance/visual gates.

- [ ] **QA-07 — Complete accessibility and language review.**  
  **Needs:** QA-01, UX-04, CONTENT-08. **Deliver:** real player evaluation of remapping, controller-only flow, text/UI scaling, contrast, non-color cues, motion/camera options, audio alternatives, motor-demand settings and difficulty/recovery assists. Ship English and localization-ready data; add other languages only with actual translation/review ownership in BOOT-04. **Pass:** all essential actions/objectives work through supported inputs and assist settings; text expansion and pseudo-localization do not clip or break layouts; readable tutorials do not depend on spoken developer help.

- [ ] **QA-08 — Close defects and repeat human acceptance.**  
  **Needs:** QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, CONTENT-09. **Deliver:** prioritized defect ledger with reproductions, resolutions and release disposition; renewed first-session and whole-campaign human checks on the candidate build. **Pass:** no known critical crash, save loss, progression dead end or duplicate-reward defect; other accepted limitations have an accountable release decision and user-facing documentation where needed. Human gates use actual participants and the exact candidate, with retests scoped to changes.

- [ ] **G13 — Release candidate verified.**  
  **Needs:** G00, G01, G12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08. **Pass:** product scope, performance, durability, co-op, accessibility, visual/audio quality and human gates have traceable evidence. External hardware/testing gaps remain unchecked; do not replace them with confident prose.

## Phase 13 — package, release, and support

- [ ] **REL-01 — Produce versioned distributable packages.**  
  **Needs:** G13. **Deliver:** promote the exact tested QA-01 native Windows/Linux and bounded browser-demo candidate artifacts, with code/content/protocol/save versions, dependency/license notices, symbols and source-to-artifact manifest. **Pass:** package hashes match verified candidates; fresh machines complete the intended journey without development tools; native solo works offline; demo loading/admission obeys its catalog and session profile. A rebuilt/re-signed package returns to the affected candidate checks before promotion. Signing/distribution credentials must be obtained through the actual release process.

- [ ] **REL-02 — Test updates, rollback, and support diagnostics.**  
  **Needs:** REL-01. **Deliver:** supported update/migration path, pre-migration backup, rollback compatibility policy, build-identifiable crash/error reports, user-controlled diagnostic export and recovery/support runbook. **Pass:** update failure preserves the last durable save; an older binary does not silently overwrite a newer incompatible save; a reported issue can be traced to code/content/settings without logging secrets or unnecessary personal data.

- [ ] **REL-03 — Prepare release presentation and operating readiness.**  
  **Needs:** REL-01, REL-02, NET-08, CONTENT-08. **Deliver:** final name/branding, actual in-game screenshots/trailer captures, supported-platform/features description, controls/help, known issues, co-op hosting/join instructions, service ownership and incident/recovery procedures. Update README with current routes and accurate completion evidence while retaining legacy prototype instructions. **Pass:** advertised features match the candidate; generated concepts are not presented as real gameplay; another person can launch, host/join, recover a save and diagnose a failed connection from shipped instructions.

- [ ] **REL-04 — Complete release review and authorized distribution.**  
  **Needs:** REL-01, REL-02, REL-03. **Deliver:** accountable release sign-off, immutable candidate hashes, distribution configuration, tested staged rollout/rollback, post-release issue intake and owners. **Pass:** authorized native packages and browser demo are available through the selected distribution route and installation/join smoke tests pass there. Do all preparation before requesting any necessary final publishing/account approval; this plan itself does not create store accounts, authorize purchases or supply access.

- [ ] **G14 — Launch complete.**  
  **Needs:** G13, REL-01, REL-02, REL-03, REL-04. **Pass:** released artifacts match verified hashes, required launch features are accessible, support/rollback ownership is active, and the execution ledger links final evidence. Writing this plan or finishing engine infrastructure is not completion of the game.

## Bootstrap commands and verification routing

Run from the repository root. These are existing entry points checked during planning; revalidate them at BOOT-02. They are instructions, not evidence that a new build/test run passed. Use a bounded parallel build size appropriate to the machine.

```bash
# Enter the project toolchain first.
nix-shell

# Only when vendored dependencies are missing or require their pinned refresh.
./scripts/fetch_deps.sh

# Native engine and existing asset importer.
bazel build -c opt //:voxy_native //tools:gltf_vmesh_tool

# Focused starting coverage; this is not the complete suite.
bazel test //tests:config //tests:lego_surface //tests:lego_playground //tests:gltf_vmesh_tool_test

# Repository-wide suite used by the existing commit hook.
bazel test //tests:voxy_tests //tools:terrain_diffusion_import_test

# Browser build and development server; the server is long-running.
bazel build --config=wasm //:voxy_wasm
bazel run --config=wasm //tools:serve_wasm
```

Run the alternative native build separately, after checking its options in the current `CMakeLists.txt`:

```bash
cmake -S . -B build-salvage-native -DVOXY_BUILD_NATIVE=ON -DVOXY_BUILD_TESTS=ON -DVOXY_BUILD_TOOLS=ON
cmake --build build-salvage-native -j 4
ctest --test-dir build-salvage-native --output-on-failure

# Required whenever shared LEGO surface constants/formulas change.
python3 scripts/sync_lego_surface.py --check
```

WASM builds require the repository's Emscripten setup. At this baseline `.github/workflows/pages.yml` pins Emscripten 6.0.1; confirm the active pin and dependency/toolchain paths rather than assuming the shell supplies them. Inspect `.bazelrc`, `build/`, `scripts/` and CI before changing the toolchain. CMake WASM configuration must use the Emscripten toolchain and explicitly disable native targets as appropriate; do not treat a host CMake build as WASM validation.

Gameplay assets now require the explicit `salvage-rigid-v1` import profile and a digest-bound sidecar. The two-positional importer interface remains for legacy assets; do not use it as a fallback when a new gameplay asset is rejected. Build the real tools, then cook into a new output directory whose parent already exists:

```bash
bazel build -c opt //tools:gltf_vmesh_tool //tools:gameplay_sidecar_tool
python3 tools/salvage_assets/cook_gameplay_asset.py \
  --sidecar tools/salvage_assets/fixtures/probe.gameplay.json \
  --sources data/salvage/authoring_probe \
  --output /tmp/new-salvage-probe-bundle \
  --converter bazel-bin/tools/gltf_vmesh_tool \
  --validator bazel-bin/tools/gameplay_sidecar_tool

# Full actual-tool offline profile/cook coverage, separate from the main hook.
bazel test -c opt //tests:gltf_vmesh_tool_test //tools:cook_gameplay_asset_test
```

The diagnostic Blender generator now exists at `tools/salvage_assets/authoring_probe.py`; its exact CLI and reproducibility checks are in `tools/salvage_assets/README.md`. It is a reference for new asset generators, not a complete game asset library. Use `blender --background --factory-startup --python-exit-code 1 --python <generator.py> -- <asset arguments>`, a clean scene and distinct output paths; preserve source files. The metadata/cook contract is documented in `tools/salvage_assets/GAMEPLAY_SIDECAR.md`. A successful Blender process does not prove in-engine appearance or correct collision.

Verification routing:

| Changed area | Required checks beyond compilation |
|---|---|
| Shared schemas/commands | Canonical encoding, malformed/repeated requests, atomic rollback, save/protocol compatibility |
| Assembly compiler/physics | Analytic mass/inertia, all shape consumers, contacts/CCD/queries, momentum, fixed-tick water, capacity rejection |
| Gameplay/economy | Actual controls through both jobs, resource/ownership invariants, rescue, save/reload, co-op races |
| Rendering/shaders | Shared-source check, validation layer, visible native/browser images, underwater ordering, motion/history, measured full-frame cost |
| Assets/cooking | Source manifest, units/axes/materials, metadata correspondence, engine preview, collision/socket/LOD behavior |
| Streaming/lifecycle | Cancel/unload/device loss, bounded memory, collision readiness, save identity and retirement fences |
| Networking | Real rendered clients, adversarial ordering/loss, revision recovery, authority/economy, moving crew and host load |

Use focused tests while implementing, then the relevant gate's full integrated route. Test labels for new salvage modules must be added to both build systems; the existing suite cannot cover code that is never compiled or registered. Run required repository checks before committing. A documentation-only edit does not justify claiming new runtime verification.

## Repository source map

These files are starting points; inspect their current symbols before editing. This map does not require an agent to read the earlier blueprint or conversation.

| Responsibility | Existing source paths |
|---|---|
| Instructions/build entry points | `AGENTS.md`, `README.md`, `BUILD`, `CMakeLists.txt`, `.bazelrc`, `shell.nix`, `flake.nix` |
| Shared mode/lifecycle/input integration | `src/app/application.hpp`, `src/app/application.cpp`, `web/index.html`, `src/engine/platform/wasm/entry.cpp` |
| Playground state and legacy limits | `src/game/lego_playground.hpp` |
| Brick-like terrain surface and layout | `src/terrain/lego_surface.hpp`, `src/terrain/lego_layout_cache.hpp`, `shaders/lego_surface.wgsl`, `scripts/sync_lego_surface.py` |
| Physics API and backend boundary | `src/physics/physics_types.hpp`, `src/physics/physics_backend.hpp`, `src/physics/gpu/gpu_physics_backend.cpp` |
| Collision scale and primitive drawing | `shaders/physics_broad_phase.wgsl`, `shaders/physics_primitives_compact.wgsl`, `src/render/primitive_path.cpp` |
| Structural connectivity/damage | `src/gameplay/structural_assembly.hpp`, `src/gameplay/structural_assembly.cpp` |
| Water fields and composition | `src/render/water_simulation.cpp`, `shaders/water_clipmap.wgsl`, `src/render/raycast_path.cpp`, `src/render/blit_path.cpp`, `shaders/ray_blit.wgsl` |
| Mesh importer/runtime format | `tools/gltf_vmesh_tool.cpp`, `src/moto/vmesh.hpp`, `src/render/mesh_path.hpp`, `shaders/mesh_path.wgsl` |
| Existing authority and replay references | `src/server/wreckwater_authority_runtime.hpp`, `src/server/wreckwater_authority_runtime.cpp`, `src/game/wreckwater_replay.hpp` |
| Build/test/packaging integration | `tests/BUILD`, `tools/BUILD`, `.github/workflows/pages.yml`, `scripts/fetch_deps.sh` |
| Existing browser profiling | `scripts/benchmark_browser.mjs`, `scripts/benchmark_wasm_render.mjs`, `docs/performance-profiling.md` |

Search related headers/tests/callers with `rg` before extending a type. Changing a shape or body layout requires auditing every CPU/WGSL producer/consumer, buffer stride and build inclusion. Renaming the old LEGO or WRECKWATER symbols wholesale is not a prerequisite.

## Decision ledger

The entries below are the starting implementation defaults. A task may refine unspecified details within its scope. Changing an explicit contract requires updating this table, affected requirements/tests, and downstream owners. Do not let a decision live only in a chat message.

| Decision | Initial default | Owner task / allowed resolution point |
|---|---|---|
| D01 Product/platform | Solo + creative + 1–4 co-op; Windows/Linux; desktop WebGPU demo; four regions; 50–70 functional parts | BOOT-04; later scope change must name added/removed gates |
| D02 World representation | Heightfield terrain; explicit editable wrecks; clustered machines | DATA-03, SIM-01; no implied voxel excavation |
| D18 Review availability | Continue dependent preparation after implemented/validated prerequisites when reviewers are unavailable; keep all prerequisite/dependent acceptance and gate commits open | 2026-09-08: all three existing review workers authoritatively errored on account usage limits. No credit reset or new worker was requested. This scheduling allowance preserves every review and launch requirement. |
| D19 Proportional verification — 2026-09-08 | Owner reports repeated captures of the same views with minimal change. Use focused checks for small fixes; new screenshot sets require a meaningful visible milestone or a specific unresolved visual regression. Do not repeat all detail levels/platforms for changes that do not affect them. Prioritize assembling the authored boat/dock in the cove. Preserve required gate checks and independent/human acceptance. | Root; applies immediately to ongoing ASSET/LOOK work. Retain existing evidence without further capture churn. |
| D20 Gameplay focus — 2026-09-08 | Owner repeatedly rejects screenshots and work without playable progress. Stop screenshot capture/review loops. Walkable dock/boarding are implemented; prioritize physical boat movement, buoyancy and helm controls. The cove now preserves seventeen actual welds and owns `CoveBoatAssembly`; use that preparation for the live skiff. Use focused gameplay/state checks and advance after they pass. Defer further visual passes until a meaningful playable milestone or an explicit owner request. | Root; applies to all following agents. Full launch scope and final visual/human gates remain mandatory. |
| D21 LEGO identity — 2026-09-09 | Owner reaffirms this is a LEGO-style game and objects to the flat, non-LEGO terrain. The smooth cove fallback was an implementation shortcut, not approved product direction. Complete LEGO-01's brick terrain and matching contacts before expanding salvage scenarios, then prioritize PLAY-02 so players can build and modify their own machines. | Root; supersedes the smooth-terrain assumption for the playable cove. Retain D20's prohibition on repetitive captures. LEGO-01 restoration now passes its scoped gameplay checks; full physics/art gates remain open. |
| D22 Starting workshop stock — 2026-09-09 | Root-selected initial tuning under D21: a fresh host world receives 48 general material and zero machinery so the first paid additions can be built immediately. This is not a claimed owner-selected amount or a salvage reward. Reset/Launch never refill it; future SAVE recovery restores its existing recorded balance. Current Leave destroys this RAM world, with no cross-world transfer. Original starter loans remain valueless when dismantled. | Root; centralized in `coveStartingMaterials` and actual fresh-world bootstrap. The paid-pontoon browser journey verifies 48 → 24 → 0, exact undo/redo/refund, and no Reset refill. Campaign economy balance remains later tuning. |
| D23 Initial functional settings — 2026-09-09 | Root-selected first tuning controls: propeller enabled state, thrust cap and reversal; helm enabled state and steering cap; winch enabled state. Limits cycle 100/75/50/25/0 percent. Keep/Launch applies free configuration through the canonical refit; undo/redo and Reset preserve it. Engine networks, control channels, automatic rope payout and winch force/speed limits remain later work and have no misleading controls. | Root under D20/D21. Runtime registry overlay only; installed content schemas and canonical serialized formats are unchanged. SAVE must preserve settings and paid identities. |
| D24 Saved designs and expedition ownership — 2026-09-09 | A transferable blueprint contains design intent only. Loading reuses currently owned matching parts and buys missing parts through the existing refit; no saved file can revive a removed starter loan or grant inventory. The browser library provides 32 named designs with one backup each, strict IndexedDB transactions and export/import. It persists by browser profile/origin. The UI explicitly distinguishes saved designs from unfinished expedition progress saves. | Root under D20/D21. SVBP v1 and browser storage are documented in `docs/salvage-blueprint-format.md`. Full recovery must preserve world lineage, tokens, processed markers, balances and physical state; no fresh-material grant on restored worlds. Native storage/named controls remain open. |
| D25 First durable delivery — 2026-09-09 | A completed generator haul uses a whole certified SVCE checkpoint as its initial durability barrier. Secure cargo at its observed pose, complete canonical banking, join Pause, publish the exact archive through the exclusive host store, then acknowledge its SHA-256 before success/Resume. The persisted completed job/cargo state prevents another payout after recovery. Failed saves retain a frozen pending delivery for explicit retry; unacknowledged work may be absent after restart. | Root under D20/D21; PLAY-04/SAVE-04. Existing SVCE/SVSC/SVSG schemas and installed content are unchanged. Native/browser hosts share the acknowledgment contract. Browser reload and Linux ordinary automatic delivery/restart plus real permission-failure/retry/restart are verified in the linked PLAY-04 checkpoints. This does not waive separate journal/compaction, live fault coverage, native acceptance, harbor upgrades, latching or general autosave. |
| D26 First harbor capability — 2026-09-09 | Implement a fixed powered boat hoist at the existing berth, unlocked only after durable generator banking. Four force-limited suspension lines raise/lower a compatible boat for recovery and later refitting; retain actual boat mass, buoyancy, collision and gravity. This is a bounded harbor service, not the general articulated crane in MECH-02. Refuse unsupported craft or unsafe attachment with clear feedback. | Root under D20/D21; PLAY-04/SAVE-04. Preserve existing installed assets and v1 saves. SVCE v2 explicitly records installed harbor profile 1 even when detached; v1/profile 0 remains the pre-hoist world. The extension retains each hoist line's state and length; derive anchors/forces from the installed mechanism and compiled craft, never from untrusted saved handles. No active-hoist save may silently load without its constraints. Gameplay controls, visible structure, lifecycle and actual native/browser restart remain required before claiming the harbor reward complete. |
| D27 Clear the lift berth on installation — 2026-09-09 | The actual browser journey showed that securing a generator high over the berth can obstruct the returning boat. Explicit harbor installation therefore transfers the already banked generator to a bounded supported position on the outer edge of the existing pier, leaving the walking lane clear. Numerical terrain sampling showed the pier is over deep water, so support uses both unchanged LEGO terrain and actual fixed deck cells; reject unsupported, occupied or steep locations. Retire the old static cargo body, admit one replacement preserving its cargo identity and inventory, join both lifetimes and save its actual new pose with profile 1 before the upgrade unlocks. | Root under D20/D21/D26. No reward is paid again, no logical cargo is recreated, and existing v1/v2 loads never silently relocate cargo. This transfer happens only through the explicit installation action; subsequent saves retain the actual position. Native/browser restart and collision checks are required. |
| D28 Compliant lift slings — 2026-09-09 | Actual browser r10 records 49.817 kN and 46.331 kN snatch loads above the unchanged 45 kN break threshold, at about 12–15 mm excess cable length. Its exact pre-lift archive is preserved and a focused real restart reproduces the overload. Add an explicit optional axial spring compliance with critical damping to distance attachments; zero retains existing hard-rope behavior. Harbor slings use 5e-6 m/N (200 kN/m), with the existing 30 kN applied-force cap, 45 kN break threshold, actual mass/gravity/water and physical extension. | Root under D20/D21/D26; this scoped component is validated in harbor-live-r01. The internal GPU command expands coherently to 96 bytes; the live attachment record stays 128 bytes and SVCE bytes remain unchanged. Sling material derives from the current profile on restore. The GPU impact/energy comparison, existing hard-rope regressions, exact failing browser save replay, final native journey and fresh full browser journey pass. Do not raise limits, fake motion, discard failed evidence or claim a 6-ton rating. |
| D29 Explicit durable rescue — 2026-09-09 | Replace playable-cove R/Reset with an explicit recovery service: reuse the accepted craft and every fitted paid/loan part, retire constraints before relocation, return the player to the dock, reset only undelivered generator cargo to its authored recovery site, and preserve already banked cargo/harbor progress. No reward, new entitlement, paid refund or replacement identity is granted. Join actual post-relocation physics and publish the whole archive before unlocking Resume; failures remain paused for explicit retry. | Root under D20/D21; PLAY-05/SAVE-04 component verified in rescue-r01. Native/browser actual controls, repeated restart, installed/uninstalled harbor and native permission failure pass. This explicit rescue teleport does not complete a mission or replace physical hauling. Current accepted-boat recovery does not replenish deliberately removed loans or general cut fragments; those remain mandatory before full PLAY-05. No general free-berth/boat stability or save-fault certification. |
| D30 Starter rebuild service — 2026-09-09 | Add an explicit dock/workshop starter-rebuild action separate from Rescue/R. Rebuild the trusted starter recipe using the existing world entitlement, atomically retire all earlier active loan instances, and retain fitted paid parts as bounded owned storage with exact durable IDs, condition and configuration. Give no material refund and no sale value to loans. Stored paid parts can be reused without another purchase; repeated recovery must not create another stock copy. Preserve design data and the existing mission/cargo state. | Root under D20/D21/D29; component verified 2026-09-10 in starter-recovery-r01. Canonical authority, archive/replay, physical admission, paid-stock reuse and actual native/browser reload pass; full PLAY-05 loss/cut/blueprint matrix remains required. Do not substitute undo-only replenishment, re-bootstrap the session, silently restore retired IDs or mark a pure planner as the full player service. |
| D31 Protected rebuild designs — 2026-09-10 | Before starter rebuild, protect the last accepted custom boat as a normalized design-only blueprint inside the whole expedition archive. Keep four distinct designs of at most 32 KiB each; skip the installed original starter, deduplicate repeats and refuse a fifth new design before replacing the boat. Never silently evict a backup. Load/cycle/export through the workshop; rebuilding from a design uses owned parts and normal costs. Explicit removal requires the editor to match the accepted boat and saves before Resume. | Root under D20/D21/D30; scoped component verified in recovery-design-r01. SVCE v3 adds bounded canonical SVBP designs after the harbor extension; v1/v2 remain byte-identical without backups. No identities, health, money, loans or free parts are carried by a design. Actual native/browser recovery, persistence and removal pass; full PLAY-05 loss/fragment matrix stays open. |
| D32 Cut/fragment prerequisites — 2026-09-10 | PLAY-05 requires cutting-loss recovery while the general mechanics phase depends on G06, which includes PLAY-05. Implement the cut/fragment preparation and its required physical/ownership/save integration early as preparatory prerequisite work, without passing G06/G07 or waiving their criteria. Retain one canonical owning build across its rigid roots; cuts disable exact weld IDs and preserve all paid/loan part identities. Compile every resulting root and inherit its parent post-solve velocity field; never apply the breaking impulse again or discard a functional fragment because capacity is full. | Root under D20/D21; MECH-05/PLAY-05 prerequisites. Immutable cut/motion preparation passes in cut-preparation-r01; authorized cut transactions, selective history retirement, logical SVSC/SVJB v3 and exact recovery now pass in cut-authority-r01. Bounded live cove cutter, multi-body publication, root archives and physical recovery now pass in live-cut-r01. Full allocation/fault coverage, general damage/joints and remaining loss/exploit/gate criteria still apply. |
| D33 Cove wave clock correction — 2026-09-10 | The loaded browser loop advanced water about six times faster than 60 Hz physics. Bind the shared FFT and analytical swells to the exact submitted cove tick/phase. Both native/browser cove profiles use one tick per submission until general SIM-05 provides per-tick catch-up fields; keep the existing two-tick flight bound. Pause/restore/service neutral ticks preserve the archived wave seconds; derive elapsed time from relative integer ticks, not absolute f64 ticks or displayed-frame time. | Root under D20/D32; scoped clock/phase verification passes: native haul and final 29-stage browser delivery/reload in SIM-05/cove-clock-r01. No schema/content geometry change; v1-v3 semantic resume preserves saved wave phase. Do not claim full WaterField, GPU water velocity, CPU approximation bounds, player-clock joining or performance acceptance. |
| D03 Units/coordinates | Contract A; isotropic .02 m integer placements; 1 stud = 1 m, .32 m plate pitch, 24 proper rotations | DATA-01; migration required for a later change |
| D04 Mechanics ownership | GPU default; one authoritative backend per interacting island/world; explicit measured CPU alternative | MECH-03, before general fracture/network production |
| D05 Water authority | Versioned fixed-tick gameplay field consistent with rendering; CPU query approximation is not presumed exact | SIM-05; record sampling/error policy and acceptance fixtures |
| D06 Persistence | Host-owned world; certified checkpoints + durable receipts/journal; semantic replay | SAVE-01, MECH-08, NET-05 |
| D07 Cargo latching | Independent while towed; incorporated once into rigid cluster while latched | SIM-08; update compiler/save/network contract together if changed |
| D08 Building/recovery | Workshop major edits; limited field tools; protected starter/blueprints/unique progression cargo | PLAY-02, PLAY-05, UX-01 |
| D09 Art pipeline | Reproducible Blender source/scripts + GLB/metadata + cooker; MCP optional; image generation assists authored materials | ASSET-01 through ASSET-07 |
| D10 Transport/hosting | Host authority with real encrypted native/browser adapter; no lockstep or host migration | NET-02; record selected protocols, compatibility, topology and provisioning |
| D11 Memory/terrain | Per-mode packages, bounded queues; measure regional resident terrain before choosing chunk streaming | WORLD-03 through WORLD-07 |
| D12 Launch quality | Stated hardware/frame/capacity/human gates; English plus localization-ready data | BOOT-04, QA-01; threshold changes require rationale and new measurements |
| D13 Schedule/resources | Preliminary milestone ranges only; production forecast based on measured throughput and real staff | CONTENT-01; no fabricated release date or funding |
| D14 Region travel | One active region; separate local jobs; whole-party checkpointed harbor transitions | WORLD-04, NET-07 |
| D15 Demo compatibility | Isolated two-job/14-definition demo; native host with native/browser guests; full campaign rejects demo-only peers | BOOT-04, WORLD-02, NET-02, QA-01 |
| D16 Early visible quality — 2026-09-07 | Owner reports the BOOT-05 cove looks substantially worse. Its primitive fixture is not an accepted visual target. Add LOOK-01 to G02 and prioritize it during asset work, before expanding content. Keep original route appearance as a regression reference. | Root; evidence: `docs/validation/salvage/BOOT-05/native-final-salvage/`, optional concept and inline art direction. No data migration or removal of final quality gates. Independent LOOK-01 review remains required. |
| D17 Browser failure semantics — 2026-09-07 | Compile and link JS-based C++ exceptions (`-fexceptions`) with existing Asyncify, and use `-sABORTING_MALLOC=0` on the fixed heap so allocation failure can reach transaction cleanup. Native WASM EH plus Asyncify emitted an SDK incompatibility warning and is not the selected configuration. | DATA-04 / root and simulation_production; [policy and evidence routing](docs/validation/salvage/DATA-04/integration/browser-failure-policy.md). No data migration. Later frame-time gates must measure this configuration; no inherited performance or arbitrary host-failure recovery claim. |

Add revisions here with date, decision ID, reason, evidence, affected tasks, migration consequences and responsible reviewer. “Easier to implement” alone is not evidence that a removed game requirement is unnecessary.

## Execution ledger — keep this current

Goal started on 2026-09-07: implement all tasks through G14, mark verified items complete, and commit after every successfully verified gate. No gate is complete at goal creation. The existing engine and previously generated concept image are reference foundations only.

Copy one row per claimed task. Keep handoffs factual and self-contained. Reviewers must be able to find source changes and evidence without a conversation link.

| Task ID | State / owner | Branch or revision; owned paths | Deliverable and evidence location | Review / blockers / next action |
|---|---|---|---|---|
| Owner-requested main checkpoint | Verified for publication / root | Current source, required authored assets, implementation plan and compact validation evidence | `docs/validation/salvage/checkpoints/2026-09-10-main.md` | Owner explicitly requested pushing to main on 2026-09-10 and reiterated that the presentation is unfinished. Remote main was `7563f61` at fetch. Required suite passes after increasing its measured-insufficient 900-second timeout: 2,038 native cases pass, three skip, four remain disabled; terrain import 10 pass/1 skip. Browser checks and both brick-checker build targets pass. This is a work-in-progress publication, not completion of a new game gate. LEGO-02 remains the next playable milestone. |
| LEGO-02 individual bricks | In progress / root | Brick asset recipe/content, Cove capacity and workshop pointer controls; preserve canonical refit and save ownership | `docs/validation/salvage/LEGO-02/` | 2026-09-10: first implement studded 1×2, 2×2, 2×4 assets and verify stacked sockets/clearance using the real compiler; then integrate catalogue, useful capacity and direct controls. Scratch `build-cove-bricks-1kpmad89`. Original 1×2/2×2/2×4 meshes and nine LODs are cooked in `data/salvage/bricks/r01`; the actual compiler accepts a three-layer stack in all 24 rotations and refuses misalignment/overlap. See `docs/validation/salvage/LEGO-02/brick-assets-r01/`. Cove palette admission, capacity and direct controls remain pending; no task/gate claimed. |
| MECH-05 live cutter | Verified bounded integration / root under D32 | Actual C/cutter control, intact-design protection, multi-root publication, rider/helm/winch mapping, SVCE v4 reload and recovery/rebuild | `docs/validation/salvage/MECH-05/live-cut-r01/` | Native r03: 15 stages including active-cable refusal; browser r01: 14 plus drained Leave. Roots 7/15 and exact paid stock survive; 54 cove + 21 save Bazel cases, 46 CPU + 8 GPU CMake cases and all builds pass. Retired-parent input bug fixed; failed r01 retained. Scratch `build-cove-live-cut-ubvsrtrj`; playable frozen package on local port 36787. Original port 38206 remains unchanged. No full task/gate or gate commit. |
| MECH-05 Rescue boarding | Verified scoped fix / root under D32 | Complete-spectrum occasional placement query; corrected fast-sampler origin; Spawn/Launch/Rescue integration and water GPU regression | `docs/validation/salvage/MECH-05/boarding-r01/` | The old estimate was 75 cm below actual GPU water. Five water tests pass, including 40 GPU comparisons within 5 mm across two spectra, boundaries and saved phases. Native unchanged failed journey now passes eight stages; browser r02 passes six stages plus drained Leave. All 13 parts, paid IDs 35/100 and unique cargo survive. R01 browser gameplay passed but its old-module poll raced normal Leave navigation; the driver correction is verified by r02. Both native builds and WASM pass. Scratch `build-cove-boarding-esi6q6gq`, frozen native-boarding-r01/web-r01; test profile `/tmp/voxys-startup-jLGqhu`, port 41289 now has the tick-164 rescued checkpoint. Next last-intact protection, child resource reservation/rollback and actual live cutting. No screenshot, full gate or commit. |
| MECH-05 complete-section rescue | Scoped atomic move verified; boarding/cuts pending / root under D32 | `CoveRigidRoots` recovery preparation; Application joined Rescue command reservation/publication; actual section/ownership and recovery checks | `docs/validation/salvage/MECH-05/root-recovery-r01/` | 42 CPU cases across two selections and the actual two-section GPU case pass; both native builds and WASM pass. Native delivered Rescue/two restarts/sailing passes 7 stages. Browser actual attached-cargo Rescue/two saves/reloads plus Leave passes 6 checkpoint stages, preserving paid IDs 35/100, zero material and the unique generator. Current and previous binaries both fail the same wider-boat boarding journey after successful Rescue/restart; initial recovery positions match. The water-placement cause is now fixed and verified in boarding-r01 without weakening boarding reach. Next last-intact design protection, complete child reservation/rollback and native/browser live cut/reload/distant recovery. Scratch `build-cove-root-recovery-xgzzff3s`; frozen native-recovery-r01/web-r01. Test profile `/tmp/voxys-startup-jLGqhu`, port 41289, now contains a rescued checkpoint rather than attached cargo. No screenshots, full gate or commit. |
| MECH-05 live fragment resume | Scoped v4 host/CPU roots verified; full cuts pending / root under D32 | Player root/deck/helm binding, root-aware cove scene/restore preparation, Application all-root admission/capture/tow and v4 save hosts | `docs/validation/salvage/MECH-05/root-resume-r01/` | 39 CPU and 3 CMake restore cases pass; three sections retain every part/motion and correct rider/winch frames without GPU publication. Both native builds and WASM pass. Native v4 purchases/two restarts pass 14 stages; historical v1 load/F10 upgrade/v4 restart passes 3. Browser r01 completes two paid v4 reloads; r05 passes the nine-stage resumed tow/save/reload/reel/release/sail journey on the same frozen application. R01 waypoint failure, r02 stale browser endpoint, r03 missed Pause and r04 proof that scrolling moved the click onto the panel are retained. The driver now waits for a stable target and records trusted input delivery. Next actual multi-root runtime admission/reload and capacity rollback, atomic cut publication and all-root Rescue through a reserved joined command batch. Cutting disabled; no full task/gate or commit. Scratch `build-cove-root-resume-_csjbh9y`; retained browser `/tmp/voxys-startup-jLGqhu`, origin port 41289. |
| MECH-05 live root ownership | Scoped owner/Launch and motion preparation verified; full cuts pending / root under D32 | `CoveRigidRoots`; checked joined-boundary transactions; Application ownership/render/observation/Leave/Launch; inherited motion and checked world translation | `docs/validation/salvage/MECH-05/live-roots-r01/` | Native gameplay passes 37 stages and frozen browser web-r01 passes 29, including paid ownership, delivery, reload and sailing. Final 11-case combined GPU/owner/motion selection and 11 mass-frame cases pass; final Bazel/CMake native and WASM application builds pass. Subsequent motion helper is native-executed and browser-compiled, not a browser fragment claim. Next full capacity reservation/rollback and all-root startup, atomic cut/render publication, player/tow/harbor root binding, v4 physical save/restore and distant recovery. Cutting stays disabled; no full task/gate or gate commit. Scratch `build-cove-live-roots-qxbqt1ib`; retained browser `/tmp/voxys-startup-powYJQ`. |
| MECH-05 atomic parent retirement | Native primitive verified / root under D32 | `PhysicsMutationBatch::bodyDestroys`, preallocated linear-time membership/retirement, GPU refusal/transfer/reuse cases and actual authored split | `docs/validation/salvage/MECH-05/parent-retirement-r01/` | Eight native transaction cases and the actual authored split pass; both native build paths and WASM compile. Combined test runfiles corrected, original setup failure retained. Keep explicit-fixed-tick policy: the cove accumulator needs checked drained-pause admission. Next own/reserve all live roots, activate after joined execution, map every consumer, wire v4 physical saves and recover remote fragments before exposing cutting. No full task/gate or browser fragment-execution claim. |
| SIM-05 cove wave timing | Scoped correction verified / root under D33 | `cove_water_clock.hpp`, application frame/save clock, checked physics water-phase binding and focused native/browser checks | `docs/validation/salvage/SIM-05/cove-clock-r01/` | 69 Bazel / 193 WASM cases and both native/WASM builds pass. Native haul/restart passes 25 stages before final arithmetic refinement; final browser r04 passes 29 actual-control stages through paid construction, loaded return, hoist, durable reward, phase-preserving reload, no second payout and sailing at 2.42526 m/s. R01 GPU loss, r02 obsolete overreeling and r03 unavailable headless adapter remain documented. The corrected driver uses the native tow-then-hoist strategy. Continue live roots/cuts; full SIM-05, player timing, general catch-up and displayed-frame performance remain open. No screenshots or gate commit. |
| MECH-05 physical root archives | Portable codec verified; live owner integration pending / root under D32 | SVCE v4 exact root keys/motions, stable helm/rider bindings, unchanged legacy encoding and bounded decoding | [Evidence and next integration](docs/validation/salvage/MECH-05/root-archives-r01/README.md) | 224 Bazel / 21 CMake / 187 WASM cases and all application builds pass; two complete-envelope v4 golden hashes match. Startup still admits v1-v3; complete live root ownership, capture/restore, player/rope/render mapping and rescue are mandatory before v4 gameplay saves/cutting. The scoped wave-clock correction and corrected browser tow/hoist/reload journey now pass in SIM-05/cove-clock-r01. Continue bounded live roots and atomic publication. No full task or gate acceptance. |
| MECH-05 cove rigid roots | In progress / root under D32 | CoveBoatAssembly per-root collision/water preparation and stable control-part selection; runtime water consumers and actual cove/GPU checks | [Evidence and exact next integration](docs/validation/salvage/MECH-05/cove-roots-r01/README.md) | Prepared roots and three focused tests pass, including actual GPU replacement with inherited motion and settling. Browser seabed failure is corrected and caught by a new old-fails/new-passes full-pipeline regression; six browser and 88 native cases pass. Corrected native fresh delivery/save/restart journey passes 34 stages. Browser r06 reached actual hooking/lifting but the cable broke; water time advanced about six times faster than physical time. The scoped shared-phase correction and corrected 29-stage browser delivery/reload now pass in SIM-05/cove-clock-r01; full WaterField epochs and performance remain open. Browser controls count physics ticks and tow home before hoisting, with actual delivery/save assertions retained. Next atomic live cut publication, per-root archives, player/rope/render remapping and rescue. Cutting stays disabled. No screenshots or gate passed. |
| MECH-05 cut authority | Scoped component verified / root under D32 | `BuildModel` shared cut mutation; session command/transition/history/journal/recovery/codec; preparation contract; CMake test dependencies and focused tests | [Cut authority evidence and next integration](docs/validation/salvage/MECH-05/cut-authority-r01/README.md) | Progress: exact owned-weld cut preserves parts/money/IDs, selectively retires Undo, reserves the full journal payload and waits for execution. Logical v3 recovery handles pending/committed cuts and repeated starter rebuild without paid duplication; old save bytes remain exact. 250 Bazel, 156 CMake, 179 authority/save WASM and 88 compiler/fracture WASM cases plus all application builds pass. Next generalize CoveBoatAssembly/Application to every root, reserve all physical capacity, map player/ropes/rendering, archive every root motion and add actual cutter/rescue journeys. Current adapter rejects cuts; last playable preview stays port 38206. No screenshot, full gate or gate commit. |
| MECH-05 cut preparation | Preparation verified; live integration pending / root under D32 | `construction/assembly_fracture.*`, Bazel/CMake and shared WASM validation, conservation/refusal and actual cove content tests | [Cut preparation evidence and next integration](docs/validation/salvage/MECH-05/cut-preparation-r01/README.md) | Exact enabled-weld cuts preserve every owned part; full compiler produces all fragments, root/endpoint mappings and checked post-solve inherited motion. 256 parts / 64 roots, asymmetric conservation, malformed/stale/capacity refusal and actual paid-pontoon shape preparation pass. 126 Bazel, nine CMake and 88 actual WASM cases; both native builds/WASM pass. Next authorized cut journal/replay and multi-root live publication/save/rescue. No live cutter or full task/gate acceptance. |
| PLAY-05 retained recovery designs | Scoped component verified / root | `codex/salvage-implementation`; normalized design backup, SVCE v3, atomic pre-rebuild capture, workshop load/cycle/removal and actual save/reload | [Recovery design evidence](docs/validation/salvage/PLAY-05/recovery-design-r01/README.md) | Four distinct accepted custom designs remain protected; full capacity refuses new backup before replacement. Original starter/recovered designs deduplicate across fresh ownership IDs. Native passes 12 real-control stages with permission failure/retry/restarts; legacy/fresh browser pass 10/28 stages plus Leave. Layout/configuration, exact paid identity, money and unique cargo survive. Backups and explicit removal persist. 178 Bazel cases, three CMake CPU cases, Node UI/save checks and all application builds pass. Full PLAY-05 loss/fragment matrix and gates remain open. |
| PLAY-05 starter rebuild and paid-part storage | Component verified / root under D30 | `codex/salvage-implementation`; canonical kit/stock/transaction/codec/replay, cove launch/restore, workshop and save hosts | `docs/validation/salvage/PLAY-05/starter-recovery-r01/` | Progress: final native r03 passes 9 stages with real disk failure/retry, two rebuilds, paid reuse and three restarts; final fresh browser r03 passes 28 stages through construction/haul/delivery/rebuild/reuse and three reloads plus Leave. Exact paid IDs/condition/configuration and materials are conserved; existing saved designs remain. 128 authority, 36 movement/mapping, 9 archive, 17 save-coordinator and preview UI cases pass; both native builds/WASM pass. Adversarial previous-grant binding rejection was added and reverified in final native/browser journeys. Continue remaining loss/exploit/fragment/blueprint recovery before claiming full PLAY-05; no full gate or commit. |
| PLAY-05 protected rescue | Durable accepted-boat return component verified / root under D29 | `codex/salvage-implementation`; Application recovery/pause ownership, native/browser save hosts and controls, actual recovery checks | `docs/validation/salvage/PLAY-05/rescue-r01/` | Progress: native r05 unbanked/attached and r04 suspended/disk-failure/retry paths each pass 8 stages; focused browser r01 passes 6 and fresh r02 passes 25, both plus Leave. Paid IDs/materials, unique cargo and harbor progress survive repeated save/restart. 155 native regressions and 16 save/20 preview UI cases pass; both native builds and WASM pass. Final preview adds only the unit-tested Leave-button lock to the gameplay-tested browser package. Next finish starter replenishment/paid storage and exploit coverage before PLAY-06; parent/full gates stay open. No screenshots or gate commit. |
| PLAY-04 harbor live integration / SAVE-04 | Component verified / root under D26/D27/D28 | Shared Application lifecycle, harbor runtime/rig/material, fixed renderer/player collision, archive/restore, native/browser save/UI, shared rope solver and checks | `docs/validation/salvage/PLAY-04/harbor-live-r01/` | Native r12 passes 12 stages; browser r13 passes 30 fresh-game stages plus Leave. Both install/relocate/lift/save/restart/lower/release/sail with unchanged paid identity/material/cargo. 63 affected native + 20 render, impact/energy and 13 save/19 preview UI cases pass; both native builds and WASM pass. Exact browser overload baseline is preserved and compliant replay passes with unchanged force limits. Next PLAY-05 rescue, then second job; full parent tasks/gates stay open. No screenshots or gate commit. |
| PLAY-04 harbor lift / SAVE-04 | Physical/archive preparation verified; live integration next / root under D26 | `cove_harbor_lift.*`, `cove_harbor_state.hpp`, cove archive/restore boundary, focused tests, both build registrations and WASM evidence runner | [Physical lift, exact formats and self-contained continuation](docs/validation/salvage/PLAY-04/harbor-lift-r01/README.md) | Progress: actual GPU lift/hold/controlled lowering/release succeeds without changing boat mass or force limits; 152 WASM state cases and native checks pass; both native and browser applications build. Faster lowering remains a documented solver/rig limit. Existing v1 worlds retain exact bytes and geometry semantics; profile-1 installation/physical restore explicitly remains unavailable. Next integrate visible powered machinery, dock controls, durability and all four ropes through real native/browser restart. No parent/gate acceptance or new gate commit. |
| PLAY-04 / SAVE-04 native delivery | Linux delivery/retry/restart component complete / root | Native host's opt-in bounded observation, physical-key X11 driver and actual disk/restart evidence | [Native delivery evidence and continuation](docs/validation/salvage/PLAY-04/native-delivery-r01/README.md) | This turn is progress: both native builds and three observer refusals pass. Ordinary automatic save/restart passes 34 stages; actual unwritable-root error/frozen pending state/F10 retry/restart passes 40. Both preserve the paid craft, static generator and 96 material, refuse a second payout and sail away. No screenshots. Next implement the durable harbor lift/unlock; retain broader faults, Windows, water/stability, prerequisite review and full gates. No full-gate commit. |
| PLAY-04 / SAVE-04 | Browser durable-delivery component complete; native validation recorded separately; harbor upgrade open / root | Application delivery/pause acknowledgment, native/browser save hosts, player UI and actual delivery/restart checks | [Delivery evidence and next-work contract](docs/validation/salvage/PLAY-04/delivery-r01/README.md) | Concrete progress: actual paid lifting rig, hook/lift/steered return/delivery/save/reload/repeated-H/sailing passes in 22 browser records; reward stays 96 and cargo stays static. Both native builds and WASM build, 264 native regressions, 12 coordinator and 16 UI cases pass. No screenshots. Next native automatic delivery/restart and durable harbor lift/unlock, then rescue/second job and remaining save faults. No parent/full gate or gate commit claimed. |
| LEGO-01 | Complete / root under D21 | App, authored terrain contacts/generated consumer, cove composition and focused tests | [Brick restoration, actual cargo/skiff grounding, walking/jumping and intact browser towing](docs/validation/salvage/LEGO-01/terrain-r01/README.md) | No screenshots. Parent SIM-03/LOOK-01 and game gates remain open. Prior failed outboard rig is opt-in only. Next PLAY-02. |
| SAVE-02/04 | Linux native Save/restart component complete / root | Serialized bounded worker, native Pause/F10 status, explicit world/root startup, paired durable lineage activation and nonblocking GPU callbacks | [Native host evidence and next-work contract](docs/validation/salvage/SAVE-04/native-host-r01/README.md) | This turn made concrete implementation progress: both native builds and WASM build, 335 affected native checks, 16 ext4 and 16 CMake storage checks, and the actual 14-record native construction/save/two-restart journey pass. Paid boat and inventory survive both loads; further buying, competing-process refusal and missing-world refusal work. Temporary key diagnostics removed; no screenshots. Next actual durable cargo delivery and restart/reward fault handling, then remaining storage UX/platform scope. Parent tasks/G04 remain open; no gate commit. |
| SAVE-04 | Browser Save/reload component complete / root | Owning restore, fresh GPU tick, selected-world startup, paired durable lineage, manual save UI and real physical reload | [Evidence and next work](docs/validation/salvage/SAVE-04/resume-r01/README.md) | Updated local playable package: `/tmp/voxys-cove-save-playable`, `http://127.0.0.1:38201/?experience=salvage-cove&telemetry=0` (keep this origin for browser saves). Both builds, 311 native, 8 save/UI and 16 preview checks pass. Three real page reloads preserve paid parts, inventory, accepted job and live cable; later purchase, reel/release and sailing work. No screenshots. Next native host storage, world selection/export/import, durable banking and live failure recovery. Full SAVE/G04 unfinished; no gate commit. |
| SAVE-04 | Cove archive and live capture/preflight component complete / root | `cove_save.*`, installed-content fingerprint, paused application capture and saved-boat/player reconstruction, native/WASM entry and focused journeys | [Format](docs/salvage-cove-save-format.md), [evidence and next implementation](docs/validation/salvage/SAVE-04/archive-r01/README.md) | 263 native / 150 strict WASM cases and both builds pass. Actual towing capture and paid 13-part boat reconstruction pass; the actual paid archive survives mirrored browser publication/database-owner reopen using the game validator. No screenshots. Next real world selection, storage coordinator/UI, restored GPU tick/motion/rope/water, fresh-owner lineage publication and actual process/page reload. This turn made implementation progress; no full gate or commit. |
| SAVE-04 | Local Pause/Resume boundary complete / root | Owned scheduler pause, neutral final tick, observation/session join, cove water/input freeze and browser/native controls | [Evidence and archive continuation](docs/validation/salvage/SAVE-04/pause-r01/README.md) | Both builds, 52 native and 16 UI cases, 17 real browser records plus Leave from pause pass. No screenshots. Full archive, durable acknowledgment, actual save/load/restore, WaterField and fault acceptance remain open. No full gate or commit. |
| SAVE-04 | Body/rope observation and player-resume component complete / root | Combined bounded GPU packet, exact rope state, cove cable-length UI, validated player state | [Evidence and continuation](docs/validation/salvage/SAVE-04/capture-r01/README.md) | 51 native GPU/cove and 14 UI cases, both builds, one real browser journey (13 records + drained Leave) pass. No screenshots. Full pause/drain, session/player/water tick joining, physical archive, storage acknowledgment and actual save/load remain open. Parent/gate unchecked; no commit. |
| SAVE-03 | Browser world-generation component complete / root | `web/expedition_store.js`, frozen native/browser fixture, private-browser tests and module packaging | [19 storage cases, 5 actual tab/crash stages and complete continuation](docs/validation/salvage/SAVE-03/browser-r01/README.md) | This goal turn made concrete progress: strict two-copy publication, exclusive ownership, input/queue bounds, corruption/metadata checks, close/drain and cross-runtime bytes pass. Actual reloads, tab closure and pre-/post-commit browser kills preserve complete generations without false acknowledgments. Next domain/physical session archive, stable world selection, game save coordinator/UI, journal durability, physical restore and Windows. Full SAVE-03 and save gates stay open; no gate commit. |
| SAVE-02 | Linux file-generation component complete / root | `save_generation.*`, `platform/native/save_store.*`, native/ext4 tests and both build systems | [Mirrored publication, 16 process kills, actual paid-part reload and next integration](docs/validation/salvage/SAVE-02/native-r01/README.md) | Previous verified component: actual native file persistence, latest-copy recovery, exclusive ownership, error/uncertainty handling and the portable outer wrapper pass. 211 native, 12 CMake and 143 WASM checks pass. CMake authority manifest mismatch fixed and target built. Browser world generations now pass the subsequent SAVE-03 component above. Next domain/physical archive and game save worker, Windows and physical restore. SAVE-02 and gates remain open; no gate commit. |
| SAVE-01 | Portable logical checkpoint/journal component complete / root | `session_save.*`, shared recovery fixtures, build integration and strict WASM runner | [Format, 199 native/140 WASM/140 UBSan results and next storage boundary](docs/validation/salvage/SAVE-01/session-r01/README.md) | Previous verified checkpoint: full logical save bytes, corruption/identity/count rejection, native/WASM frozen fixtures and encoded purchase/banking replay pass. RAM-model compaction/reload rejects old requests and repeated rewards. No screenshots. Linux file generations now pass the subsequent SAVE-02 component above. Next browser world storage, domain/physical archive, durable acknowledgment/compaction and certified physical restore. Parent SAVE-01 and all SAVE gates remain open; no gate commit. |
| PLAY-02 / SAVE-01 / SAVE-03 | Browser named designs and blueprint codec checkpoint complete / root under D21/D24 | Canonical SVBP bytes; ownership-safe draft loading; strict IndexedDB current/backup transactions; name/copy/rename/export/import controls | [Persistent designs, actual reload/purchase/sailing, format and verification](docs/validation/salvage/PLAY-02/designs-r01/README.md) | Previous verified gameplay checkpoint: named designs survive reload; copies/backups/file transfers and corrupt-import refusal pass; loading buys the extra pontoon then sails the actual configured craft. Both builds, 192 native checks, 14 UI cases, 8 isolated IndexedDB checks and 9 gameplay stages pass. No screenshots. Session/checkpoint/journal bytes now pass the subsequent SAVE-01 component above. Next native/browser expedition storage, certified physical recovery and remaining native/input/controller/camera scope. Parent tasks and gates remain open. |
| BOOT-01 | Complete / lego_gameplay; reviewed by root | Baseline `7563f61`; `docs/validation/salvage/BOOT-01/` | [Source inventory](docs/validation/salvage/BOOT-01/report.md) | Root independently checked HEAD/status, instructions, routes, build declarations, toolchain caches and hook state; no runtime claim |
| BOOT-02 | Complete / root; render_architecture packaging; lego_gameplay staging | `codex/salvage-implementation`; shared native/WASM builds and package fixes | [Build integration](docs/validation/salvage/BOOT-02/report.md) | Fresh Bazel/CMake native and WASM pass; 102 focused Bazel cases pass, 77 latest CMake CPU cases pass; source-matching assets and six negative package controls; failures retained; full hook suite remains a gate check |
| BOOT-03 | Complete / root; independent evidence review lego_gameplay | `scripts/` capture helpers and task evidence; source instrumentation only after review | [Measured baseline](docs/validation/salvage/BOOT-03/report.md) | Visible route and separate native/browser memory captured; presentation target unmet; sparse GPU tails, partial memory, unavailable staging/retirement and historical provenance limits explicit; later QA gates remain unchecked |
| BOOT-05 | Complete / render_architecture; root integration and source/evidence review | `salvage.cfg`, shared app/config/entrypoints, `src/game/expedition/`, preview UI and tests; final CMake native `c4dc00f1…`, WASM `d4b6575d…` | [Implementation and runtime evidence](docs/validation/salvage/BOOT-05/implementation.md) | Real browser Reset/R/Leave/re-entry, empty-world GPU retirement, final native cove and legacy launches pass. Two product reset bugs and one harness failure preserved/fixed. WRECKWATER graphical launch limitations explicit. Technical placeholder only; owner rejects current visual quality; LOOK-01 remains required |
| G00 | Complete and committed / root; independent final review lego_gameplay | `7f28fabfabf3d63726f6cfa1001c3ce1ed557911` — `G00: establish expedition baseline and verified preview lifecycle` | [Handoff and checks](docs/validation/salvage/G00/report.md), [accepted review](docs/validation/salvage/G00/review.md), [checkpoint result](docs/validation/salvage/G00/checkpoint/summary.json) | All BOOT tasks and complete checks passed. The enabled pre-commit hook independently rebuilt/reran both targets and passed; 404 reviewed files committed. Unfinished DATA-03/ASSET-02/LOOK-01 excluded. This hash was recorded after the commit and will travel with the next plan update. Visual quality remains unapproved |
| BOOT-04 | Complete / root; reviewed by lego_gameplay | `docs/validation/salvage/BOOT-04/` | [Scope/ownership/hardware matrix](docs/validation/salvage/BOOT-04/report.md) | Independent review reproduced host/Chrome identity and confirmed all required/unprovisioned scope; no runtime or funded-staffing claim |
| DATA-01 | Complete / simulation_production; root integration; independent review lego_gameplay | `src/game/construction/construction_types.*`, `tests/test_construction_types.cpp`, `web/salvage_data.mjs`, `scripts/test_salvage_data.mjs`; Bazel/CMake registered | [Validated foundation](docs/validation/salvage/DATA-01/README.md) | 13 sanitizer tests, 13 integrated CTest cases, Bazel construction target, 6 Node cases pass; independent review found no defects. Later build/save/authority contracts remain unchecked |
| DATA-02 | Complete / simulation_production; independent review lego_gameplay; root integration/final fix review | `src/game/construction/part_catalog.*`, `tests/test_part_catalog.cpp`, task evidence; root owns shared build files | [Catalog evidence](docs/validation/salvage/DATA-02/README.md) | 16 optimized Bazel, 16 refreshed sanitizer and 16 final CMake cases pass; immutable owned catalog, bounded authoring validation and exact asset resolution; optimized compiler failures preserved and fixed without suppression |
| ASSET-01 | Complete / root; reviewed by lego_gameplay and render_architecture | `tools/salvage_assets/`, `data/salvage/authoring_probe/`, task evidence | [Authoring evidence](docs/validation/salvage/ASSET-01/report.md) | Clean Blender runs produce identical GLB and preview pixels; tall variant fits; overwrite rejected with unchanged outputs; MCP absent and optional; no runtime-art claim |

| DATA-03 | Complete / lego_gameplay; simulation_production independent review/golden fixture; root integration | `src/game/construction/build_model.*`, `tests/test_build_model.cpp`, task evidence and both shared build systems; outside G00 | [Integrated acceptance](docs/validation/salvage/DATA-03/integration/report.md) | All 23 cases pass in optimized Bazel, CMake/CTest and strict sanitizer builds; native and WASM libraries compile. Independent 509-byte wire fixture matches. Canonical builds only, no physical inventory authority claim. Awaiting next verified gate commit |
| ASSET-02 | Complete / lego_gameplay; render_architecture independent implementation review; root integration and lego_gameplay build review | Metadata schema, parser, cooker, fixtures and normal Bazel/CMake targets; original probe unchanged | [Integrated acceptance](docs/validation/salvage/ASSET-02/integration/report.md) | 9 parser + 20 actual-converter cases pass through each build system; all 9 parser cases also pass in each combined suite. Two integrated cooks byte-identical. Linux authoring validation only; strict glTF profile/runtime appearance remain separate. Awaiting next verified gate commit |
| DATA-04 | Complete / simulation_production; lego_gameplay independent core review; root app/build integration | `src/game/expedition/game_session.*`, focused/combined tests, app lifecycle and browser exception configuration | [Integrated acceptance](docs/validation/salvage/DATA-04/integration/report.md) | 21 core cases pass in optimized Bazel, CMake, strict native and actual JS-EH WASM; both combined targets pass 29 session/preview cases. Real native GPU retirement, two native launches and browser lifecycle/legacy routes pass. Bootstrap owner-role bug fixed; build and graphics-environment failures retained. Memory-only authority, fake preparation; visuals unapproved. Awaiting next verified gate commit |
| ASSET-03 | Complete / render_architecture; root converter review/integration; simulation_production independent wrapper review | `tools/gltf_vmesh_tool.*`, converter tests, strict wrapper/profile manifest and task evidence | [Integrated acceptance](docs/validation/salvage/ASSET-03/wrapper-integration/report.md), [sanitizer/source report](docs/validation/salvage/ASSET-03/report.md) | 33 converter + 23 real-tool cooker cases pass in Bazel/CMake and standalone sanitizer workflows. Repeated complete bundle byte-identical. Matrix/texture/tangent defects fixed; unsupported features and unsafe bounds reject. Runtime hierarchy/material appearance remains ASSET-04; visual quality unapproved. Awaiting next verified gate commit |
| DATA-05 | Implementation in progress / root continuing saved simulation_production work; design reviewed | `game_session.*`, bounded transaction/outbox/recovery components and focused tests; root owns shared build/app integration | [Design](docs/validation/salvage/DATA-05/design.md): two ordered preparations, admitted/processed frontiers, exact compensation, private same-identity escrow and explicitly volatile journal | Two ordered preparations, bounded volatile outbox and exact private undo/redo/escrow are implemented. [Compensation checkpoint](docs/validation/salvage/DATA-05/stage-c1/README.md): 42 GameSession + 12 journal cases pass in Bazel/CMake, strict undefined-behavior checks and actual configured WASM; nine publication boundaries allocate nothing. Native/WASM applications compile and application/cove regressions pass. [Coherent checkpoint capture and model transport](docs/validation/salvage/DATA-05/stage-d1/README.md) now pass: 50 GameSession + 12 journal cases in both native build systems, strict undefined-behavior checks and configured WASM, plus seven preserved-allocation-failure checks in both runtimes. [Imported image validation and exact replay](docs/validation/salvage/DATA-05/stage-e1/README.md) now pass 80 shared cases across Bazel/CMake, strict undefined-behavior checks and configured WASM, with 96 admission/172 replay allocation failures preserved in both runtimes. [Fresh local authority recovery](docs/validation/salvage/DATA-05/stage-f1/README.md) now passes 89 shared cases in both native build systems, strict undefined-behavior checks and configured WASM; 297 actual allocation failures preserve input in each runtime. Parent retirement, fresh identity/writer, reserved-ID skipping, crash-cut convergence and local lease revocation are implemented. [Trusted entitlement retirement](docs/validation/salvage/DATA-05/stage-g1/README.md) now passes 98 shared cases, four allocation-free retirement boundaries and 297 preserved recovery-factory allocation failures per native/WASM runtime. Active-loan refusal, selective history invalidation, exact replay, replacement fixtures and both application builds pass. The subsequent [DATA-06 live integration](docs/validation/salvage/DATA-06/stage-b1/README.md) revalidates current authority sources with 122 shared cases, fresh observer identity and 294 current recovery-factory allocation cuts; historical 297 counts remain scoped to their earlier source hashes. Independent transaction review remains; shipping storage and scene restoration belong to later SAVE/SIM/BOOT gates. Root continues locally; no task/gate acceptance yet |
| DATA-06 | Implementation validated; independent review pending / root | `session_events.*`, GameSession/recovery hooks and baselines, app identity/diagnostics, focused tests and shared builds; DATA-04 prerequisite complete | [Design](docs/validation/salvage/DATA-06/design.md), [transport checkpoint](docs/validation/salvage/DATA-06/stage-a1/README.md), [live integration evidence](docs/validation/salvage/DATA-06/stage-b1/README.md) | Stage-b1 passes all 122 shared cases in Bazel/CMake, strict undefined-behavior checks and configured WASM. Each runtime: nine transaction/four retirement publication guards allocate nothing; 26 creation/seven baseline/294 recovery failures preserve input. Baseline overrun/receipt eviction and in-place restart recover without duplicating economics. Both applications build; real hardware browser journey proves quiet Reset, fresh re-entry, single closure and exact inventory. Wire v1 unchanged; earlier manifests retain historical counts/source hashes. Independent [observation review](docs/validation/salvage/DATA-06/REVIEW_HANDOFF.md) and separate DATA-05 review remain; no DATA-06/G01 acceptance yet |
| SIM-01 | Implementation validated; independent compiler review pending / root | Complete CPU mass/collision/buoyancy/functions and immutable assembly/cache identity | [Compiler design](docs/validation/salvage/SIM-01/design.md), [cache grammar](docs/validation/salvage/SIM-01/cache-wire-v1.md), [review request](docs/validation/salvage/SIM-01/REVIEW_HANDOFF.md) | [Stage-e1](docs/validation/salvage/SIM-01/stage-e1/README.md) passes all 78 shared cases in Bazel/CMake, strict undefined-behavior checks and configured WASM; independent 670-byte identity matches both platforms. Each runtime: 74 preserved allocation-failure cuts, 7,000 frame/9,000 socket/1,000 identity reads without allocation. Both core libraries build. Earlier failures remain scoped and retained. Independent review/finding resolution remain; SIM-01 and simulation/product gates stay unchecked. |
| SIM-02 | Preparatory implementation / root; stage-f1 motion/frame conversion verified; SIM-01 review pending | `rigid_mass_frame.*`, `authored_shape.*`, `authored_shape_pool.*`, `shape_handle.hpp`, common geometry, GPU atlas/layout/shaders, facade/backend resource ownership, direct-render stride, tests/build/authority integration; `authored_body_frame.*`, checked sector/COM conversions, corresponding CPU/GPU tests and diagnostic packaging | [Design](docs/validation/salvage/SIM-02/design.md), [stage-a1](docs/validation/salvage/SIM-02/stage-a1/README.md), [stage-b1](docs/validation/salvage/SIM-02/stage-b1/README.md), [stage-c1](docs/validation/salvage/SIM-02/stage-c1/README.md), [stage-d1](docs/validation/salvage/SIM-02/stage-d1/README.md), [stage-e1](docs/validation/salvage/SIM-02/stage-e1/README.md), [stage-f1](docs/validation/salvage/SIM-02/stage-f1/README.md) | Stage-f1 now passes 10 CPU frame and 16 shared GPU cases across both native build systems, strict undefined-behavior/float-cast checks and hardware Chrome, with 16 exact paired readbacks. Checked root/COM motion and point adapters preserve far-sector precision and refuse world/f32 overflow; shipping kernels verify rotated forces and actual sector-crossing integration. Both applications build; live authored-body consumers remain unconnected. Earlier backend-owned resources and facade access now pass 14 shared GPU cases in Bazel/CMake, strict undefined-behavior/float-cast checks and hardware Chrome. All 14 paired readbacks match exactly; 131 native cases pass, including CPU refusal and world-move/drain checks. Both applications build. Actual authority checks match 88 source/header files and 37 translation units without forbidden binary dependencies; generated native headers match. Stage-e1 records fresh versus cached runs, final hashes and preserved failures. No route yet opts into these resources. Stage-d1 retains earlier shipping-kernel and full application lifecycle evidence; the Chrome/Wayland mouse-lock crash remains open under QA-06 and slow X11 captures do not establish performance. Typed body admission, automatic application shape-use declarations, root/COM consumers, retained revision/provenance, loss/recovery, complete geometry capabilities and independent/prerequisite review remain open. No authored-body capability, visual acceptance or gate passes. |
| SIM-03 | Physical skiff sailing and fixed dock collision implemented / root under D18/D20 | Typed dynamic/static authored bodies, owned queue submission, exact exterior scenery union, GPU root rendering and moving player/helm | [Latest dock collision](docs/validation/salvage/SIM-03/dock-collision-r01/README.md), [sailing](docs/validation/salvage/SIM-03/sailing-r01/README.md) | Sixteen focused native tests pass, including actual-hull impact versus a clear-water control. Real browser boarding, sailing, steering, observed boat recovery and two-body drained Leave pass. Both applications build. One pending presentation observation fixes browser readback backlog. No screenshots or gate pass. Next towing/salvage through scheduling, water and campaign identity; complete CCD/containment and review. |
| SIM-04 | Owned ticks plus cargo pose/event consumption implemented; campaign boundary pending / root | Bounded proof/readback queues, actual submission serials, contiguous confirmed events and incarnation checks | [Latest towing and event evidence](docs/validation/salvage/SIM-08/towing-r01/README.md), [initial tick proof](docs/validation/salvage/SIM-04/tick-proof-r01/README.md) | One-slot winch-break test proves pre-encoding backpressure and confirmed event identities. Native and browser gameplay checks pass. Future GameSession publication, per-operation evidence joins, explicit stall/device-loss/overflow fault checks, pause and water epochs remain open. No gate pass. |
| SIM-08 | First physical towing playable / root under D20 | Separate 420 kg generator, authored winch/tow-eye anchors, F/Q/Z and browser controls, cable, two-body water and recovery | [Towing implementation and evidence](docs/validation/salvage/SIM-08/towing-r01/README.md) | 41 native checks, 9 UI checks and 14 real browser input stages pass; both applications build. Hook/reel/pay-out/tow/release/reset/Leave work. Latching, general selection, campaign cargo/rewards, full solver coverage and review pending. No screenshots or gate commit. |
| ASSET-04 | Implementation in progress / root; workers exhausted capacity, continue locally | v2-r04 pontoon and v1-r02 authored hierarchy stand; shared strict loader, registry, bounded GPU owner and real native/browser routes | [Asset specification](docs/validation/salvage/ASSET-04/design.md), [runtime design](docs/validation/salvage/ASSET-04/runtime-design.md), [integration history](docs/validation/salvage/ASSET-04/integration/report.md), [hierarchy checkpoint](docs/validation/salvage/ASSET-04/hierarchy.md), [latest rendered rotations](docs/validation/salvage/ASSET-04/rotations.md) | Guides, six-part/five-weld crossbeam assembly and matched 30-second motion checkpoints are recorded above. New asymmetric stand proves full parent transforms, nonuniform scales, actual shared geometry, outward triangles/normals, all 24 CPU rotations and adjacent-sector rebasing. Three native hierarchy cases pass in each build system; seven registry plus six MeshPath GPU cases pass in Bazel, and 69 shared CPU WASM cases pass without skips. Ten paired views and actual browser mixed-LOD controls/resize/Reset/drained Leave/re-entry/device loss plus original pontoon regression pass. Two clean Blender/cook runs reproduce asset bytes and preview pixels. Sources/package frozen in the linked evidence. All 24 rendered assembly rotations now pass with 34 matched image pairs, eight native admission cases per build system and 70 shared CPU WASM cases. Live camera-sector transitions now pass in both runtimes, with real browser keyboard/socket checks and 54 regressions per native build system; see [camera evidence](docs/validation/salvage/ASSET-04/sectors.md). The [visible hierarchy bound](docs/validation/salvage/ASSET-04/hierarchy-bounds.md) now passes with four paired views and current 70-case WASM checks. [v2-rc01](docs/validation/salvage/ASSET-04/release-candidate.md) is staged with verified source/previews/recipes and unchanged runtime payloads. [Exact final-manifest admission](docs/validation/salvage/ASSET-04/release-runtime.md) now passes: 25 native cases, 70 CPU WASM cases, seven matched image pairs, actual browser lifecycle/device loss and all five inspection routes. Independent moving-image/technical review and immutable publication remain; use the [review handoff](docs/validation/salvage/ASSET-04/REVIEW_HANDOFF.md). No ASSET-04, G02 or LOOK-01 visual acceptance yet |
| LOOK-01 | Walkable cove now has initial physical sailing / root under D18/D20 | Cove controller, authored deck collision, moving player/helm, live GPU mesh poses and existing opaque/water composition | [Current playable milestone](docs/validation/salvage/SIM-03/sailing-r01/README.md), [prior dock movement](docs/validation/salvage/LOOK-01/dock-play-r01/README.md), [opaque/water checkpoint](docs/validation/salvage/LOOK-01/opaque-r01/README.md) | Board and sail with real controls; R recovers player and boat. No screenshots captured or reviewed for sailing. Visual quality, shadows, final materials and independent/human acceptance remain open. Follow D20 and advance gameplay. |
| ASSET-05 | Scoped kit/runtime correction and close-contact inspection / root; ASSET-04 independent review pending under D18 | Nine installed r09 authored parts, 27 LODs, three registries, two exact 11-part/17-connection starters; unchanged pontoon candidate | [Implementation](docs/validation/salvage/ASSET-05/README.md), [r09 correction](docs/validation/salvage/ASSET-05/r09-correction.md), [24 paired close views](docs/validation/salvage/ASSET-05/r09-contacts/README.md) | Geometry/proxy corrections and prior runtime/motion/loss checks retained. Root now viewed all 48 close-contact images at three LODs; current config/startup and browser control regressions pass. Material findings move to ASSET-06. Independent technical review after ASSET-04 acceptance remains. No live compound physics, parent acceptance, publication or gate commit yet. |
| ASSET-06 | Scoped dry materials and filtered scene lighting validated / root under D18 | Surface-detail route uses r04 winch/helm, r02 pontoon and the submitted/cached environment filter; full kit/cove unfinished | [Latest integration](docs/validation/salvage/ASSET-06/environment-consumer-r01/README.md), [material standards](tools/salvage_assets/MATERIALS.md), prior checkpoint links above | 89 focused native checks in both builds; 5 exact consumer readbacks across native/browser; 24 reviewed scene images; 14 controls, drained Leave/re-entry, device loss and legacy mesh diagnostics pass. One 1,228,944-byte filter, 6,714,416 total requested bytes. Remaining parent requirements and independent reviews stay open. No gate or commit. Follow D19: stop repeated full image matrices for minor changes; progress toward meaningful scene/gameplay milestones. |

For each completed task, record the exact tested revision/content identity, commands, actual results, screenshots/captures where relevant, hardware and settings, limitations, and reviewer. For a blocked task, name the missing capability/input and what independent work can proceed. Keep generated media provenance with its assets and durable architectural decisions in this file or repository specifications linked from the task evidence.
